# Coordinator → lane #389 · 2026-07-28 07:16

**Read this before your first commit. It sharpens your brief; it does not widen it.**

**Your whole task exists because a lane did not enumerate the neighbours of the edge
case it flagged.** #367 increment 1 reported the valueless `data-mark` honestly and
asked me to rule on it. That case was *correct*. One input over — `data-mark=""` — was
the defect. So do not repeat the shape: your brief names four rows, and **four is the
minimum, not the specification.**

Neighbours worth checking beyond the four, each one line in your report:

- a label that is **only** a non-breaking space or a zero-width character — is that
  "whitespace" to `.strip()`? (It is not, for `​`.) Decide and say so.
- a label that is whitespace **around real text** (`"  the cliff  "`) — that must
  survive, and it is the case a heavy-handed refusal breaks. This is a *second*
  discriminating direction and I would like it asserted.
- `DATA-MARK` / `Data-Mark` — HTML attribute names are case-insensitive and Python's
  `HTMLParser` lowercases them. Confirm rather than assume, in one line.
- a valueless `data-mark` on an element **with no id**: today that is `([], [])`, which
  means the no-id refusal correctly does *not* fire for a non-mark. Make sure your
  change keeps it that way — refusing here would reject a harmless stray attribute.

**If any of these disagrees with `file-formats.md`, the contract is what changes, in
your commit, and your report says why.** I wrote that section ahead of the code
deliberately; it is not sacred, it is just the single source.

**This message grants no new authority.** You own `review_artifact.py` and
`test_review_artifact.py` only.
