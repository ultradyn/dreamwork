# Review — #1166 round 9 (`cx-1166r9parse` @ `2a1323bfabe1d8f9865ae389f26f30fce6c87349`)

## Why this round is reviewed rather than gated on its report

**A defect here does not show up in this landing.** This code decides whether a lane's WARN row-set
legitimately differs from the baseline — and the gate that lands it runs the OLD code from the main
checkout. The new partitioning first bites on the NEXT gate, against a DIFFERENT branch, where it will
look like that branch's problem. That is why every round of this task gets a reviewer with a fixture
gate, and it is why a clean self-report is not sufficient here.

This is round **nine**. Rounds 1-8 each reported success; three P1s survived into round 8 anyway, found
by review, not by the lane. Treat the report below as claims to verify, not findings to confirm.

## What round 9 claims to have fixed (round 8's three P1s)

Quoted from its report — **each of these is a claim you should reproduce, not accept**:

- **P1a — unanchored path delimiter collapsed two receipts to one identity.**
  Claimed before: two delimiter-bearing receipt renames gave `0/2 -> 0/2, added=0 removed=0, exit 0`
  (the gate saw no change where two occurred).
  Claimed after: `2/2 -> 2/2, added=2 removed=2, gate exit 1`.
- **P1b — `subn(count=1)` normalised an age phrase occurring in the PATH.**
  Claimed before: a path containing the literal text `acknowledgement is 1 day(s) old` caused the
  actual 17-day field to remain unnormalised → `added=1 removed=1, gate exit 1` (a false RED).
  Claimed after: path phrase preserved, only grammar-bound ages canonicalised → `added=0 removed=0,
  gate exit 0`.
- **P1c — global promotion of all after-rows meant a second receipt's first expiry wedged the gate.**
  Claimed before: A already expired while B first expires → `1/1 -> 2/2, added=1 removed=0, gate exit 1`.
  Claimed after: `1/1 -> 1/2, added=0 removed=0, gate exit 0`.

Its stated mechanism: the complete emitted WARN grammar is now full-matched; only captured
`ack_age`/`overdue_age` FIELDS are canonicalised, preserving arbitrary path/reason text; receipt
collisions raise/refuse; and the baseline lint now carries structurally parsed OK+WARN receipt paths,
so per-path promotion can distinguish a NEW tracked receipt from a KNOWN-OK first expiry across
midnight.

## What to attack, in priority order

1. **Reproduce all three before/after pairs yourself.** The before-states are the reviewer's own
   constructions from round 8; if any of them no longer reproduces on this branch for a reason OTHER
   than the fix, that is the finding.
2. **The P1b fix is the one most likely to be over-narrow or over-broad.** "Only captured age fields
   are canonicalised" is a claim about a parser. Try: an age phrase in a REASON string; two age phrases
   in one row; an age phrase in a path AND a real field in the same row; a row whose path contains the
   entire WARN grammar. The question is whether the full-match grammar can be escaped by content.
3. **P1c introduced per-path baseline state.** New state is where new wedges live. What happens when a
   receipt path appears in the baseline as OK and in the after-set as WARN and ALSO gets renamed? When
   a path is present in the after-set but absent from the baseline entirely? When the baseline itself
   is unparseable?
4. **Collisions "raise/refuse" — verify the refusal is reachable and correct**, not just present. A
   refusal that fires on a legitimate case wedges every landing; one that cannot fire is decoration.
5. **The direction the round did NOT claim:** it says "linked-worktree WARN equivalence to master is
   not claimed." Check what that concedes. `#1191` records that `check_landed_guards` is structurally
   blind inside a worktree, and the gate's own lint runs from inside `.worktrees/`. Does any of round
   9's partitioning depend on rows that cannot appear where the gate runs?

## Facts you can rely on (measured by the coordinator, not by the lane)

- Master at review time: `f860a1cb` (the branch rebased cleanly onto it).
- The protected sync-conflict receipt `.git/wt/cache/ci-status/master.sync-conflict-20260729-032627-QJRKU52.json`
  **may be READ** (lint opens and hashes it) but **MUST NOT BE MUTATED**. Its SHA-256 is
  `9da2cc90e5744be8304306e5286ed0715c497ef20947615353354263c4854cf0`; the lane reports it unchanged.
  **Verify that hash yourself and report it** — it is the one irreplaceable artifact in scope.
- The lane reports `947 passed in 324.94s`, guards `5 passed`, lint exit 0 / 11 warnings / 0 ERROR.
- Tests are claimed ADDITIVE: `test_land_lane.py +465/-0`, `test_lint.py +584/-0`. **Verify no existing
  assertion was weakened or deleted** — that is the cheapest way a nine-round task ends up green.

## Constraints

**You are a REVIEWER: read, run, and report. Do NOT fix, do NOT commit, do NOT rebase, do NOT merge.**
Run fixtures in a scratch directory of your own, never against a real branch.
**Never run `dev/land_lane.py` against a real branch. Never run `dev/reap.py`. Never `--force`.**
**No `git stash`** — the stash stack is shared across worktrees.
**Lanes are live: NEVER kill, signal, or disturb a running process**, and never `pkill -f` a pattern
that can appear in another agent's command line.
**THE LEDGER HAS A SINGLE WRITER — THE COORDINATOR**: no mutating `dev/ledger.py` verbs, no writes to
`.dreamwork/ledger.sqlite3`, `.dreamwork/status.json`, `.dreamwork/questions.md`, or
`.dreamwork/handoffs.md`. **Never Read, Edit, or open `.dreamwork/inbox.md` with a file tool** — shell
reads only. **Never write shared git config**; **never write anything under `~/.config`**.

**Do NOT use `attn`.**

## Verdict

End with exactly one of **MERGE**, **MERGE WITH FIXES**, or **ANOTHER ROUND**, then the findings ranked
P1/P2/P3. For each finding give the file, the line, and **a construction that demonstrates it** — a
claim without a reproduction is not a finding. If you cannot break it, say so plainly and say what you
tried; after eight rounds, a clean review that names its attack surface is worth more than a fourth P1
found by guessing.
Review sha (pinned at dispatch, #1056): 2a1323bfabe1d8f9865ae389f26f30fce6c87349 — review THIS commit; state the sha you actually reviewed in your verdict, and report (do not silently resolve) any mismatch with the branch tip.

# Review frame — standing rules for every review dispatch

Concatenate this into every review prompt, the way `frame.md` is emitted into every lane brief.
It exists because the alternative — remembering to hand-write these rules per dispatch — measurably
fails: two false findings in one night (`#1109`), and a third the following review.

Review dispatch is governed by construction. `dev/brief.py --review BRANCH` appends this file
verbatim and persists a receipt under `.dreamwork/review-dispatches/`; `dev/dispatch_lane.py
--review-prompt` is the persist-only check and correctly refuses a runner. To launch, use the
distinct supported path:

    python3 dev/dispatch_lane.py --launch-review PROMPT --review-branch BRANCH --review-round ROUND -- ccc --permission-mode plan @cx-reviewer

That path pins the reviewed commit, creates an **attached review branch** and its own worktree,
records the launch attempt, launches with that worktree as cwd, and sets the reviewer role. Plan
mode is load-bearing: a reviewer reads and reports; it does not receive write permission.

---

## You are working in an attached review worktree. Three things are invisible or misleading here.

The supported launcher creates a review branch at the pinned commit and checks it out under the
sibling `.worktrees/` root. **The branch line is deliberate and load-bearing**: lane containment and
safe reaping can classify the checkout, while the separate cwd lets a review and a gate overlap.
Never replace it with `git worktree add --detach`.

The review worktree still does not make every live coordinator fact visible. Each of the following
has already produced a confidently-wrong finding:

1. **Your reviewer red-proof state is not the author's state.** The registry lives at
   `~/.cache/ud-dreamwork/lane-scratch/ud-dreamwork/<lane>/lane-<lane>-<id>/redproof/registry.json`,
   keyed by lane identity and role. The launcher sets `DREAMWORK_LANE_ROLE=reviewer`, so a bare
   `redproof check` examines the reviewer's registry, not the author's. **Do not report that result
   as the author's red-proof verdict** — use `dev/lane_scratch.py --author-evidence` when the review
   needs the author's persisted evidence, and let the merge gate judge the author's registry.

2. **A sibling branch is not in your checked-out tree.** A search returning nothing proves the
   symbol is absent from **this tip**, and nothing more. Inspect a named sibling with `git show
   BRANCH:PATH`; do not treat the working-tree search as evidence about another branch.

3. **`python3 lint.py` is not necessarily clean in a review worktree, and some ERRORs are
   checkout-state artifacts** — the
   tracked `tasks.md` is a migration notice, the gitignored ledger store does not travel, and
   worktree-drain state is stale. Compare **WARN row SETS** against local `master`. Never report
   absolute warning counts, and label any review-worktree-state ERROR as such rather than as a
   branch defect.

**Report, do not suppress.** The instruction is to mark these **unverifiable-from-here with the
reason** — not to stay silent. A reviewer that reports nothing is worse than one that reports a
false FAULT. If something looks clone-shaped but you have direct evidence it is a real defect, say
both: what you saw, and why you believe it is not an artifact.

**Hash spaces are a related trap.** `redproof` pins `sha1(content)`. Git names a blob
`sha1("blob <len>\0" + content)`. They are different spaces; comparing one to the other proves
nothing, and `git cat-file -t <content-sha1>` failing is the expected result, not evidence of
corruption.

---

## Naming conventions that make a true search read as a phantom

In this repo a **PascalCase** name is a React wrapper under `dev/build/`, and its **camelCase**
counterpart is the builder under `client/`. `dev/build/wrapper-exports.js` states the mapping
outright (`QaCard.dwBuilder = 'qaCard'`). Searching one case in the other directory returns a true
"not found" that reads as "this symbol is fictional". Check the convention before concluding a
symbol is absent.

---

## Staleness is not a finding

The branch may sit on an older master than today's tip. Rebasing is the merge gate's job. **Judge
the diff**, not how far behind it is.

---

## What a finding must contain

Concrete, located, checkable. For each: the file and line, what is wrong, the evidence you actually
ran, and what would fix it. Distinguish **P1** (must fix before merge) from **P2** (should fix) from
**Standards** (nit). If you cannot substantiate something, say so plainly rather than softening it
into a claim.

End with one verdict: **MERGE**, **MERGE WITH FIXES**, or **ANOTHER ROUND**.

---

## Hard rules

- **Do NOT use `attn`.** Only the coordinator contacts the human.
- Do not write anything under `.dreamwork/`.
- **THE LEDGER HAS A SINGLE WRITER — THE COORDINATOR. Run no mutating `dev/ledger.py` verb**, including
  `file`, `note`, `fold`, `block`, `retitle` and `reprioritise`. This is the rule above restated as a
  verb, because that is how it gets broken: the store is `.dreamwork/ledger.sqlite3`, so filing a task
  *is* writing under `.dreamwork/` — but it does not feel like writing to a path, it feels like filing
  a follow-up, and a reviewer who would never touch that directory will run `ledger.py file` without
  noticing the rule applies. `#1071`'s round-2 review filed `#1186` exactly this way. Read-only verbs
  (`get`, `list`, `counts`) are fine.
- **Follow-ups belong in your report, not in the ledger.** Write the title and the body you would have
  filed; the coordinator files it. Nothing is lost by this and the concurrent-writer hazard goes away —
  the coordinator is writing that same sqlite store while you run.
- Do not commit, merge, or push. Your report is your stdout; the coordinator reads it.
