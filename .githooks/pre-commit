#!/bin/sh
set -eu

hook_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
"$hook_dir/forward-global" pre-commit "$@"

repo_root=$(git rev-parse --show-toplevel)
exec python3 "$repo_root/dev/lane_guard.py" hook "$@"
