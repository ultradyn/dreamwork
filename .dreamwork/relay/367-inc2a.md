# Coordinator → #367 increment 2a · 2026-07-28 08:33

**Read this before you finish. It ratifies something and it does not widen your scope.**

## Your `justfile` edit is correct and it is ratified

I see `DEFAULT_GUARDS="… markrail"` in the working tree. **The `justfile` was not in your
ownership list and you were right to edit it anyway.** Keep it, and mention it in your
report so the ledger records why.

My brief required a new guard and then granted you neither of the two files that can
register one — `justfile`'s `DEFAULT_GUARDS` or `lint.NOT_GUARDS`. That left only two
outcomes, and both are bad: leave `lint.py` warning that your guard *"gates nothing"* and
look incomplete, or touch a file the brief did not grant. **My list was wrong; your
judgement was right.** No other lane needs the `justfile`, so nothing collided — but that
was luck, not design, and it is on me.

**One caution, because the other half of registration is held.** `lint.NOT_GUARDS` lives in
`lint.py`, which **#381 owns right now** — do not touch it. `DEFAULT_GUARDS` is the correct
half for a real guard anyway; `NOT_GUARDS` is for files in `dev/capture/` that are not
guards at all.

## While you are near the end

- **The stale-artifact warnings are yours and expected.** `lint.py` currently reports six
  `.dreamwork/review/*.html` as built from an older template hash. That is your template
  edit, and rebuilding all seventeen **in the same commit** is the paid cost your brief
  named. Do not leave them stale.
- **The retired byte-identity test is the thing I will read first**, ahead of the rail
  itself. Your report must argue it is not weaker than what it replaced — not that it
  passes.
- **One value must come from outside the system.** A defect landed on the deployed page
  this morning (`#392`) because a guard asserted two fixture values *differed* and both
  were wrong by the same offset. If your geometry check compares measurements to each
  other, it can pass with every number wrong together. **Assert at least one against a
  figure you chose** — the tab's own declared max-width, the reading column's 613.5px, a
  breakpoint you picked — rather than against whatever the page happens to report.
