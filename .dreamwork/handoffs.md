# Hand-offs — work landed outside the ledger, waiting to be folded

A session that lands work it does **not** own `.dreamwork/tasks.md` for —
every session but the coordinator — appends ONE line under `## Pending`.
The coordinator reads it on every tick, folds each landing into the ledger,
and appends one `→ folded` line under `## Folded`. **Nothing ever moves**:
both sections grow only by append, so two sessions landing work at once
cannot lose each other's line — the property the dreamer inbox has and a
rewrite would not (#381).

**Section order (#406):** `## Folded` first, then `## Pending`, so an EOF
append lands under Pending and the instruction is true without a rewrite.

**Provenance notice (#469, 2026-07-29). `by grok` in a row below names the
HARNESS, not the model.** `ccc`'s `runner` field is the CLI harness: both
`@grok` (grok-4.5) and `@glm52` (glm-5.2) run the grok CLI, and the harness
exports only `CCC_PROVIDER` to the child — never the alias, never the model.
So **a lane cannot know what model it is**, and every self-reported model
name is a guess the harness's system prompt supplied. Two `@glm52` lanes
introduced themselves as *"grok-4.5 (xAI)"*.

Rows written before this notice therefore mean *the grok harness, model
unknown*, and they are deliberately **not** being back-filled: the dispatch
alias is not recoverable after the fact and a model attribution is history,
which is never guessed here. From now on **the dispatcher records the alias
it passed** — it owns that argument — and the model is derived from
`~/.config/ccc/config.toml`. Write `by ccc @glm52 (glm-5.2)`, never a name a
lane reported about itself.

One line per landing, mirroring the inbox's one-line-per-report shape.
Required: the task id (plain `#N`, sub-id `#Na`, or combined `#N/#M`), the
**sha** (what landed), who is claiming it, and a one-line `what`. The
`→ folded` line is how the writer marks one consumed, and it is itself an
append — never a deletion — so a folded hand-off is not flagged twice.

## Folded
- **#517/#518** → folded (2026-07-30 07:52): merged `0ac902bf` (lane `1f105aec`, lane-517doc, glm-5.2) — delivery-modes.md caught up to the landed #342b code. #518: the stale 4-route enumeration replaced by a 9-row `WRITE_ROUTE_HANDLERS` table, each handler verified while writing (GATED ×4 COMPLIANT; `/decide` NOT GATED with #515 named as in flight — no future asserted as present; `/tint`+`/deploy` non-waking by design with the reasons). #517: new "Wake channels outside the route table" section — control-plane wakes (`/run-mode`, `/posture` triple + delivery axis) as the explicit always-instant carve-out (a delivery toggle that waits for the tick defeats the toggle), journal failure-path wakes as always-loud error diagnostics, the migrate one-shot noted, `question-updated` deferred to #516 in flight. Review-gated by the coordinator: full diff read; six line citations spot-checked against master — all exact (`WRITE_ROUTE_HANDLERS`:14541, unguarded `/decide` log_event:14334, four handler defs). Doc lane: no test red-proof applies.
- **#512** → folded (2026-07-30 07:36): merged `a0bf2fc` (lane `d9f555cc`, lane-512show, glm-5.2) — `journal_consume show <receipt-id>`: read-only full-payload read via the single public `Journal.get_receipt` (cursor-independent, so already-consumed receipts read back — consume moves only the cursor). Header + verbatim UTF-8 payload (the deliberate #126-collapse exception: this output is for READING a multi-line instruction, not a line-log). Unknown id / absent journal → one-line stderr + EX_USAGE, no db created (#501 discipline). 10 passed; coordinator independent red at `if receipt is None:` → `if False:` → exactly `test_show_unknown_receipt_exits_usage` FAILED; cp-restored byte-identical. Live dogfood in the same tick: `show 36d7e68d-c630-…` printed the blindly-consumed ord=39 human instruction in full — the incident that filed #512 is now a one-command recovery.
- **#508** → folded (2026-07-30 07:32): merged `5ad3dc0` (lane `2d75d111` `db9ef405` `d45382e0` `b8d503ea`, lane-508deploy, glm-5.2) — `just deploy` success is now IDENTITY, not liveness: `verify_deployed` returns 0 only when `listening_pid(port) == expect_pid` AND its argv runs the snap (the pid is load-bearing because autoreload's in-place re-exec keeps the old argv identical to the new snap); `wait_port_free` requires the port to STAY free (settle defeats the close-on-exec execv flicker that `stop_deployed`'s 50ms poll could grade as free); the recipe drops `--dev` from the deployed start (autoreload was the re-exec enabler and serves nothing on a frozen snapshot the recipe restarts; GENERATION still bumps, tabs still reload). Mechanism confirmed in a fixture before any fix. 9 new tests, each red-proved by the lane. Coordinator independent reds on the MERGED tree at lines the lane did not inject: `if holder != expect_pid:` → `if False:` → exactly `test_verify_deployed_refuses_foreign_holder` FAILED; settle-check disabled → exactly `test_wait_port_free_settle_rejects_the_execv_flicker` FAILED; both cp-restored byte-identical. Gate found one real suite failure — my own diff3-conflict resolver left `|||||||` base markers + stale duplicated lines in handoffs.md (the resolver predated diff3 style; lesson material) — cleaned, grammar fixed, silence test green.
- **#514** → folded (2026-07-30 07:27): merged `df5e764` (lane `9ad47c76`, lane-514wake, glm-5.2) — wake-semantics audit under `delivery: batched`, static read-only, all 13 `log_event` sites + 1 raw append traced. Verdicts: 4 COMPLIANT (`/command` by kind, `/answer`, `/ask`, `/comment`), **2 VIOLATION** — F1 `/decide` wakes unconditionally (journals a receipt but the #342 gate missed it; silently undoes batched mode for review decisions) and F2 `question-updated` ungated + unjournaled (the batched-class question signal interrupts the loop; possibly #509-adjacent) — 7 UNCLEAR (control-plane, journal failure-path, migrate one-shot). Review-gated by the coordinator: full 222-line doc read, claims spot-checked against watch.py (`emits_wake`:13418, `PREEMPT_KINDS`:13406, unguarded `_handle_decide` log_event, `question-updated`:12917 — line drift +15, claims accurate). Follow-ups filed #515–#519 (F1→#515 P1, F2→#516 P2, doc gaps→#517/#518 P3, do-now exactly-once→#519 P2). Audit lane: no test red-proof applies.
- **#505** → folded (2026-07-30 07:06): design, merged `5eab68f4` (lane `e55791eb` `87092028`, lane-505arch, glm-5.2) — `render-architecture.md`: the wholesale-rerender smell designed against a MEASURED 16-surface reset inventory (1 is his bug — prose selection inside `#view`; 11 re-applied by hand post-swap; the chrome survives because `renderChrome` is already a keyed diff). Real IGC (I1–I5 × G1–G5): his three directions refuted as standalone fixes (ids alone don't survive the swap; content-check is an accelerator not a fix; React/preact = build step + second render authority against his own single-file architecture, pushed back honestly as invited). Survivor: keyed reconciliation of `#view` (morphdom-idiom) + content-hash skip — generalises `renderChrome`, subsumes #141/#503/#494 (`data-keep` retained as a reconciliation key), every transitions.md family's fate named. Review-gated by the coordinator (full doc read, IGC structure verified, claims spot-checked against watch.py: `setContent`'s `innerHTML` swap, `renderChrome` keyed reuse + content gate, `data-qid`/`data-keep` identity attrs, `.qaghost`, watch-design.md:2741 single-file; line drift +6/+13 from later merges, claims accurate). Q1–Q4 surfaced in questions.md with declared sub-decisions (recs: vendored morphdom; hold no-build; `#view` first; guard the corpse rule). Doc lane: no test red-proof applies. **`#505` stays open** — implementation follows his ruling.
- **#357** → folded (2026-07-30 07:02): implementation, merged `33b754ae` (lane `c952fe05` `7eac5e2d`, lane-357impl, glm-5.2) — `dev/ledger.py`'s `emit_warnings()`: the warning footer on every verb's success path, stderr-only (stdout stays pipeable), stateless, exit-transparent, seven counts each from its PRODUCTION reader (`store_ids_by_state`, `lint.check_unfolded_answers`, `watch.parse_open_answers/parse_open_questions`, `open_journal` head−cursor) — never a second copy. Q5/Q6 rulings implemented as settled (every verb, full line, no throttle). **Green red-run at the gate — a finding, acted on**: the `if rc == 0:` gate was bound by none of the lane's 7 red-proofs (sabotage to `if True:` passed all 7); coordinator added `test_footer_absent_on_a_failing_verb` (`64d8a3e7`), red-proved by the gate's own injection (FAIL line 188), cp-restore → 8 passed, full suite 1526. First live dogfood: the store `fold` for this very task printed `warnings: 127 open tasks · 4 unanswered questions · 234 untyped · 107 missing origin` — the counts are real and immediately informative. **`#357` fully landed** (design + impl); the untyped/missing-origin hygiene counts it surfaced are follow-up material, not this task.
- **#511** → folded (2026-07-30 06:53): merged `b498e690` (lane `99e2d9c0`, lane-511live, glm-5.2) — master red cleared. Option (a) re-point of the four live migrate acceptance tests at `tasks.md.deprecated` (the frozen post-cutover ledger); the history test splits text (frozen) from git walk (the `tasks.md` path's 678 commits) and its verify moved to `verify_db` with the rich snaps because the CLI couples text+walk through one `--ledger` path the cutover split. Preconditions asserted at runtime (`## Open` present; `len(snaps) > 1`). Coordinator independent red at a tool line the lane did NOT inject (`ud-dw-tasks-migrate`'s groomed-conflict `_flag`): **first sabotage GREEN — a finding, acted on**: `False and X or Y` precedence left landed-section ids flagged, the injection never reached the branch; corrected to a full disable → FAIL at line 419 (`assert groomed`); cp-restore → 64 passed. Full suite 1518 passed. Collateral: the coordinator's own #498/#499 fold lines violated the handoffs by-claimer grammar ("folded by coordinator"), lint's live-silence test caught it — fixed `99761f4a`.
- **#506** → folded (2026-07-30 06:12): merged `76fd99c4` (lane `984fa9b1` `29fc4cce` `dd208e4d`, lane-506pip, grok-4.5) — his 03:54 do-next. `linkify` emits `pipBtn` immediately after every known-internal file link it promotes — the same `linkable_paths` closed set decides link AND pip (no second list); pip outside the backticks (chrome, not copy) inside a `.mdfile` nowrap unit so the glyph never orphans; `user-select:none` so a drag across prose never swallows the button (#505); reduced-motion drops only the colour transition; external github.com links get a link and no pip (a pip floats a local view). Guard `qlinkpip.mjs` registered (68) with both link kinds DERIVED from live data.json at runtime (known `.dreamwork/lessons.md` + unknown `nosuch/vanished.md` planted in one fixture question body — a green without the pair is vacuous). Coordinator independent red at a line the lane did NOT inject (lane injected the pipBtn call): **`user-select:none` removed from `.pipbtn`** → exactly the user-select check FAILed, restored byte-identical → qlinkpip PASS. Visual verdict by the coordinator (desktop rest/hover, mobile rest): pip quiet at rest, accent on hover, unit holds at 390px, unknown stays code. 372 passed. **`#506` stays open** only in that #505 (the general render-architecture smell) is the proper-home follow-up.
- **#510** → folded (2026-07-30 06:10): design, merged `7ebf431f` (lane `9e1491e5`, lane-510orc, glm-5.2) — his 04:54 do-next. `orchestrator-posture.md`: the referent investigated (his "opus 5 claude orchestrator" usage at questions.md:1119) — designed against orchestration MODE (coordinator implements nothing, only dispatches+reviews; witnessed orthogonal to `delegation` — coordinator-only mode held constant while fleet size moved 2→5 in dogfood-orchestration.md); identity escalated as Q1 with a rec against (provenance owned at dispatch per ccc-runner-routing.md, not a posture dial). Real IGC I1–I5 × G1–G5: **a fifth posture axis is the sole survivor** — fold-onto-delegation refuted (a number can't encode a mode, the #443 defect), `hierarchical` run-mode value refuted (re-bundles #443 + preempts), control-only refuted (no restart-survival, #426), sibling file refuted (the #445/#342 widen-not-sibling standing ruling). Q1–Q3 surfaced to him in questions.md with declared sub-decisions (recs: mode; binary `hands-on`|`orchestrator` absent=hands-on; land the axis, don't enable `hierarchical`). Doc lane: review-gated (full doc read, IGC structure verified, factual claims spot-checked against lint.py/watch.py); no test red-proof applies. **Dispatch incident recorded in lessons.md**: this lane and lane-511live went out without `isolation="worktree"` (coordinator error) and ran git in the main checkout — topology repaired (lane commit preserved on lane-510orc, coordinator commit rebased home, stray branch deleted); the lane's own recovery was graceful. **`#510` stays open** — impl split (subagent webui, coordinator docs) follows his ruling.
- **#501** → folded (2026-07-30 04:58): merged `fa61998f` (lane `41eec321`, lane-501consume, glm-5.2) — `dev/journal_consume.py pending|consume`, the batched-delivery drain half of #342. Thin composition over the landed `events_since_cursor` projection and the `advance_cursor` CAS — no `user_events/` change, no new journal query; the atomicity seam (read and advance are two calls) is named in the module docstring and proved safe-by-construction (append-only chain; only an alteration refuses, which no public API can induce — the test reaches it by direct SQL corruption). Five red-first tests seeded via the production `receive()` path with derived preconditions. Coordinator independent red at a line the lane did NOT inject (lane injected: empty-path write, pending-loop skip, advance-in-pending, advance-removed, refusal-swallowed): **`expected=head.event_hash` corrupted to a wrong hash** → exactly `test_consume_advances_then_pending_empty` FAILED (exit assertion, line 277), restored byte-identical → 5 passed. Live first use the same hour: he set `delivery: batched` at 04:52 and the coordinator's first `consume` drained 37 historical events to set the baseline; `pending` now reads empty. Full suite post-merge: 2 pre-existing reds in `test_tasks_migrate_verify.py` (stale against the #294 shim — filed `#511`, verified identical at pre-merge `76367115`), nothing from this lane.
- **#504** → folded (2026-07-30 04:50): design, merged `12fd763c` (lane `08b031dc`, lane-504chat, glm-5.2) — his 03:39 add-idea. `composer-chat.md` reconciles the third chat ask against the four approved directions that bind it (#229/#270 v2, #253, #263, #342); a real IGC (A/B/C × G1–G5) decides his `chat` command **is** the #229 main-dreamer first slice — no parallel store, no second inbox, reply via the CLI-only seam. His "get unread at loop start" is, by name, the #342 cursor read (lane-501consume's `journal_consume pending`). Pushback kept: "just in case" understates the receipt, which under #342 is the delivery path. Four forks surfaced to him as a questions.md entry with declared sub-decisions Q1–Q4 (recs: `/command` chat kind; "topic chat" UI word; batched delivery; minimal dashboard chat list). Doc lane: review-gated (full diff read, IGC structure verified, claims spot-checked against #229/#342); no test red-proof applies. **`#504` stays open** — implementation is #373's, gated on his Q1–Q4.
- **#503** → folded (2026-07-30 04:28): merged `6584c7e7` (lane `29c673f7`, lane-restcollapse, grok-4.5) — his 03:30 do-next. Root cause confirmed by repro: `expand()` emitted no `data-keep`, so `snapshotFolds`/`restoreFolds` (#141) never saw the peeks and the ~2s poll re-render detached the open node (his 03:39 data.json suspicion was right). Fix reuses the #141 idiom with stable keys (`status-rest`, `file:*`, `dream:*`, `dreams-archive`); restore is silent; peeks also now travel on his gesture via `foldDetailsLocal` (the brief's own sanction; `transitions.md` + `watch-design.md` updated same commit). Guard `restcollapse.mjs` registered (67). Coordinator independent red at the `restoreFolds` CALL SITE (the lane injected at the emit site): guard FAILed all four assertions, restored byte-identical → PASS. **The merge surfaced a stale full-call literal pin** in `test_page_reflows_prose_but_not_raw_text` (broke on the new keep args while asserting the unchanged thing); coordinator repinned to the `mdB(d.files[n])` prefix and red-proved the repin (renderer sabotage → FAIL; restore → 372 passed). Full suite: 372 passed; guard suite's burndown+markrail reds triaged to chronic load (identical at pre-merge, at deployed `d62265d9`, and at merged master) and filed as `#507`.
- **#496** → folded (2026-07-30 03:55): merged `d0dddc62` (lane `c691f34`, lane-496routes, glm-5.2) — E2Shadow extended to `/decide` (#289, the genuinely unshadowed route; the brief guessed `/deploy`, already exercised — the lane halved the brief's assumption and was right to). Literal alarm 8→9 kept literal with the born-hollow warning comment; `REVIEW_DECISIONS` closed set asserted at runtime; stale "six routes" docstring fixed. Lane red-proofs both directions (literal→8: `9 != 8`; `/decide` removed from the table: 404 in the status list). Coordinator independent red: `_journal_receive` neutered to `return None` → `E2Shadow.test_every_write_route_commits_a_receipt_and_changes_nothing_else` FAILED (all 503s), restored byte-identical → 15 passed. Folded to landed in the store.
- **#357** → folded (2026-07-30 03:45): design increment, merged `b32ea47b` (lane `ecac44a0`, lane-357throttle, glm-5.2) — the Q5 ruling recorded (footer on EVERY verb, ruled 03:11) and his throttle sketch evaluated with a genuine IGC: **refuted** on statelessness (needs memory a stateless verb process cannot hold), never-suppress-unseen (drift state hides a warning forever), and surface-early (a throttle delays the thing he asked to hasten — the sketch is internally contradictory, the headline he asked to have surfaced). The surviving fork (full line vs terse hint on read verbs) escalated as **Q6, now in questions.md** with rec full-line. Doc-only lane: review-gated (full diff read, IGC structure verified, factual claims spot-checked); no test red-proof applies. **`#357` stays open** — implementation behind Q6.
- **#500** → folded (2026-07-30 03:25): first slice, merged (lane-500bridge, worktree) by glm-5.2 — the bridge plugin scaffold (`plugins/ud-dreamwork-matt-pocock-skills/`: SKILL.md, `tracker_adapter.py`, T1–T5 seam checks); the adapter maps tracker ops onto `dev/ledger.py` verbs (create→`file`, close→`fold --note`, list_open→`counts`, injectable runner) and grill questions onto the production `watch.human_block` seam (new `_insert_first_open_entry` helper — no production "new Open entry" helper existed; flagged as a seam the core may want). Authority floor only: no activation, no lifecycle hooks. **Coordinator red-proof was a GREEN RED-RUN and the repo rule fired**: injecting `fold`→`note` in `close()` passed 11/11 — T1–T5 never asserted WHICH verb close/list_open invoke. Finding acted on coordinator-side: T1b argv-mapping checks added (`e5c5cf51`), red-proved against the identical injection (exactly the new test failed, showing the real argv), restored byte-identical, 13/13 green. The lane also never appended its Pending line here — recorded by the coordinator instead. **`#500` stays open** — activation, lifecycle hooks, grill-skill wiring remain.
- **#342** → folded (2026-07-30 03:05): lane B, merged (lane `b1cb91a6` `62eb4177` `58afee93` `d6b7a2a9` `0eff7602`) by glm52 (lane-342b, worktree) — the delivery posture axis (instant|batched, absent=instant; lint closed-set + file-formats in the same commit), per-kind wake routing (`emits_wake`: receipt unconditional, wake conditional; do-now/do-next pre-empt even batched), and the picker chip on the shared 10s arm (no second gesture). Lane red-proofs per surface incl. catching its OWN hollow POST-body red-run twice; coordinator red-proof: `/comment` gate removed → exactly `test_answer_ask_comment_withhold_wake_in_batched` FAILED, restored byte-identical. Guard gate: summaryjson caught the new axis (whitelist fix `d62265d`) + a pre-existing `research` mirror drift (#484). Deployed `d62265d9` on :35110. **`#342` stays open** — the tick-consume habit is `#501`.
- **#495** → folded (2026-07-30 02:10): merged `08e8d790` (lane `6aee7d9`); the lane's repoint at the frozen `tasks.md.deprecated` plus store materialization via the REAL `perform_cutover` verified by my independent red — `_metadata_clause`'s body-break removed in `lint.py` failed exactly `test_the_four_false_positives_stay_silent`, restored byte-identical; 373/373, lint clean. Model corrected per the dispatch record (glm52, not the lane's self-report — `ccc-runner-routing.md`: a lane cannot know its own model).
- **#413** → folded (2026-07-29 03:41): merged `ac6bcf3` (`4966d9c`); 6 fakes inventoried, 0 unverifiable, and its refutation of my brief kept — the motivating fake's 409 was never stale, the blindness was its SCOPE, so a stale-literal checker would have passed over the instance that motivated the task. Two lanes widened `health.mjs` for the same fact; resolved as a union. Verified: 20 PASS, and reinstating 409-only gives exit 1 on the coverage and message checks while the two state assertions stay green — the empirical case for the coverage assertion being the load-bearing part.
- **#263f** → folded (2026-07-29 03:36): merged `8ccd2fb` (`E4` `0024ad2`, `E5` `a67f308`, `E5b` `a328507`) into `#263`'s entry. Merge deliberately held 35 minutes until the client half existed. Verified in an isolated worktree: reverting `writeVerdict`'s `landed` to `res.ok` fails 10 of 12 `rejectwrite` checks including the permanent-loss vector, success-side check still passing; 12 PASS on the merged tree. Guard count 56 after a union resolution. `E6` is the remainder.
- **#461** → folded (2026-07-29 03:21): the corrected scope is three guards — `health`, `pushhealth`, `fileimg` — all converted, nothing left to roll out. Batch 2 rejected (`1197d41`, branch discarded): its red-proof aimed at an `argv[3]` pin the conversion itself added, and that pin would have reddened eight guards under `just guards`, which hands every guard the port the shared server already holds. Measured: converted `morph` exits 1, master's exits 0.
- **#461r** → folded (2026-07-29 03:03): merged `8e7ea50`; 8 of 18 own-server guards adopted, remaining eight named as a list rather than a count. **`#461` stays open.** My independent squatter proof came back GREEN first — I squatted 39782 while `gitrow` serves on 39781, because its port arithmetic is direct rather than `++port`; on the right socket it exits 1 and names the wrong target.
- **#462a** → folded (2026-07-29 03:03): merged `b1551b1`; the copyable remedy on the staleness row, guard `staleremedy` registered (55) and 11 checks verified by me on the merged tree, my own red deleting `revealStaleAction()` failing exactly the two motion checks. **`#462` stays open** on his consent call (asked 02:56). The lane withdrew its first refutation after pushback and found the real one: a deployed server's `__file__` is the snapshot, so re-exec is byte-identical.
- **#458** → folded (2026-07-29 02:42): merged `327345b` into `## Recently landed`; verified independently by replacing `strip_notice`'s `_BLOCK_RE.sub` with `return text` (4 tests red, incl. the shrink proof) and by making `NOTICE_OPEN` a visible ledger head so both `TestIndifference` tests red on real `lint.LEDGER_ID` / `watch.parse_ledger` id sets. Restored byte-identical, 15 pass. `#294` unblocked on this channel.
- **#445** → folded (2026-07-29 02:42): merged `1462aeb`; **`#445` deliberately STAYS OPEN** — a design is not a ruling, and it is now asked with `Q1`/`Q2`/`Q3` declared. doc-map conflict resolved as a union (30 == 30 both directions). Artifact verified by me: one real `#ask`, both metas, no stray `>`, fixed table layout.
- **#263e** → folded (2026-07-29 02:27): merged `df2989e` (`E1` `69b8573`, `E2` `d460947`, `E3` `38ef409`) into `#263`'s entry; verified end-to-end by the coordinator against a server whose pid was asserted from `ss -ltnp` — `POST /command` → `202`, `Location: /user-events/<id>`, receipt id present in `receipts`/`transitions`/`events`. Two earlier probes reporting `200` were reaching a stale lane server, not this code; lesson recorded. Successor: `E4`/`E5`/`E6`, then lanes G and H.
- **#420** → folded (2026-07-28 15:55): merged `61354fb` into `## Recently landed`; its ten stale blockers re-verified with `parse_ledger` and nine cleared in `30458af` (`#172` was the tenth and closed on its own). Its `#419`-reverse-direction finding is folded into `#419`'s entry as that check's red fixtures.
- **#172** → folded (2026-07-28 15:50): merged `775022d` into `## Recently landed`; his edge-pin rule verified by three route rectangles rather than a screenshot (identical box on `/` and `/questions`, trailGap 0 on a long `/review` where the bar widens), guard `projtitle` registered and red-proved on six named assertions. Refuted my brief's claim that the tab title lacked the project name — `#153` had already put it there, which is why he believed the work was done.
- **#402a** → folded (2026-07-28 15:45): merged `f1f269b` into `#402`'s entry; `gate402a.py` PASSED against the merged tree with the pre-merge baseline, and the fix verified on REAL data — the first post-merge run reported `current_task_ids [] -> ['172', '420']`, found both live lanes and pruned the dead `402a` one. Its refutation of my gate's stale-pid premise is recorded in the entry; `#402` stays open for the `file-formats`/`lint` vocabulary (`#402b`, which collided within a minute) and for `awaiting_human`, still hand-written.
- **#367** → folded (2026-07-28 15:45): merged `98670ae`; **`#367` deliberately STAYS OPEN** — this landed the option previews he asked to see, not the feature, and he has since ruled (C with a collapsible chevron index, collapsed by default). The hand-off line says *landed* because the grammar has no word for *increment 2 of n landed*, which is `#415`; that gap is what made `test_lint.py::TestHandoffs` red on master until this line existed.
- **#411** → folded (2026-07-28 14:28): merged `1f01a95` into `## Recently landed`; gate re-run on the merged tree against an explicit pre-merge baseline — 2 recovered, 44 dated unchanged, None 5→3, bait probe still inert
- **#331** → folded (2026-07-28 13:18): merged `cb476a7` into `## Recently landed`; gate re-run on the merged tree with an explicit pre-merge baseline — 19 ids recovered, landed 152→171, span core 4 copies → 1, all three heads pinned including `status_sync`
- **#399** → folded (2026-07-28 12:24): merged `0595b13` into `## Recently landed`; burndown green on the merged tree, and the 3 remaining `just test` failures verified pre-existing on master; residual space-separated multi-id gap filed as #412
- **#398** → folded (2026-07-28 09:31): folded into `## Recently landed` citing `9f2012a`; verification owed
- **#397** → folded (2026-07-28 09:52): folded into `## Recently landed` citing `1b508b0`; recommendation (do-not-extract) accepted, no ruling requested, worktree alternative filed as #405
- **#401** → folded (2026-07-28 09:58): audit half folded into the `#401` entry citing `f2c950e`; fix half stays open, still needs `watch.py`
- **#392a** → folded (2026-07-28 10:00): verified and closed against `#392`'s entry citing `159917b` — independent red taken in a worktree, deployed page checked (38/38 day-precision, zero two-figure). Misfiled Pending-shaped line was the `#406` fixture; relocated under Pending after the check went red-loud.

- **#263g** → folded (2026-07-29 01:52): the artifact increment is consumed; **`#263` itself stays open by design** — its second gate opened at 01:37 and lanes **E**, **G**, **H** are now authorised, with lane E (increments 20-22) in flight. The gate artifact is recorded in `#263`'s entry as the decision-taken record it became, not as a closing commit.
- **#269v** → folded (2026-07-29 01:52): the verification is consumed into `#269`'s entry as the 01:43 correction — acute draft durability is **(b) partly shipped**, restart-verified, `localStorage`, no debounce. **`#269` stays open by design**: the authorised remainder is the IndexedDB upgrade, cross-tab `R1` and 30-day GC, with `#459` (`#askbox`, popout `#ptext`) sequenced ahead of it.
## Pending

- **#398** · landed `9f2012a` · 2026-07-28 09:26 · by ccc @grok — lint check: post-obligation briefs must mention handoffs.md

- **#397** · landed `1b508b0` · 2026-07-28 09:42 · by ccc @glm52 — design plan: client extraction is mechanically cheap (interpolation count 1) but leans do-not-extract; does not unblock #331/#352 (Python), multiplies the registry damage class, and four named breaks (deploy/serving/autoreload/audit) must ship together if ruled in
- **#401** · landed `f2c950e` · 2026-07-28 09:47 · by ccc @grok — research: executed id-grammar matrix (14 patterns × 17 forms); #401 silent drop reproduces; 7 silent rejects ranked
- **#392a** · landed `159917b` · 2026-07-28 09:43 · by ccc @glm52 — date-only question ages show ONE figure (paintDayAge, data-day flag); today reads "today"; timed commits keep two figures
- **#401** · landed `e53d70c` · 2026-07-28 10:09 · by ccc @grok — hand-off id grammar accepts plain/sub-id/combined; malformed outside section; Folded-then-Pending so EOF append works (#401+#406)

- **#399** · landed `ddc6614` · 2026-07-28 12:08 · by dreamer-399b @grok — _landed_ids counts the historical inline form again (col0 + ref-field exclusion keeps #367 closed); burndown + forgotten_folds both green, 3 opposite-direction reds proven; just-test exit 1 is 3 pre-existing/unrelated guards (qacard=#392, docktarget/noteprop=motion flake under load) proven identical on master

- **#331** · landed `ddc4e3e` · 2026-07-28 13:13 · by dreamer-331 — one IDS_ONLY_SPAN core in watch.py, consumed by lint.LEDGER_ID and status_sync.LEDGER_HEAD; landed 152→171, open 135, all 19 joined-span ids recovered; red-proved both directions; the three guard reds (qacard/docktarget/noteprop) reproduce at parent 97becd9 and were fixed on master by 7007d5b+e15b0c0 (guard-contract fixes, not this parser change)
- **#411** · landed `25a3fe4` · 2026-07-28 14:08 · by grok (wt/411) — (also `54c68e8`) answered_at anchor \A→^+re.M+.search so the 2 second-line markers recover (5→3 None); 44 dated byte-identical; lint WARN with derived count; both red-proofs discriminating

- **#367** · landed `a36c674` · 2026-07-28 15:02 · by grok (wt/367p) — option previews A/B/C at true below-cliff geometry; chrome measured at load (A 167.9 / B 127.2 / C 31.8 px at 780); 16px margin holds at 640; row-count red-proof 3→1 rows
- **#402a** · landed `78be285` · 2026-07-28 05:32 · by grok (wt/402a) — status_sync liveness is pid-primary (kill -0) with brief-path fallback; `^ccc @` -> broad `ccc`+brief-substring so a flag between binary and alias no longer hides the lane; dreamers pruned by the same test; unknown probe raises (exit 3, fields untouched) instead of returning []; coverage line derived from file keys; mixed int/str/sub-id ids sort by str; docstring's "wrapper pid dies" claim was backwards — measured, pid is the survivor; watch.py does not render current_task_ids today (no renderer change); 3 red-proofs + just test exit 0 (1008 passed, all guards PASS)
- **#172** · landed `33284c2` · 2026-07-28 15:40 · by grok (wt/172) — project basename edge-pinned in title bar (#hproj); tab title already had dreamwork/<project>; guard projtitle.mjs with three-route identity box
- **#420** · landed `2d7e242` · 2026-07-28 15:43 · by glm52 (wt/420) — read-only census of all 139 open entries (parser-supplied counts, blocking posture, the #419 blocked-on-human question cross-check, 10 stale blockers, symbol-overlap duplicates, ranked next-five); doc only, no review artifact (coordinator working document); 1 pre-existing pytest failure on the live #367 unfolded hand-off, outside this lane's ownership
- **#430** · landed `1dd973f` · 2026-07-28 18:03 · by the coordinator — one shared above-fold check for review artifacts, replacing the inline copy each lane wrote; criterion restated from `bottom < innerHeight` (unsatisfiable for a three-decision ask block 870px tall) to "the ask starts above the fold and its first decision does too"; three red-proved preconditions — viewport actually applied (both width AND height, because the default width 1280 equals the desktop request so a width-only check passes the very bug), page actually scrolls, element actually present; declared a tool not a guard in `lint.NOT_GUARDS` with the reason it gates nothing yet
- **#429** · landed `a54d162` · 2026-07-28 18:20 · by glm52 (wt/fold) — the #263 artifact's decision moved into the hero, following 421-question-options rather than inventing an idiom: `#ask.top` 594→188 desktop and 1006→266 mobile, first decision above the fold at both, verified by the shared checker; content preserved (2109→2116 visible words, every load-bearing sha identical, both `3 of 5` hits still inside the retained 16:24 correction); Q1/Q2/Q3 detail moved below the fold at 1298/1523/1790 so the decision is above and the detail below; the criterion's remaining half — making `#ask` a documented contract so the other 19 artifacts can be measured at all — stays open on #429
- **#425** · landed `cf452d2` `51729f4` · 2026-07-28 19:07 · by glm52 (wt/425) — the safety net for #368's symlink, not the symlink: `just deploy` resolves a symlinked `watch.py` via `git ls-tree` (rev-aware, cycle-capped) and asserts the snapshot defines top-level `main` and `GENERATION` **before** `pkill`, so a broken link is refused with the dashboard still up instead of after it goes dark; the recipe and `dev/deploy_state.py` share one resolver so bash and python cannot drift; procedure and per-step checks in `.dreamwork/docs/migrate-watch-symlink.md` with a doc-map row; 13 tests, all temp-dir; verified by the coordinator in a scratch clone with a real symlink (19-byte blob accepted by bare `ast.parse`, resolver emits the real 500,807-byte module, guard refuses both the blob and a truncated module); `watch.py` still mode 100755 and `deprecated/` absent, because the flip is #368's first increment and behind his #263 Q3

- **#441** · landed `a06f6ea` · 2026-07-29 00:43 · by grok (wt/floors) — one vacuity floor **per motion** instead of one shared: `MIN_FOLD_SPAN 20` (fold travels 193px) and `MIN_GROW_SPAN 6`, because the minimum real single-line note grow measures **exactly 20px**, so the old shared 20 sat *on* the signal with zero headroom, not the 3px the filing estimated; refused a fraction-of-observed-span floor as `#444`'s trap one level down (any observed span satisfies its own fraction); `transitions.md` updated in the same commit; states.mjs 22 PASS solo and 22/22 under a concurrent second run at load 57.23

- **#360** · landed `4d4e705` · 2026-07-29 00:43 · by glm52 (wt/sshauth) — ssh-rooted self-hosted hub auth, **design and docs only**; supersedes `hub-public-auth.md`'s identity recommendation, inherits its threat model and `/data.json` leak; four options compared; public/WAN serving remains forbidden pending his review

- **#447** · landed `d387ba3` · 2026-07-29 00:54 · by grok (wt/igc) — IGC vendored as `igc-method.md` + `igc-concepts.md` (his instruction), chosen as option **D** of four by an actual IGC: **A** fails because a verbatim nested skill keeps its own `name:` frontmatter and a loader sees a second installable skill, **B** fails on a machine without `use-igcs`, **C** loses `cf-concepts`; staleness detected by upstream path + sha256 per file; `SKILL.md` instructs IGC at four judgement sites, and a lane that must choose is handed `igc-method.md` in its brief; lint check declined with four reasons (a semantic "presents options" cannot be matched reliably, and it would restate the clause it reads — `#444`'s ground)

- **#449** · landed `614a668` · 2026-07-29 01:22 · by glm52 (wt/mistperf) — the SVG mist is **shelved behind `MIST_ON=false`, not deleted**, with CSS compositor blur carrying the dissolve; same-run proof at load 49: **12.1 → 27.6 frames (+128%)**, worst frame 262 → 129ms, CSS blur measured free; **two hypotheses refuted before anything was built** — animating `feTurbulence@baseFrequency` (freezing it, and all six per-frame writes, ≈ baseline) and filtered area (a 42% ghost clamp, 553×1557 → 553×900, changed nothing); the cause is a **threshold**, two filter rasterisations per frame contending with the shader, either alone ≈ baseline; guard `dev/capture/dissolve.mjs` red-proven on two injections; successor `#453` (moved/tiled texture)

- **#455** · landed `8a83df1` · 2026-07-29 01:23 · by grok (wt/context) — the audit **refuted the brief**: 17 of 27 artifact first screens already answer ≥3 of 4 orientation questions, so a blanket context paragraph was the wrong fix; the structural hole is *"what happens if he says nothing"* at **3 of 27**, now a build-time required slot mirroring `#436`'s ask contract (production line `enforce_if_silent_contract`), refused on **absence** never on length (his 01:13/01:17 ruling); `269-draft-durability.html` — the artifact he was reading when he said he felt lost, scoring 1/4 — rewritten as the worked example; all 15 buildable artifacts current, the 12 `src`-less remain the standing reason the walking guard is unregistered

- **#456** · landed `bd0f0d1` · 2026-07-29 01:29 · by grok (wt/dayage) — ` · ` between a question's date and its age, and the pad zero to `opacity:.5`; chose **opacity over the colour token** because opacity composites against the animated shader background and a dim token does not, which is why he asked for opacity; found a second defect on the way — `.qage`'s `margin-left` was carrying the gap, so with a separator added the spacing would have doubled; `watch-design.md`'s age contract updated in the same commit

- **#457** · landed `e38e9be` · 2026-07-29 01:31 · by the coordinator — every built artifact's head carried a stray `>` (he reported it twice and suspected the template; **the template was innocent**): all four meta-anchor patterns in `review_artifact.py` matched the tag *minus its own closing `>`*, so `sub()` left the old `>` behind and an insertion at `.end()` landed before it — one stray per meta, `#436` making one and `#455` three; red-proved by reverting only `ASK_META_RE`'s `\s*>`; new check asserts no head `<meta>` is missing its close, with a derived precondition that the scanned corpus carries those metas at all; **neither originating lane could see it because both checked the meta was *present*, never that the head stayed well-formed**

- **#450** · landed `9544f9e` · 2026-07-29 01:33 · by glm52 (deficiency, master) — the containment deficiency stated per his `#288` ruling (*"just plan for it and make sure the deficiency is noted"*): per-harness capability table, the trusted-nodes-only precondition where a reader would act on it, the seams that keep later isolation possible, and the warning copy; **no mechanism built**, the namespace wall stays prototyped and unwired; carries his reframe — whoever supplies the API key can supply the harness, so it is **not our seam** rather than our unbuilt work

- **#263g** · landed `693b2e9` · 2026-07-29 01:45 · by grok (wt/gateart) — dispatched to rebuild the second-gate artifact with context, problem, IGC and a rec; **mid-flight the question was answered** (*"ack good to go"*, 01:37), so it was steered to land the page as a **record of the decision taken** rather than an ask — context, problem and IGC kept, declared `#ask`/if-silent exemptions instead of a decoy question; merge conflicted on the *built* file (the coordinator had rebuilt all 15 for `#457`) and was resolved by taking the lane's **source** and regenerating the output, never hand-merging generated HTML

- **#269v** · landed `6a6ddff` · 2026-07-29 01:43 · by grok (draftcheck, master) — his correction *"drafts are durable btw"* proved right and the ledger wrong: typed text **survives a real `watch.py` kill and restart** (pid 1614177 → 1673912 on :39897) in the review dock, the `/questions` boxes **and** the composer, verified by reading the fields rather than storage; writes on **every input event, no debounce**; a draft clears only on durable success and a **failed send keeps it**; storage is `localStorage`, so the authorised remainder is the *upgrade*; two boxes keep no draft at all — `#askbox` and popout `#ptext` — filed as `#459`
- **#436b** · landed `75a3488` · 2026-07-29 02:03 · by grok (wt/srcless) — the source-less half of the corpus is now **explicitly exempt rather than silently skipped**: a side-file with one reason per artifact, `corpus_contract_coverage` asserting the equation **as sets** (`examined ∪ side_exempt == built`, `examined ∩ side_exempt == ∅`, `{src} − {built} == ∅`), and the walking guard `dev/capture/reviewask.mjs` registered (DEFAULT_GUARDS 54); IGC over classes chose exemption over reconstruction for all 12 — no template stamp, never built through `review_artifact`, hand-editing a built file forbidden; the coordinator corrected its first expression mid-flight from `|built| − |src|` to a real set difference, because the arithmetic is only right while `{src} − {built}` is empty and cannot say so; **coordinator-verified independently** — guard PASS at load 33.54, and dropping one exemption line yields `unaccounted={tasks-page.html}` with the equation naming the missing member, restored byte-identical and green
- **#421** · landed `40ca81f` `01c9bd7` · 2026-07-29 02:12 · by glm52 (wt/subdec) + the coordinator — his A+B+D answer: A and D became conventions in `file-formats.md`'s ask contract, **B** became a `lint.py` ERROR when a folded entry drops a declared sub-decision; recognition is **declared, not guessed** (one `**Sub-decisions:** \`Q1\`, \`Q2\`` line, read only from that line, never prose) and **the marker is its own content-resolved cutoff**, so the historical corpus is silent and no sha is pinned (the objection that made `#405` refuse a hand-pinned cutoff); `#275`'s Q3/Q5/Q6 **verified** still open, so the defect B exists for is real and current; the lane initially *predicted* post-merge visibility instead of merging master and testing it, and was told to test — coordinator then added the pending-declarations clause so `0 folded, 0 checked` cannot read as "no subject" when it means "adopted, waiting for a fold", red-proved on `_answered_split` (sabotage → clause vanishes, test FAIL; restore → PASS) with the fixture asserting the declaration really sits in the open half; C withdrawn on his 01:13/01:17 brittleness ruling — no length gate, ever; 1122 tests


- **#510** · landed `PENDING` · 2026-07-30 05:30 · by lane-510orc (design; model per dispatch record — a lane cannot know its own model) — design doc `.dreamwork/docs/plans/orchestrator-posture.md` + a doc-map row; an 'Orchestrator' option in Posture, IGC'd (I1–I5 × G1–G5, survivor: a fifth posture axis). Investigated the referent — designed against orchestration MODE (coordinator implements nothing, only dispatches+reviews; the coordinator-only-loop mode) over orchestrator IDENTITY (pushed back: provenance owned at dispatch, not a posture dial). Refuted fold-onto-delegation, the disabled `hierarchical` run-mode value (re-bundles #443), control-only, and sibling-file. Escalates Q1 (mode vs identity), Q2 (`hands-on`|`orchestrator` binary), Q3 (land the axis, don't enable `hierarchical`) with recs; draft questions.md entry included, NOT edited. Design only — no code authorised
- **#513** · landed `23de13cf` · 2026-07-30 06:30 · by coordinator (inline; SKILL.md is coordinator-owned) — tick restates the resolved posture axes each beat (the structural fix for posture drift, per the human steer recovered from blindly-consumed journal event ord=39); batched-delivery tick habit `pending → process → consume` with the never-blind rule; durable-state posture entry gains the delivery axis. Store-folded 06:30.
- **#498** · landed `f9144beb` · 2026-07-30 06:38 · by coordinator (fold) — gate found the 390px bdhover overflow (real, not #507 flake: baseline PASS at f74197b0); fix lane lane-burndown2-bdhover merged `4c192843`. Red-proof on merged tree: `box-sizing:border-box` removed → bdhover FAIL ("at 390px the inspector stays inside the panel"); cp-restore → PASS. burndown guard PASS. Collateral test repair `5dfe2738` (#487 window literal), red-proved via centre-clamp reintroduction.
- **#499** · landed `f9144beb` · 2026-07-30 06:38 · by coordinator (fold) — same merge as #498; independent red at `all.slice(-lim)` (3 #499 checks FAILED; restore → burndown PASS, 372 passed).
- **#511** · landed `PENDING` · 2026-07-30 06:30 · by lane-511live — the four "live ledger" migrate acceptance tests were RED on master because they read .dreamwork/tasks.md (now the one-line migration-notice shim); chose option (a) RE-POINT at tasks.md.deprecated (the frozen ledger is the post-cutover subject, the same convention test_ledger_store.py already follows; git_snapshots — exercised ONLY by the live history test — kept its real 678-commit history via a LIVE_LEDGER_HISTORY path on tasks.md, so retire's coverage loss was avoided); the history test's verify moved to verify_db directly with rich snaps because the CLI couples ledger-text and git-walk through one --ledger path, now split by the cutover (CLI verify over the deprecated path's single commit skips the groomed-body-match as vacuous); every added/changed check red-proved: the `## Open` precondition vs the shim, `len(snaps)>1` vs the deprecated file's 1-commit history (which makes rec[tasks] genuinely empty, the vacuity the guard prevents), and verify_db body-match vs a committed groomed-body tamper (a first green red-run caught an uncommitted-tamper scaffolding bug — sqlite rolls back DML on close without commit); pytest 1518 passed 0 fail; the one lint ERROR (tasks.md no Next id header) is pre-existing on HEAD and out of lane scope
- **#357** · landed `PENDING` · 2026-07-30 · by lane-357impl — `dev/ledger.py` emits the warning footer on every verb's success path via `emit_warnings()` to **stderr** (stdout stays pipeable): his five counts (unchecked messages · open tasks · unanswered questions — the new-question count collapsed in, one parse · unfolded answers) + incomplete-data (untyped · missing origin, store source only) + journal unconsumed receipts (`head_ordinal − coordinator cursor.scanned_through`); zeros absent, clean tree silent, **stateless** (throttle refuted by the IGC), WARN never ERROR (exit codes unchanged). Reuses `store_ids_by_state` / `lint.check_unfolded_answers` / `watch.parse_open_answers`+`parse_open_questions` / `open_journal`+`head_ordinal`+`cursor` — never a second copy. Red-first `test_ledger_warnings.py` (7 checks, each red-proved by injection + cp restore); Q6 ruled (full line, I1, 03:52 `rec`) recorded in `cli-warning-layer.md` this commit
- **#505** · landed `PENDING` · 2026-07-30 · by lane-505arch (design; model per dispatch record — a lane cannot know its own model) — design doc `.dreamwork/docs/plans/render-architecture.md` + a doc-map row; the wholesale-rerender smell. Inventory: 16 reset surfaces (1 = his bug, 11 re-applied by hand post-swap, 1 partially resumed, 3 never at risk); the chrome survives because it is ALREADY a keyed diff (`renderChrome`), which is the absence-of-idiom inside `#view` (one `innerHTML` swap/tick, `watch.py:6778`, + ~11 snapshot/restore pairs). IGC (I1–I5 × G1–G5) refutes his three directions as standalone fixes — ids alone don't survive the swap, content-check only skips unchanged ticks, React/preact impose a build step + second render authority against the single-file architecture (`watch-design.md:2737`); survivor is **keyed reconciliation of `#view` (morphdom-idiom) + content-hash skip (I5)** — generalise `renderChrome`'s keyed diff, keep survivor nodes by existing keys (`data-qid`/`data-aid`/`data-sha`/`data-review`/`data-keep`) so selection/caret/scroll/focus survive as a class. Subsumes #141/#503/#494 (their snapshot/restore pairs become dead code; `data-keep` retained as a reconciliation key). Every transitions.md family named with its fate (preserved/absorbed). Pushed back on "React" honestly. Escalates Q1 (vendored morphdom vs hand-rolled), Q2 (hold no-build G4), Q3 (scope), Q4 (guard the corpse rule) with recs; draft questions.md entry included, NOT edited. Design only — no code authorised
- **#508** · landed `5ad3dc0` · 2026-07-30 07:09 · by lane-508deploy (model per dispatch record — a lane cannot know its own model) — `just deploy` reported `deployed` against the OLD process. CONFIRMED MECHANISM (fixture, scratch_508 evidence): the deployed server ran `--dev` (=> autoreload); the recipe's `mv $snap.tmp $snap` overwrote the snapshot it watches, so autoreload `os.execv`'d the OLD pid IN PLACE (same pid, new generation, port flickered free via close-on-exec then rebound) — observed pid-unchanged/generation-changed; stop_deployed's 50ms poll can grade that flicker as 'free' and succeed while the old process rebinds; the new `nohup` server then dies on bind (EADDRINUSE, invisible); the curl-liveness readiness prints 'deployed' against the old pid (reproduced deterministically). STRUCTURAL root cause: success was LIVENESS (curl 200), not IDENTITY. FIX (true-by-construction): `verify_deployed` (dev/deploy_state.py) returns 0 only when `listening_pid==expect_pid` AND its argv runs the snap; `wait_port_free` confirms the port STAYS free (settle defeats the execv flicker) before the start; the recipe captures `$!` and calls `--verify-deployed --expect-pid`, echoing success only after it. `--dev` REMOVED from the deployed start line — autoreload was the re-exec enabler and serves no purpose on a frozen snapshot the recipe itself restarts (GENERATION still bumps on the fresh process). The PID is load-bearing over argv because autoreload's in-place re-exec keeps the old argv identical to the new snap. 9 new tests, all red-proved (named lines: success-return, pid-check branch ×2 via message+rc, _pid_alive, wait_port_free refuse, settle rebind-detection, --verify-deployed CLI branch, recipe --dev re-add). Identity check stands regardless of the --dev decision.
- **#514** · landed `df5e764` · 2026-07-30 06:35 · by lane-514wake — audit of wake semantics under `delivery: batched`; report at `.dreamwork/docs/findings/514-wake-semantics-audit.md`. Traced all 13 `log_event` call sites in `watch.py` + 1 raw append in `ud-dw-tasks-migrate`; the single append fn is `log_event` (`watch.py:13451`), and the #342 gate `emits_wake(kind, target)` (`watch.py:13403`) + `delivery_mode` (fresh per-event read, `watch.py:13398`) IS landed. **4 COMPLIANT** content routes (`/command` by kind, `/answer`, `/ask`, `/comment` — each gated); **2 VIOLATION**: `/decide` (`watch.py:14321`, journals a receipt but wakes unconditionally, missed by the doc's 4-route list) and `question-updated` (`track_question_updates` `watch.py:12902`, ungated + unjournaled digest signal on the batched question class); **7 UNCLEAR** (control-plane `/run-mode`+`/posture` always-instant but doc-silent; 3 journal failure-path wakes exception-only; migrate one-shot). `dev/journal_consume.py` drain confirmed silent (no self-wake); a do-now wakes at POST naming the event, not the batch. No contradiction of the ruling itself — the doc's route enumeration is stale (4 of 9 `WRITE_ROUTE_HANDLERS`) and silent on control/system/error wakes. Proposed follow-ups #515–#519; **audit only, owns no production file**
- **#512** · landed `a0bf2fc` · 2026-07-30 07:10 · by lane-512show — `dev/journal_consume.py show <receipt-id>` (third subcommand, #512): a read-only full-payload verb so batched delivery is never lossy. Composes the single public `Journal.get_receipt(receipt_id)` (by-id, cursor-INDEPENDENT read) — no `user_events/` change, no new journal query; prints a key:value header (receipt_id/state/revision/client_action_id/request_digest) then the payload decoded UTF-8 verbatim (no 80-char cap, no #126 newline-collapse — the deliberate exception, this output is for an agent to READ a multi-line instruction). Never advances the cursor; works for ALREADY-CONSUMED receipts (consume moves only the cursor, the row persists — the recovery case for a blindly-consumed event). Unknown id / absent journal → one-line stderr + EX_USAGE(64), no db created (read-only #501 discipline). 5 red-first tests seeded via the production `receive()` path with runtime-derived preconditions (payload len > _PREVIEW_LIMIT asserted, cursor-moved asserted, multi-newline asserted). Red-proof per test via scratch-snapshot/cp restore. Pending/consume/_PREVIEW_LIMIT/_format_event untouched
- **#517/#518** · landed `1f105aec` · 2026-07-30 · by lane-517doc — `.dreamwork/docs/plans/delivery-modes.md` caught up with the landed #342b code per the #514 audit. **#518:** the stale "What changes in watch.py's command handlers" section (named 4 of 9 `WRITE_ROUTE_HANDLERS`, framed the gates as a future increment) is rewritten design-as-built with a 9-row table verifying each handler against watch.py while writing — the four gated content routes (`/command`,`/answer`,`/ask`,`/comment`), `/decide` (NOT gated — its `emits_wake` gate is in flight under #515, named as such, never asserted as present), `/tint`+`/deploy` (non-waking by design, with the reason), and the control routes; the "authorises no code / next increment" framing is reframed to landed-as-#342b. **#517:** new "Wake channels outside the route table" section states a policy for every wake the ruling was silent on — control-plane wakes (`/run-mode`,`/posture` triple + delivery axis) as an explicit always-instant carve-out (a delivery toggle that doesn't wake until the next tick defeats the toggle); journal failure-path wakes (`_journal_receive`/`_journal_record_health`/`_journal_reject`, exception-only) as always-loud error diagnostics; the one-shot `ud-dw-tasks-migrate` wake; and `question-updated` (policy pending under #516, in flight). Every code claim cites a watch.py line verified while writing (line numbers drift; none copied from the audit — e.g. the audit's `log_event:13451`/`emits_wake:13403` are now `13464`/`13418`). Doc-map row for `delivery-modes` stays accurate (directory enumeration; the plan file remains). Doc lane — no code touched; no server started, no port bound
- **#515** · landed `5fdc1c6` · 2026-07-30 · by lane-515decide (model per dispatch record — a lane cannot know its own model) — `#514` F1 fix: `_handle_decide` journaled its receipt via the E3 cutover but emitted its `review-decision` wake line with NO `emits_wake` guard, so under `delivery: batched` a `/decide` woke every time — silently undoing batched mode for review decisions (the same content family as `/answer` and `/comment`). The wake line is now gated behind `if emits_wake("/decide", target):` mirroring the `/comment` idiom (`watch.py:~14337`); the receipt commit and store write are UNCHANGED — withholding the wake IS batching, the cursor carries the decision to the tick. Did not touch `track_question_updates` (~12917, lane-509sig) or `emits_wake`/`PREEMPT_KINDS`. Two red-first tests in `TestDeliveryWakeRouting`, each red-proved by injection + cp restore (never `git checkout`), each naming its production line: **(1)** `test_decide_withholds_wake_in_batched` — RED by removing the `if emits_wake("/decide", target):` guard (the line fires in batched → the withhold `assertFalse` fails); production line injected: that guard in `_handle_decide`; **(2)** `test_decide_wakes_in_instant_and_default` — RED by removing the `log_event(...)` `review-decision` emission (zero lines in instant → the `len==1` fails); production line injected: that emission in `_handle_decide`. Preconditions derived at runtime (the hollow-check rule): posture file carries the delivery axis (`read_posture_file[\"delivery\"]==\"batched\"`/`\"instant\"`/absent), target is store-mode (`source_of_truth==\"store\"`, else `/decide` refuses and a withheld-line check is vacuous), route returned 202, and the decision landed in the store (`_review_decisions`). Born-red run against unfixed production: batched test FAILED at the wake-line assertion after all preconditions passed (not hollow). pytest `test_watch.py` 374 passed (372 + 2). The two pre-existing lint ERRORs (`tasks.md` no `Next id` header; the `#398` brief-obligation on this brief) are dispatch-level, out of lane scope, and not caused by this change
