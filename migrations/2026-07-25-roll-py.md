# 2026-07-25 — roll.py

## What changed

`roll.py` ships with the skill: advisory dice for backlog-vs-maintenance
and which maintenance item (weighted, CLI-configurable, stdlib only,
never binding). Selection step 3.2 mentions it.

## How to apply

Nothing required. Optionally persist custom weights as a Routines line in
DREAMWORK.md, e.g. `roll: --backlog 80 --weight docs=4`.
