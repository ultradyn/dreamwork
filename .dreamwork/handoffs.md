# Hand-offs — work landed outside the ledger, waiting to be folded

A session that lands work it does **not** own `.dreamwork/tasks.md` for —
every session but the coordinator — appends ONE line under `## Pending`.
The coordinator reads it on every tick, folds each landing into the ledger,
and appends one `→ folded` line under `## Folded`. **Nothing ever moves**:
both sections grow only by append, so two sessions landing work at once
cannot lose each other's line — the property the dreamer inbox has and a
rewrite would not (#381).

**Section order (#406):** `## Folded` first, then `## Pending`, so an EOF
append lands under Pending and the instruction is true without a rewrite.

One line per landing, mirroring the inbox's one-line-per-report shape.
Required: the task id (plain `#N`, sub-id `#Na`, or combined `#N/#M`), the
**sha** (what landed), who is claiming it, and a one-line `what`. The
`→ folded` line is how the writer marks one consumed, and it is itself an
append — never a deletion — so a folded hand-off is not flagged twice.

## Folded
- **#420** → folded (2026-07-28 15:55): merged `61354fb` into `## Recently landed`; its ten stale blockers re-verified with `parse_ledger` and nine cleared in `30458af` (`#172` was the tenth and closed on its own). Its `#419`-reverse-direction finding is folded into `#419`'s entry as that check's red fixtures.
- **#172** → folded (2026-07-28 15:50): merged `775022d` into `## Recently landed`; his edge-pin rule verified by three route rectangles rather than a screenshot (identical box on `/` and `/questions`, trailGap 0 on a long `/review` where the bar widens), guard `projtitle` registered and red-proved on six named assertions. Refuted my brief's claim that the tab title lacked the project name — `#153` had already put it there, which is why he believed the work was done.
- **#402a** → folded (2026-07-28 15:45): merged `f1f269b` into `#402`'s entry; `gate402a.py` PASSED against the merged tree with the pre-merge baseline, and the fix verified on REAL data — the first post-merge run reported `current_task_ids [] -> ['172', '420']`, found both live lanes and pruned the dead `402a` one. Its refutation of my gate's stale-pid premise is recorded in the entry; `#402` stays open for the `file-formats`/`lint` vocabulary (`#402b`, which collided within a minute) and for `awaiting_human`, still hand-written.
- **#367** → folded (2026-07-28 15:45): merged `98670ae`; **`#367` deliberately STAYS OPEN** — this landed the option previews he asked to see, not the feature, and he has since ruled (C with a collapsible chevron index, collapsed by default). The hand-off line says *landed* because the grammar has no word for *increment 2 of n landed*, which is `#415`; that gap is what made `test_lint.py::TestHandoffs` red on master until this line existed.
- **#411** → folded (2026-07-28 14:28): merged `1f01a95` into `## Recently landed`; gate re-run on the merged tree against an explicit pre-merge baseline — 2 recovered, 44 dated unchanged, None 5→3, bait probe still inert
- **#331** → folded (2026-07-28 13:18): merged `cb476a7` into `## Recently landed`; gate re-run on the merged tree with an explicit pre-merge baseline — 19 ids recovered, landed 152→171, span core 4 copies → 1, all three heads pinned including `status_sync`
- **#399** → folded (2026-07-28 12:24): merged `0595b13` into `## Recently landed`; burndown green on the merged tree, and the 3 remaining `just test` failures verified pre-existing on master; residual space-separated multi-id gap filed as #412
- **#398** → folded (2026-07-28 09:31): folded into `## Recently landed` citing `9f2012a`; verification owed
- **#397** → folded (2026-07-28 09:52): folded into `## Recently landed` citing `1b508b0`; recommendation (do-not-extract) accepted, no ruling requested, worktree alternative filed as #405
- **#401** → folded (2026-07-28 09:58): audit half folded into the `#401` entry citing `f2c950e`; fix half stays open, still needs `watch.py`
- **#392a** → folded (2026-07-28 10:00): verified and closed against `#392`'s entry citing `159917b` — independent red taken in a worktree, deployed page checked (38/38 day-precision, zero two-figure). Misfiled Pending-shaped line was the `#406` fixture; relocated under Pending after the check went red-loud.

- **#263g** → folded (2026-07-29 01:52): the artifact increment is consumed; **`#263` itself stays open by design** — its second gate opened at 01:37 and lanes **E**, **G**, **H** are now authorised, with lane E (increments 20-22) in flight. The gate artifact is recorded in `#263`'s entry as the decision-taken record it became, not as a closing commit.
- **#269v** → folded (2026-07-29 01:52): the verification is consumed into `#269`'s entry as the 01:43 correction — acute draft durability is **(b) partly shipped**, restart-verified, `localStorage`, no debounce. **`#269` stays open by design**: the authorised remainder is the IndexedDB upgrade, cross-tab `R1` and 30-day GC, with `#459` (`#askbox`, popout `#ptext`) sequenced ahead of it.
## Pending

- **#398** · landed `9f2012a` · 2026-07-28 09:26 · by ccc @grok — lint check: post-obligation briefs must mention handoffs.md

- **#397** · landed `1b508b0` · 2026-07-28 09:42 · by ccc @glm52 — design plan: client extraction is mechanically cheap (interpolation count 1) but leans do-not-extract; does not unblock #331/#352 (Python), multiplies the registry damage class, and four named breaks (deploy/serving/autoreload/audit) must ship together if ruled in
- **#401** · landed `f2c950e` · 2026-07-28 09:47 · by ccc @grok — research: executed id-grammar matrix (14 patterns × 17 forms); #401 silent drop reproduces; 7 silent rejects ranked
- **#392a** · landed `159917b` · 2026-07-28 09:43 · by ccc @glm52 — date-only question ages show ONE figure (paintDayAge, data-day flag); today reads "today"; timed commits keep two figures
- **#401** · landed `e53d70c` · 2026-07-28 10:09 · by ccc @grok — hand-off id grammar accepts plain/sub-id/combined; malformed outside section; Folded-then-Pending so EOF append works (#401+#406)

- **#399** · landed `ddc6614` · 2026-07-28 12:08 · by dreamer-399b @grok — _landed_ids counts the historical inline form again (col0 + ref-field exclusion keeps #367 closed); burndown + forgotten_folds both green, 3 opposite-direction reds proven; just-test exit 1 is 3 pre-existing/unrelated guards (qacard=#392, docktarget/noteprop=motion flake under load) proven identical on master

- **#331** · landed `ddc4e3e` · 2026-07-28 13:13 · by dreamer-331 — one IDS_ONLY_SPAN core in watch.py, consumed by lint.LEDGER_ID and status_sync.LEDGER_HEAD; landed 152→171, open 135, all 19 joined-span ids recovered; red-proved both directions; the three guard reds (qacard/docktarget/noteprop) reproduce at parent 97becd9 and were fixed on master by 7007d5b+e15b0c0 (guard-contract fixes, not this parser change)
- **#411** · landed `25a3fe4` · 2026-07-28 14:08 · by grok (wt/411) — (also `54c68e8`) answered_at anchor \A→^+re.M+.search so the 2 second-line markers recover (5→3 None); 44 dated byte-identical; lint WARN with derived count; both red-proofs discriminating

- **#367** · landed `a36c674` · 2026-07-28 15:02 · by grok (wt/367p) — option previews A/B/C at true below-cliff geometry; chrome measured at load (A 167.9 / B 127.2 / C 31.8 px at 780); 16px margin holds at 640; row-count red-proof 3→1 rows
- **#402a** · landed `78be285` · 2026-07-28 05:32 · by grok (wt/402a) — status_sync liveness is pid-primary (kill -0) with brief-path fallback; `^ccc @` -> broad `ccc`+brief-substring so a flag between binary and alias no longer hides the lane; dreamers pruned by the same test; unknown probe raises (exit 3, fields untouched) instead of returning []; coverage line derived from file keys; mixed int/str/sub-id ids sort by str; docstring's "wrapper pid dies" claim was backwards — measured, pid is the survivor; watch.py does not render current_task_ids today (no renderer change); 3 red-proofs + just test exit 0 (1008 passed, all guards PASS)
- **#172** · landed `33284c2` · 2026-07-28 15:40 · by grok (wt/172) — project basename edge-pinned in title bar (#hproj); tab title already had dreamwork/<project>; guard projtitle.mjs with three-route identity box
- **#420** · landed `2d7e242` · 2026-07-28 15:43 · by glm52 (wt/420) — read-only census of all 139 open entries (parser-supplied counts, blocking posture, the #419 blocked-on-human question cross-check, 10 stale blockers, symbol-overlap duplicates, ranked next-five); doc only, no review artifact (coordinator working document); 1 pre-existing pytest failure on the live #367 unfolded hand-off, outside this lane's ownership
- **#430** · landed `1dd973f` · 2026-07-28 18:03 · by the coordinator — one shared above-fold check for review artifacts, replacing the inline copy each lane wrote; criterion restated from `bottom < innerHeight` (unsatisfiable for a three-decision ask block 870px tall) to "the ask starts above the fold and its first decision does too"; three red-proved preconditions — viewport actually applied (both width AND height, because the default width 1280 equals the desktop request so a width-only check passes the very bug), page actually scrolls, element actually present; declared a tool not a guard in `lint.NOT_GUARDS` with the reason it gates nothing yet
- **#429** · landed `a54d162` · 2026-07-28 18:20 · by glm52 (wt/fold) — the #263 artifact's decision moved into the hero, following 421-question-options rather than inventing an idiom: `#ask.top` 594→188 desktop and 1006→266 mobile, first decision above the fold at both, verified by the shared checker; content preserved (2109→2116 visible words, every load-bearing sha identical, both `3 of 5` hits still inside the retained 16:24 correction); Q1/Q2/Q3 detail moved below the fold at 1298/1523/1790 so the decision is above and the detail below; the criterion's remaining half — making `#ask` a documented contract so the other 19 artifacts can be measured at all — stays open on #429
- **#425** · landed `cf452d2` `51729f4` · 2026-07-28 19:07 · by glm52 (wt/425) — the safety net for #368's symlink, not the symlink: `just deploy` resolves a symlinked `watch.py` via `git ls-tree` (rev-aware, cycle-capped) and asserts the snapshot defines top-level `main` and `GENERATION` **before** `pkill`, so a broken link is refused with the dashboard still up instead of after it goes dark; the recipe and `dev/deploy_state.py` share one resolver so bash and python cannot drift; procedure and per-step checks in `.dreamwork/docs/migrate-watch-symlink.md` with a doc-map row; 13 tests, all temp-dir; verified by the coordinator in a scratch clone with a real symlink (19-byte blob accepted by bare `ast.parse`, resolver emits the real 500,807-byte module, guard refuses both the blob and a truncated module); `watch.py` still mode 100755 and `deprecated/` absent, because the flip is #368's first increment and behind his #263 Q3

- **#441** · landed `a06f6ea` · 2026-07-29 00:43 · by grok (wt/floors) — one vacuity floor **per motion** instead of one shared: `MIN_FOLD_SPAN 20` (fold travels 193px) and `MIN_GROW_SPAN 6`, because the minimum real single-line note grow measures **exactly 20px**, so the old shared 20 sat *on* the signal with zero headroom, not the 3px the filing estimated; refused a fraction-of-observed-span floor as `#444`'s trap one level down (any observed span satisfies its own fraction); `transitions.md` updated in the same commit; states.mjs 22 PASS solo and 22/22 under a concurrent second run at load 57.23

- **#360** · landed `4d4e705` · 2026-07-29 00:43 · by glm52 (wt/sshauth) — ssh-rooted self-hosted hub auth, **design and docs only**; supersedes `hub-public-auth.md`'s identity recommendation, inherits its threat model and `/data.json` leak; four options compared; public/WAN serving remains forbidden pending his review

- **#447** · landed `d387ba3` · 2026-07-29 00:54 · by grok (wt/igc) — IGC vendored as `igc-method.md` + `igc-concepts.md` (his instruction), chosen as option **D** of four by an actual IGC: **A** fails because a verbatim nested skill keeps its own `name:` frontmatter and a loader sees a second installable skill, **B** fails on a machine without `use-igcs`, **C** loses `cf-concepts`; staleness detected by upstream path + sha256 per file; `SKILL.md` instructs IGC at four judgement sites, and a lane that must choose is handed `igc-method.md` in its brief; lint check declined with four reasons (a semantic "presents options" cannot be matched reliably, and it would restate the clause it reads — `#444`'s ground)

- **#449** · landed `614a668` · 2026-07-29 01:22 · by glm52 (wt/mistperf) — the SVG mist is **shelved behind `MIST_ON=false`, not deleted**, with CSS compositor blur carrying the dissolve; same-run proof at load 49: **12.1 → 27.6 frames (+128%)**, worst frame 262 → 129ms, CSS blur measured free; **two hypotheses refuted before anything was built** — animating `feTurbulence@baseFrequency` (freezing it, and all six per-frame writes, ≈ baseline) and filtered area (a 42% ghost clamp, 553×1557 → 553×900, changed nothing); the cause is a **threshold**, two filter rasterisations per frame contending with the shader, either alone ≈ baseline; guard `dev/capture/dissolve.mjs` red-proven on two injections; successor `#453` (moved/tiled texture)

- **#455** · landed `8a83df1` · 2026-07-29 01:23 · by grok (wt/context) — the audit **refuted the brief**: 17 of 27 artifact first screens already answer ≥3 of 4 orientation questions, so a blanket context paragraph was the wrong fix; the structural hole is *"what happens if he says nothing"* at **3 of 27**, now a build-time required slot mirroring `#436`'s ask contract (production line `enforce_if_silent_contract`), refused on **absence** never on length (his 01:13/01:17 ruling); `269-draft-durability.html` — the artifact he was reading when he said he felt lost, scoring 1/4 — rewritten as the worked example; all 15 buildable artifacts current, the 12 `src`-less remain the standing reason the walking guard is unregistered

- **#456** · landed `bd0f0d1` · 2026-07-29 01:29 · by grok (wt/dayage) — ` · ` between a question's date and its age, and the pad zero to `opacity:.5`; chose **opacity over the colour token** because opacity composites against the animated shader background and a dim token does not, which is why he asked for opacity; found a second defect on the way — `.qage`'s `margin-left` was carrying the gap, so with a separator added the spacing would have doubled; `watch-design.md`'s age contract updated in the same commit

- **#457** · landed `e38e9be` · 2026-07-29 01:31 · by the coordinator — every built artifact's head carried a stray `>` (he reported it twice and suspected the template; **the template was innocent**): all four meta-anchor patterns in `review_artifact.py` matched the tag *minus its own closing `>`*, so `sub()` left the old `>` behind and an insertion at `.end()` landed before it — one stray per meta, `#436` making one and `#455` three; red-proved by reverting only `ASK_META_RE`'s `\s*>`; new check asserts no head `<meta>` is missing its close, with a derived precondition that the scanned corpus carries those metas at all; **neither originating lane could see it because both checked the meta was *present*, never that the head stayed well-formed**

- **#450** · landed `9544f9e` · 2026-07-29 01:33 · by glm52 (deficiency, master) — the containment deficiency stated per his `#288` ruling (*"just plan for it and make sure the deficiency is noted"*): per-harness capability table, the trusted-nodes-only precondition where a reader would act on it, the seams that keep later isolation possible, and the warning copy; **no mechanism built**, the namespace wall stays prototyped and unwired; carries his reframe — whoever supplies the API key can supply the harness, so it is **not our seam** rather than our unbuilt work

- **#263g** · landed `693b2e9` · 2026-07-29 01:45 · by grok (wt/gateart) — dispatched to rebuild the second-gate artifact with context, problem, IGC and a rec; **mid-flight the question was answered** (*"ack good to go"*, 01:37), so it was steered to land the page as a **record of the decision taken** rather than an ask — context, problem and IGC kept, declared `#ask`/if-silent exemptions instead of a decoy question; merge conflicted on the *built* file (the coordinator had rebuilt all 15 for `#457`) and was resolved by taking the lane's **source** and regenerating the output, never hand-merging generated HTML

- **#269v** · landed `6a6ddff` · 2026-07-29 01:43 · by grok (draftcheck, master) — his correction *"drafts are durable btw"* proved right and the ledger wrong: typed text **survives a real `watch.py` kill and restart** (pid 1614177 → 1673912 on :39897) in the review dock, the `/questions` boxes **and** the composer, verified by reading the fields rather than storage; writes on **every input event, no debounce**; a draft clears only on durable success and a **failed send keeps it**; storage is `localStorage`, so the authorised remainder is the *upgrade*; two boxes keep no draft at all — `#askbox` and popout `#ptext` — filed as `#459`
- **#436b** · landed `75a3488` · 2026-07-29 02:03 · by grok (wt/srcless) — the source-less half of the corpus is now **explicitly exempt rather than silently skipped**: a side-file with one reason per artifact, `corpus_contract_coverage` asserting the equation **as sets** (`examined ∪ side_exempt == built`, `examined ∩ side_exempt == ∅`, `{src} − {built} == ∅`), and the walking guard `dev/capture/reviewask.mjs` registered (DEFAULT_GUARDS 54); IGC over classes chose exemption over reconstruction for all 12 — no template stamp, never built through `review_artifact`, hand-editing a built file forbidden; the coordinator corrected its first expression mid-flight from `|built| − |src|` to a real set difference, because the arithmetic is only right while `{src} − {built}` is empty and cannot say so; **coordinator-verified independently** — guard PASS at load 33.54, and dropping one exemption line yields `unaccounted={tasks-page.html}` with the equation naming the missing member, restored byte-identical and green
- **#421** · landed `40ca81f` `01c9bd7` · 2026-07-29 02:12 · by glm52 (wt/subdec) + the coordinator — his A+B+D answer: A and D became conventions in `file-formats.md`'s ask contract, **B** became a `lint.py` ERROR when a folded entry drops a declared sub-decision; recognition is **declared, not guessed** (one `**Sub-decisions:** \`Q1\`, \`Q2\`` line, read only from that line, never prose) and **the marker is its own content-resolved cutoff**, so the historical corpus is silent and no sha is pinned (the objection that made `#405` refuse a hand-pinned cutoff); `#275`'s Q3/Q5/Q6 **verified** still open, so the defect B exists for is real and current; the lane initially *predicted* post-merge visibility instead of merging master and testing it, and was told to test — coordinator then added the pending-declarations clause so `0 folded, 0 checked` cannot read as "no subject" when it means "adopted, waiting for a fold", red-proved on `_answered_split` (sabotage → clause vanishes, test FAIL; restore → PASS) with the fixture asserting the declaration really sits in the open half; C withdrawn on his 01:13/01:17 brittleness ruling — no length gate, ever; 1122 tests
