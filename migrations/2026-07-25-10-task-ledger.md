# 2026-07-25 — durable task ledger

## What changed

`.dreamwork/tasks.md`: a committed ledger of open tasks and their
**permanent ids**. Task backends are session-scoped — a restart empties
the list and takes the ids with it, which breaks every reference in
docs, questions, dreams, and commit messages. The ledger is now the
durable half: ids live there (with the next one to hand out), tasks
carry theirs in `metadata.id` and in their subject, and initialization
restores the backend from it before judging the queue.

Found by dogfooding: restarting the skill's own session dropped a queue
of eight tasks whose ids were referenced across four documents.

## How to apply

Create `.dreamwork/tasks.md` from the current open tasks — one line each:
id, title, priority/type/size, owner or blocked-on, pointer to any plan.
Ids continue from the highest ever used on that target (never reuse);
record the next id in the file. Add `metadata.id` to existing tasks and
put the id in their subjects. From then on the ledger is rewritten by the
increment that changes the queue (reflection checklist step 5), not on a
separate beat.
