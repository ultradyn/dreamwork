# Coordinator handoff — 2026-07-31 ~15:40 AEST

**From:** this grok coordinator session (ud-dreamwork dogfood loop)  
**To:** the next session that loads `ud-dreamwork`  
**Stop reason:** Max asked for handoff soon; explicit instruction **do not start any new subagents**. Maintenance/upkeep only after API recovery. This file is also the **persist-before-compact** record.

---

## 0. One-screen resume

| Fact | Value |
|------|--------|
| **Repo** | `/home/xertrov/.llm-general/skills/ud-dreamwork` |
| **HEAD** | `1f2da3d1` — `handoffs(#583): fold dual-column question view + register qdual guard` |
| **Tree** | **Clean** |
| **Origin** | `master...origin/master [ahead 825]` — local only; **do not push** unless Max asks |
| **Deploy** | `:35110` **current**, rev `1f2da3d1`, pid **1632776**, snap `~/.cache/dreamwork/deployed/ud-dreamwork-watch.py` |
| **Posture** | `pace: hot` · `asking: near-auto` · `delegation: 3` · `delivery: batched` · `orchestration: orchestrator` |
| **Run mode** | `hot` |
| **Store** | open **119** · landed **355** (as of handoff write) |
| **Journal** | `pending` **empty** |
| **Coord-inbox** | No new messages after #397 DONE + FLAGs (see §inbox) |
| **In-flight lanes** | **None.** Do not trust `status.json` agents list — it was stale (565/569/571 still listed as fleet); refresh on first tick. |
| **Subagents** | **Do not spawn** until Max re-authorises (his last instruction). |

**Immediate next act for the new session:** load `ud-dreamwork` + worktrees plugin if dispatching; re-arm heartbeat / coord-inbox / watch-events monitors; `just deploy` only if HEAD ≠ deployed; drain `journal_consume pending` if any; treat open questions on Max’s desk as higher priority than self-picked backlog; then either wait for Max or (if authorised) dispatch from §next.

---

## 1. Authoritative state

### Git
- Branch: `master`
- HEAD: `1f2da3d1`
- Working tree: clean (no staged, no unstaged, no untracked owed work at handoff)
- Ahead of origin by **825** commits — normal for this dogfood repo; not a merge crisis

### Deploy (load-bearing dashboard)
- Port **35110** only (standing-authorised `just deploy`)
- Identity-verified: recipe success is pid+argv, not curl-200
- At handoff: `data.json` → `deployed.state=current`, `rev=1f2da3d1`
- Client extraction is live: `client/{style.css,app_body.html,components.js,views.js,favicon.js,router.js,command.js,shader.js}` read by `_read_client()`; watch.py ~5.4k lines (was ~15k pre-#397)

### Posture / delivery (restate every tick — #513)
```
pace: hot
asking: near-auto
delegation: 3
delivery: batched
orchestration: orchestrator
```
Under **orchestrator**: coordinator implements only coordinator-owned files (SKILL.md, handoffs fold lines, ledger folds, briefs, status). UI/feature work → dispatch or file. This session’s last acts were gate/fold/register/deploy/visual — allowed coordinator gate work, not new feature invention.

### Monitors (re-armed late this session after max_runtime kill)
- Heartbeat 4.75m — task id was `019fb6a7-2382-7c70-bc45-babe062e4645` (session-local; re-arm if dead)
- Coord-inbox: `tail -n 0 -F /home/xertrov/.cache/agent-comms/ud-dreamwork/coord-inbox.md`
- Watch-events: `tail -n 0 -F .dreamwork/watch-events.log`

### Worktrees
- No live lane worktrees under the usual paths at handoff check
- Lane commits live in independent clone worktrees; merge via `git fetch <wt> master:lane-<name>` then cherry-pick, **not** assume shas exist in main until fetched

---

## 2. What landed this coordinator cycle (oldest → newest)

Session continued after compaction with standing dreamwork loop. User then: *check git each heartbeat until UI watch.py refactor lands; other agent will message inbox.* Refactor (**#397**) was already on master + inbox DONE — normal operation resumed.

### Coordinator / gate landings
| Task | What | Shas / evidence |
|------|------|-----------------|
| **#397** (prior lane, consumed this session) | Client extraction out of watch.py into `client/` | Merge `69b9cd06`; inbox DONE; watch.py ~15,666→~5k; served page byte-identical proof in inbox |
| **Hand-off fold backlog answer** | Max ask: 23 hand-offs “to fold” — all listed ids already `landed`; backlog was missing `→ folded` lines, not unmerged work. #576 catches this going forward | `ab39daa7` |
| **#571 Q1–Q4 → questions** | Surfaced settings design forks | `5995d673`; title fix `#584 (from #571)` `444cc7e2` |
| **#575 finding + #585 fix** | #533 truncation guard is **post-commit-blind** (HEAD==tree after commit). Fix: HEAD-vs-HEAD~1 retroactive ERROR | finding `b36ce003`; lint `6e3f7d4f` red-proved (3090-line truncation) |
| **#577** reply composer | `/chat-reply` + composer on `/chat/<id>`; reconcileGuard keep-rule for in-view confirmation | cherry-picks `47ee8731`+`93ff9a03`+`1f84a5d7`+`e20ef44e`; fold `b5817351`+`03d0b960`; independent red on `apply_chat_turn` role passthrough |
| **#583** dual-column question | `/question?qid=` body left, taller sticky compose right, visible-midpoint `--qcol-top` on `body`, third width exception 1040px, min-width 1000px stacks | cherry-picks `3fb4544c`+`ca02bc87`+`1e8d0353`; fold+`qdual` register `1f2da3d1`; independent red on STYLE assembly (`watch.py` STYLE ← empty) |
| **#583 visual** | Desktop geometry **PASS** on live :35110 — side-by-side body/compose, sticky compose, wrap 1040px, `body.question` | Screenshots `/tmp/583-desktop.png` (ephemeral); DOM measure: body left ~120 w553, compose left ~705 w455 sticky |

### Store states (spot-check at handoff)
| id | state | note |
|----|-------|------|
| 577 | landed | reply composer |
| 583 | landed | dual-column |
| 585 | landed | retroactive truncation |
| 571 | landed | design only |
| 584 | open | implement #571 |
| 578–582 | open | undispatched backlog from receipts |
| 572 | open | GitHub etiquette (on Max’s desk as questions) |
| 574, 568, 566 | open | P3 / polish |
| 124 | open | re-scoped; next is demand-driven seam not big-bang split |
| 465 | open | containment consent |
| 500 | open | matt-pocock bridge remainder |
| 576 | open in store | check may have landed earlier; verify before re-dispatch |

---

## 3. Verification honesty

- **#577 independent red:** sabotage `apply_chat_turn` role passthrough → exactly reply-human-turn test FAIL → cp-restore byte-identical → 7/7 green.
- **#583 independent red:** empty STYLE assembly → dual-column CSS focus-scope test FAIL → cp-restore → 4/4 green.
- **#585 red-proof:** committed truncation with HEAD==tree → pre-commit silent, retroactive ERROR.
- **#583 visual:** PASS desktop dual-column. Mobile stacks (body.question wrap shrinks); not a full mobile visual campaign this session.
- **Pre-existing red (not this work):** `test_all_open_questions_still_render_after_the_age_change` fails on clean master (live-data age format).
- **No full `just test` / full guards** in the final handoff window. Last full suite numbers live in older fold lines (#397 claimed 1764 pytest / 82 guards at merge). Do not invent a green suite for this handoff.
- **`status.json` was stale** at handoff (still claimed fleet 565/569/571 in flight). Coordinator should rewrite on first tick; do not dispatch “finish those lanes.”

---

## 4. Inbox / receipts

### Coord-inbox (path)
`/home/xertrov/.cache/agent-comms/ud-dreamwork/coord-inbox.md`

### Last meaningful message (acted on)
`[lane-clientextract] DONE 69b9cd06 — MERGED to master… You can continue.`  
Plus #397 review FLAGs — see **#397 shared deploy siblings** below.

### Journal
- Delivery: **batched** → tick habit is `pending → process → consume --through <head-ord>`
- At handoff: **pending empty**
- Never consume blind; never bare `consume` without through-ordinal after #531

### Ledger warning footer (live)
`1 unchecked messages · 119 open tasks · 5 unanswered questions · 1 unfolded answers · 239 untyped`  
Unblock: find the one unfolded answer + any unchecked human message; untyped is hygiene not a P0.

---

## 5. Open questions (his desk) — prefer over inventing work

From live `questions_open` at handoff:

1. **#572** — GitHub PR/comment etiquette design forks (Q1–Q4+ in questions.md)
2. Hand-off fold backlog — **answered** this session (not unmerged); confirm UI still shows as answered
3. **#584 (from #571)** — settings Q1–Q4 rulings (table-in-store · TEXT+JSON · settings.py · sibling-of-posture)
4. **#465** — lane-containment guard in front of this repo’s commits? (consent)
5. Cross-machine answers — still open historical

Also standing: **grok-4.5 credential 401** — fall back glm-5.2; glm-5.2 lanes must never `read_file` images.

---

## 6. Next work (ordered)

### Do not do until Max says so
- Spawn subagents / open worktrees
- Push origin
- Big-bang watch.py splits (#124 remainder is demand-driven seam only)
- Implement settings (#584) without Q1–Q4 rulings

### When dispatch is re-authorised (suggested order)
1. Chat / Max messages / journal pending first (standing steer)
2. **#578** `dw request-feature` CLI  
3. **#579** burndown period-label hover hint  
4. **#580** posture free-text subagent policy  
5. **#581** dashboard quick-links row  
6. **#582** /answers input styling  
7. P3: **#574** posture HR+soft-fade · **#568** bdhover `.depart` binding · **#566** /tasks badges store-backed  
8. **#397 shared-deploy-siblings** — per-snapshot sibling dir; needs Migration + deploy rehearsal (FLAG from #397 lane; pre-#480 widened)

### Coordinator-owned hygiene (no subagent)
- Refresh `status.json` (this handoff’s first durable act if still stale)
- Fold any new handoffs Pending lines that lack Folded counterparts (#576)
- Keep questions.md via **search_replace only** (never full-file write — #533)
- `git commit --only <paths>` always (new files: `git add` first)

---

## 7. Conventions that cost real bugs when ignored

1. **`git commit --only <paths>`** — concurrent agents; bare `git commit` sweeps the index (`12f47e3` incident).
2. **Independent red every merge** — cp-backup → sabotage a **pre-existing** production line → discriminating FAIL → cp-restore `cmp` byte-identical. Green red-run = finding, never relief.
3. **Assert preconditions the check depends on** at runtime; name the production line a sabotage would hit.
4. **handoffs.md:** `## Folded` first, then `## Pending`; both append-only; `→ folded` is append never delete.
5. **Lane model attribution:** dispatcher records alias; lanes cannot know their model (#469).
6. **Deploy:** identity not liveness; port 35110; stop → wait-port-free → ship → start → verify.
7. **#505 morphdom:** `#view` keyed reconcile; DOM that must survive a `/mtime` tick needs keep-rules / keys, not hope.
8. **transitions.md** for every appear/disappear/expand/collapse — no snaps among drifts.

---

## 8. FLAG: #397 shared deploy siblings

`.dreamwork/docs/findings/397-shared-deploy-siblings.md`

Deploy snapshots are per-target (`$dir/<basename>-watch.py`) but **siblings are shared** (`$dir/client/`). A `just deploy` from any worktree can repaint the main dashboard’s CSS while identity still reports current. Pre-existing since #480; extraction widened blast radius. Fix: per-snapshot sibling dir + Migration trailer + rehearsal. **Do not fix casually mid-fleet.**

Also from #397 reviewers (fixed before merge, for awareness):
- serving/deployed.report / deploy_state compare watch.py **plus** DATA_SIBLINGS per-revision
- styleguide audit must count client-only commits
- empty client assets refuse at load
- #425 symlink + sys.path; main() handler outside bind try

---

## 9. Commits this session (reference)

```
ab39daa7  hand-off fold backlog answer
5995d673  #571 Q1-Q4 → questions.md
444cc7e2  #571→#584 title (clears #306)
b36ce003  #575 finding (post-commit-blind guard)
6e3f7d4f  #585 retroactive HEAD~1 truncation check
47ee8731+93ff9a03+1f84a5d7+e20ef44e  #577 cherry-picks
b5817351+03d0b960  #577 fold + red note
a81af906  #583 brief
3fb4544c+ca02bc87+1e8d0353  #583 cherry-picks
1f2da3d1  #583 fold + qdual in DEFAULT_GUARDS
```

---

## 10. Persist-before-compact checklist (this file covers)

| Item | Where |
|------|--------|
| Current task / where we stopped | §0, §6 — handoff; no in-flight lane; #583 gate complete |
| Open questions count + titles | §5 |
| What landed + shas | §2, §9 |
| Verification honesty | §3 |
| Deploy / posture / delivery | §1 |
| Inbox / journal cursor habit | §4 |
| Next tasks ordered | §6 |
| Standing rules | §7 |
| Stale status.json | §1, §3 — rewrite on first tick |
| Do-not-do list | §6 |
| Compaction-unsafe residue | Only ephemeral screenshots under `/tmp/583-*.png`; durable state is git + this file + ledger |

---

## 11. Stop condition

Max: process maintenance/upkeep; **no new subagents**; hand off soon.  
This coordinator has **stopped selecting new work and will not dispatch**. Continue from this file unless Max redirects.

**First message for the next agent (suggested):**

> Resume ud-dreamwork from `.dreamwork/docs/handoff-2026-07-31.md`. HEAD `1f2da3d1`, deploy current on :35110. No in-flight lanes. Do not spawn subagents until Max re-authorises. Refresh status.json (stale fleet claim). Prefer Max’s desk questions over backlog #578–#582.

---

*Written 2026-07-31 ~15:40 AEST · session handoff + persist-before-compact*
