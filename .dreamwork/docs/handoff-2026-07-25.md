# Handoff — dreamwork loop on ud-dreamwork itself, 2026-07-25 evening

Written 21:33 by the coordinator, on the human's instruction to wind down
after the in-flight increment. **Status: dreamer-panels is finishing #142
(burndown panel, increment 3 of its bound); this doc gets a final update
when it lands.** Everything durable is on disk; this doc is the map, not
the record — the ledger (`.dreamwork/tasks.md`), `questions.md`,
`lessons.md` and git are authoritative.

## Where things stand

- **HEAD at writing**: 252207f on master. Verification baseline:
  **395 pytest, lint clean, 30/30 guards** (last full `just test` by
  dreamer-panels at 9ed526f; coordinator re-ran pytest+lint — matches).
- **In flight**: #142 burndown/stats panel — dreamer-panels, holdings
  watch.py, test_watch.py, watch-design.md, dev/capture/, justfile guard
  list. Approved plan: arrivals/completions/open-backlog derived from
  `git log -p .dreamwork/tasks.md`; the panel STATES its provenance
  coverage instead of drawing a human-vs-loop split the ledger cannot
  support (measured: 6/63 entries carry a human marker → #213).
- **Landed today (this session segment)**: #86 P1 plugin commands
  (a5a889d), #197 priority ordering (3f411f3 + adopted follow-up
  3073055), #140 deployed-revision line (a621f31), #166 commit-row
  expand + more-detail principle (9ed526f). Before that, the qsec run:
  #196, #199 P1, #198, #163, #175, #165. Details in Recently landed.

## Awaiting the human — priority order (all in questions.md)

1. **P1 — may I deploy the dashboard?** The live :35110 serves a
   revision now ~20 watch.py commits behind, missing the #179 focus fix,
   the whole input-safety suite (#163/#175/#165/#199), and everything
   above. His submissions through the live page are unprotected by the
   new persistence. DREAMWORK.md says no deploys; a deploy plainly
   happened once; authority question stands.
2. **P1 — which "t3 connect"?** (#202) Three candidates found; decides
   #201 build-vs-integrate.
3. P2 — cross-machine answers routing; PreCompact hook shipping (#138);
   ud-dw-generate ownership (untracked at repo root, deliberately never
   touched); #194 commit-range source for zip installs (rec: ship a
   generated changelog).
4. P3 — dreamtask stage 6 (harvest); dreamhub URL space (#96).

## Parked deliberately

- **Composer row batch (#161 → #164 → #99)** — parked until the human is
  back: taste is the deliverable. Plan at `docs/plans/composer-row.md`.
- **#194 steps 3-4** (init step + discovery subagent) — gated on the P2
  commit-range answer. Steps 1-2 landed (bin/ud-dw-githash, frontmatter
  contract).
- **#98 queue panel** — needs a DREAMWORK.md fit-check (new page
  surface) before anyone builds it.

## Next-dispatch recommendations (when the loop resumes)

1. **Guard-craft batch**: #210 (assert-the-fixture-difference sweep) +
   #207 (deletion-observability class) — both from today's finds, both
   protective, no taste gate. Dream `2026-07-25-2031` has the shape.
2. **#204** now has its adoption path: #166's expand handler takes a
   LIST of surfaces; each snapping peek is one list entry away.
3. **Shared runner family**: #192 + #148 + #203 as one increment.
4. #205 (dreamwatch — heartbeat into monitor) has a full plan at
   `docs/plans/heartbeat-into-monitor.md`, unstarted.

## Process rules earned today (the expensive ones)

- **#206 — six documented message crossings.** Claims and holdings move
  through FILES; messages advise. A landed-report names what it did NOT
  do. Never conclude a report is absent from a read taken inside the
  window it was due. A held path carrying changes you did not make is a
  question, not a commit (the coordinator committed a live agent's tree
  at 3073055 and it was luck it was complete).
- **Enforce against the tree, not the ledger snapshot** — the
  coordinator demanded a contract that had landed three hours earlier.
- **A backup is not a backup until it exists** — work was destroyed by a
  checkout racing a failed patch-save, and recovered only because agent
  transcripts record every edit (replay the tool calls from
  `subagents/agent-*.jsonl`).
- **Dreamer clocks drift forward.** Five future-stampings today (four
  dreams, then panels' reports at +30 and +72 minutes). Lint catches
  committed dreams; nothing catches report headers. Stamp from
  `date`, never from reasoning.
- The five house rules for new checks are in `dev/capture/README.md`;
  the vacuous-when-fixture-grows defence is in the dream and #210.

## How to resume the loop

Read the ud-dreamwork SKILL.md and follow `initialization.md` — it arms
the heartbeat (4.75m), the `watch-events.log` tail and the
`coord-inbox.md` tail (`~/.cache/agent-comms/ud-dreamwork/`), then
reconcile this doc against the ledger. status.json carries the stopped
state. The session task rows mirror the ledger; the ledger wins.
Guard ports 39890-39899 (hub 39880-39889); dreamhub serve on :39875 is
long-running and not the loop's. Never `git add -A` (submissions.log
holds the human's raw words); always `git --no-optional-locks`; never
touch `ud-dw-generate`.

## Final update — 21:58, loop stopped

dreamer-panels' bound completed clean: **#140 a621f31 · #166 9ed526f ·
#142 bb56f19** (+ dream `2026-07-25-2146-a-gate-that-cannot-be-checked`).
Closing verification: **403 pytest, lint clean, 31/31 guards** (panels'
full `just test` at bb56f19; coordinator re-ran pytest+lint — matches).
No orphaned guard servers; every commit staged by explicit path;
submissions.log never staged. The loop's monitors are STOPPED; resume per
"How to resume" above.

Panels' handoff notes, verbatim in substance:

- **#142 overran its 75m budget substantially** — mostly in the guard,
  and the overrun bought the collapse bug (regroupBars' cleanup cleared
  the renderer's own inline `height:N%`; #198's shape; fixed in
  bb56f19). Flagged as judgement, not defended.
- **#151's gate is an optimisation in the burndown, not a behaviour, and
  its check cannot go red** — deliberately recorded as unguarded rather
  than left with a green line implying otherwise (dream has the full
  reasoning).
- **#204 is NOT a one-liner despite #166's surface-list handler**: the
  list path fits keyed-list members only; the four plain peeks want the
  `.qsec > summary` shape. The ledger entry now says so.
- **ledger_stats caches on HEAD alone** — the chart's right edge is the
  compute moment until HEAD moves. Correct for a history panel; expect
  someone to report it as a bug once.
- The burndown panel now VISIBLY argues for #213 (provenance field): it
  reports `sourced 7/67` and states what that coverage makes impossible.

Coordinator note: none of the coordinator's four advisory messages
reached panels before its commits (#206's latency, now seven observed
crossings) — everything still landed consistently because claims moved
through files. Its reports were future-stamped (+30m, +72m, +55m); a
successor should treat REPORT header times as unreliable today and trust
git commit times.

Retired today, in order: states, thread, hubbuild, dreamtask,
guide-precompact, reviewer-skillmd, rows, identity, windowcoords, motion,
gesture, qsec, plugcmd, panels — fourteen agents, all folded.
