# Doc map — ud-dreamwork (the skill, self-hosted target)

What lives where, and what the loop keeps current. Link outward; never
duplicate content that has a home below. Single-source rule: a fact
lives in exactly one doc — generic reference (useful to every target)
at the skill root, instance-specific knowledge under `.dreamwork/` —
and everything else points at it. The skill root is shipped product:
this instance is its upstream maintainer, so docs-freshness passes
cover it too.

| Doc | Covers | Loop keeps current? |
|---|---|---|
| `SKILL.md` | The product: philosophy, loop, selection, subagents, durable state, commands, guardrails | Yes — every behavior change lands here or in a reference file |
| `initialization.md` | The 11-step init procedure | Yes |
| `reflection.md` | Post-change checklist | Yes |
| `file-formats.md` | Required shapes for loop-written, tool-parsed files; questions.md in full | Yes — a reader change lands here in the same commit |
| `.dreamwork/docs/reviews-*.md` | Fresh-eyes review findings, kept whole; the fixes land separately | Keep while its proposals are unspent |
| `compaction.md` | Pre-compaction checklist, the notice protocol, per-client sending safeguards | Yes — client dialects change |
| `writing-plugins.md` | Plugin-authoring contract, extension seams, state split | Yes — validate against each new plugin |
| `watch-design.md` | watch.py standing design: routes, confinement, write exceptions, contract | Yes — shipped beside the tool it documents |
| `stop-hook-variant.md` | Unimplemented wake fallback design | Only if implemented or invalidated |
| `DREAMWORK.template.md` | Wizard seed for new targets | Yes — must track wizard section changes |
| `migrations/` | Versioned target-affecting changes; latest filename = version | Append-only; README holds the protocol |
| `.dreamwork/docs/plans/` | Active feature plans, alphabetical (answers-page, artifact-templates, attention-modes, brief-record-storage, ccc-runner-routing, cli-warning-layer, component-transition, composer-chat, composer-row, daemon-mode, delivery-modes, draft-durability-design, dreamhub-stage1, filebytes-range, g2-question-draft, gh-etiquette-shim, goal-1-membership, goal-hierarchies, goal-tree-population, harness-containment, heartbeat-into-monitor, hook-plugin-coverage, hub-public-auth, hub-ssh-auth, lan-bind, lane-containment, ledger-sqlite, lessons-retrieval, main-agent-recap, matt-pocock-skills-bridge, migration-notices, modularity-and-startup, note-reply-threading-254, orchestrator-posture, parallel-architecture, posture-autonomy-axis, question-instruction-options, question-updated-wake, questionnaire-survey, render-architecture, review-essential-marks, session-log-questions-draft, session-log-view, subagent-containment, suite-under-lanes, superseded-contracts, task-store-schema, task-transition-boundary, tasks-page, threaded-notes-spec, ud-dreamtask, ud-dreamwork-github, user-event-inbox, user-event-journal, user-event-journal-implementation, user-settings, version-and-upgrade, watch-client-extraction, work-hierarchy, ws-delta-transport) | Prune when features fully land; `lint.py` fails the row against the directory |
| `.dreamwork/docs/visual-audit-2026-07-31.md` | Read-only visual inspection of the live dashboard, 12 routes x 6 viewports (1920/1440/1040/1000/999/390). 9 defects + 8 opportunities, each cited against `watch-design.md` or `style.css` by line. Screenshots (81) are NOT in git — they live at `~/served-html/ud-dreamwork-visual-audit-2026-07-31/`, viewable at `http://xsm:10435/ud-dreamwork-visual-audit-2026-07-31/`. Also records what it checked and found CORRECT, so nobody re-audits #583, #559, #351 | Prune when its defects are filed and fixed — #595-#601 carry them |
| `.dreamwork/docs/state-audit-2026-07-31.md` | Point-in-time audit of store vs git vs handoffs vs questions, pinned to HEAD `80efbb3a`. Its value is the **discrepancies** (§1): stale `blocked_on`, questions counted against him that were not his, worktrees invisible to the `.worktrees/`-only check. The categorised task list decays fast; the method does not | Prune when its findings are filed or fixed — #590 carries the re-rank |
| `.dreamwork/docs/backlog-rerank-2026-07-31.md` | #590's proposed priority for all 147 open tasks against his 2026-07-31 focus (watch.py modularity + built frontend), pinned to HEAD `e0aa3fee`. Movers (§2), blocked-and-stale beyond the state audit's five (§3, found a sixth: `#342`), duplicates/near-duplicates (§4, `#124`/`#368` are the same work), dead-or-superseded recommendations (§5), and a top-ten (§6). Analysis only — no priorities were written to the store by this lane | Prune once the coordinator has applied or explicitly declined its recommendations |
| `.dreamwork/docs/motion-flake-diagnosis-2026-07-31.md` | #616 diagnosis: the `motion` guard's intermittent red on a clean tree is the GUARD's race, not the product — per-frame traces show the trace window opening on the settled corpse of the #179 typing-commit's own departure, so the two first-to-last delta assertions read 0 while the product's gesture was correct in 28/28 measured runs. Carries the ready-to-apply patch (drain-before-trace + sign-from-computed-transform + #442 `transitionstart` snap detector), validated in scratch copies with red-proofs both ways | Prune when the patch lands in `dev/capture/motion.mjs` (blocked on lane-595scroll freeing `dev/capture/`) and #616 folds |
| `.dreamwork/docs/research/` | What a commissioned answer MEANS for this project; the research itself goes to the KB when it is generic. Distinct from a measurement (`measurements/` answers how-fast/how-many with a number) and from a plan (`plans/` proposes a change; research informs one). Not enumerated on purpose: research is append-mostly (kept while the conclusion holds), so a list would only grow, and the self-describing slugs make the directory its own catalog — the plans row enumerates because plans churn in both directions, which research does not. Kind contract + naming: `research/README.md` | Keep while the conclusion holds; no lint check — nothing enumerates it |
| `.dreamwork/docs/spikes/` | Timeboxed experiments that answered a question with a number; the branch holds the diff | Keep — a measured answer outlives the code that produced it |
| `.dreamwork/review/` | Rich decision artifacts paired with a questions.md entry | Banner them decided; archive with the answered question |
| `.dreamwork/{lessons,questions}.md` | Distilled lessons; asks for the human | Yes — groomed in rotation |
| `.dreamwork/tasks.md` | Five-line migration shim; the live queue, permanent ids, and next id are in the gitignored `.dreamwork/ledger.sqlite3` store | Keep only while the migration shim is supported; do not edit it as the live queue |
| `.dreamwork/docs/github-processes.md` | The repo's GitHub shape and conventions (ud-dreamwork-github plugin) | Yes — re-survey when CI, labels, or PR flow appear |
| `README.md` | Public face of the repo: what dreamwork is, install, where to start | Yes — must not drift from SKILL.md |
| `relay.py` | Appends a coordinator message to a subagent inbox — body from stdin, stamp from the clock | Yes — it encodes two failures, not a convenience |
| `transitions.md` (skill root) | How the page moves — the ONLY source; every transition obeys it, and CLAUDE.md says so | Yes — extracted from watch-design.md so there is one description, not two |
| `igc-method.md` / `igc-concepts.md` (skill root) | The IGC method + its Critical-Fallibilism conceptual grounding, **vendored** from the `use-igcs` skill so an install without it still has the method; each file carries the upstream sha it was synced from | Yes while vendored — re-sync on the docs-freshness rotation if the upstream *application rules* change (decision + matrix: `igc-bundling.md`) |
| `subagent-protocols-for-{subagents,coordinators}.md` + `subagent-protocols-watch-file.sh` (skill root) | The coordinator↔subagent inbox handshake, **vendored** from the `subagent-protocols` skill (#466, same mechanism as #447) so an install without it still has the channel; each file carries the upstream sha it was synced from | Yes while vendored — re-sync on the docs-freshness rotation if upstream changes the handshake itself |
| `CLAUDE.md` (skill root) | Rules for agents working ON this repo (not for the loop running elsewhere) | Yes |
| `lint.py` | Checks a target's `.dreamwork/` against the shapes its readers require, by calling the real readers | Yes — a reader change lands here in the same commit |
| `deployed.py` | Which revision a target's dashboard is ACTUALLY serving, by byte-comparing the deploy snapshot against history. `python3 deployed.py --target .` | Yes — the deploy snapshot's identity is recorded nowhere else |
| `dev/ledger.py` | The one supported way to fold a ledger entry and count open/landed ids (#440): reuses `watch.parse_ledger` and the anchored heading patterns, asserts the two headings are unique and ordered before AND after every write. `python3 dev/ledger.py counts`, `python3 dev/ledger.py fold <id> --note <text>` | Yes — the anti-corruption invariant it enforces lives in it, not in a linter reading the aftermath |
| `.dreamwork/docs/reload-signal-design.md` | #426's design: the identity signal a running agent reads to tell its own tree moved; the per-surface reload actions; the `#263` lane-H "shared mechanism?" decision (no); what is not worth building | Keep while its recommendations are unspent; the one increment that landed (`watch.skill_identity()`) is documented in `watch.py`'s docstring |
| `.dreamwork/docs/migrate-watch-symlink.md` | The procedure (and per-step checks) for #368's first increment flipping `watch.py` to a symlink to `deprecated/watch.py`; the #425 safety net it depends on, and the `serving_report`/`deployed.py` interactions #368 must close | Yes while #368's symlink has not landed; prune when it does |
| `.dreamwork/docs/citation-anchoring-design.md` | #914's design, relating #914 (writer side: a correct citation drifts the instant a line is inserted above it) to #880 (reader side: a wrong-line citation is undetectable) and #847 (paydown of the unqualified). **Verdict: two jobs, not one or three** — the 19 certified citations are disjoint from #847/#880 *by construction*, so the corpus job and the instrument job share no population. Measured, not assumed: the certified population is **19, not the 25 in circulation** (25 is DRIFT); one line inserted into `COMMANDS` collapses 19 → 0 with the real guard in a scratch clone; **12 of 19 pinned coordinates do not hold at their pinned revision**, so the guard certifies provenance its own `--fix` manufactured. Rejects symbol anchors (10/10 unique resolutions land in the wrong place), per-citation offsets, fingerprints, tolerant re-location and auto-re-certification. **Recommends retiring the oracle**: bind `Counter[(doc, token)]` + a resolvable revision pin — no base-revision text, no DRIFT, no line arithmetic — so the check never opens `watch.py` and the #914 event does nothing. Names its own defeating event (prose restructured), requires the PASS line to read `pinned, not verified`, and has **no migration** because it edits zero documents. §2.6 found `test_reanchor_citations.py` **red on master** (→ #918). **Design only — authorises no code**; §5 is the ~20-minute first increment against `dev/check_watch_citations.py` alone | Keep while the oracle stands; prune when §5 lands and the guard's own docstring carries the `pinned, not verified` narrowing |
| `.dreamwork/docs/suite-baseline-2026-08-02.md` | #919's measurement: **the repo was RED** — **3192 passed / 22 failed / 0 skipped / 0 errors, 3214 collected** at `a23dd6a0` (coordinator re-run at `98529835`: 18 failed; #918 then cleared 2). Every failing node id is named. Trustworthy because of what it REFUSES: the browser-guard portion was not run and it says so, arguing the omission cannot make the repo green because 22 pytest failures already do; and when master advanced 7 then 5 commits mid-lane it **declined to carry the count across the rebase boundary** rather than manufacture a current figure — *"I did not manufacture a current count"*. Records the `concurrent_tests.py` advisory before and during, and the thread caps used. **This is why `gate-coverage: 4 of 4 declared gates passed` must not be read as "the repo is green"**: no gate runs the suite (a deliberate cost decision) and nothing else did either. Also carries the UNBUILT design for a re-runnable baseline (`dev/suite_baseline.py`, atomic `RUNNING` attempts, `last_full_pass` eligible only on a clean tree, a formatter printing `last_attempt` AND `last_full_pass`) and replacement gate wording that states what was NOT run | Keep as the dated baseline record; the red inventory is worked under #923 and the recorder/gate-wording build under its follow-up — prune only when a recorded baseline supersedes it |
| `.dreamwork/docs/dangling-citations-2026-08-02.md` | #925's census of documentation citations naming files that do not exist, and **its ruling: DO NOT build an open-corpus regex guard** — over 134 distinct paths one would be wrong on **≥120** (external, runtime, historical, forward, scanner-artifact), far past the "wrong more often than right is worse than none" bar, and exactly the noise that gets a checker switched off. Scanner is `dev/dangling_citations.py`; it prints both denominators AND its exclusion rules, including that gitignored-but-present files do not travel into a lane worktree so **a worktree run over-counts — run it from the main checkout**. Measured 228 occurrences / 134 distinct at `c5e2b302`, re-run at `d9b3afad` showing +4 distinct with the composition unchanged. **The actionable set is small and named: STALE = 4 paths / 15 occurrences** (`dev/apply_reanchors_i3.py` 7 via #918, a superseded dream 3, `dev/capture/runmode.mjs` 3 and `rundesc.mjs` 2 via #547) — and even STALE is polluted by past-tense historical prose, so a deleted-file citation is not automatically a defect. **The one honest discriminator is ENROLMENT, not detection**: a human asserting "this is a present-tense claim" when it is pinned — the `dev/check_watch_citations.py` model #921 landed. States its own blind spots unprompted (misses Markdown link targets, bare un-backticked paths, paths with spaces; over-matches absolute-path fragments and `SKILL_DIR/` placeholders), calling itself *"a defensible lower bound … not a complete inventory"* | Keep while #847's campaign is unfinished — it is the raw input for enrolling a pinned population, and the ruling is what stops a noisy guard being built |
| `.dreamwork/docs/citation-repair-2026-08-02.md` | #920's re-measurement, which **refuted its own filing**: not `12 of 19` but **`false=19 correct=0 unresolved=0 examined=19`** at `c5e2b302` — every pinned occurrence is false at `dc739001`. The filed 12 had mixed a 19-occurrence denominator with a 12-member symbol-selected subset and collapsed duplicate tokens; the design's `0 of 12` proximity finding is true of its subset but does not answer the all-citation question. **This triggered the task's explicit close condition, so the lane made ZERO repairs** and instead recorded one judgement row per occurrence plus the exact replacement text — including seven in coordinator-custodial `handoffs.md` it was forbidden to touch. Carries a **re-runnable census block** that always prints `examined=19`, so `false=0` is distinguishable from examining nothing (#868); I extracted and re-ran it on master and it reproduces. Its direction-2 red-proof is the valuable half: changing the prose claim `posturePicker`→`tintPicker` while keeping a correct pin made the census report `correct=1` — **the manifest cannot prove the prose still means what its reviewer recorded, and can still bless a wrong-file claim** (content in `client/style.css` described as `watch.py`, the live instance #847 names). **Campaign ruling: merge #920 into #847 as one corpus campaign, retaining separate qualified-false and unqualified denominators** | Keep while #847's campaign is unfinished — it holds the 19 replacement texts and the honest statement of what the census cannot prove |
| `.dreamwork/docs/red-triage-2026-08-02.md` | #923's classification of master's red: **19 failures, SEVEN distinct causes** — the count of failures was never the useful number. Measured at `439034fe` (194 collected in the named 12-file set), recounted after rebase onto `681f39cb` at **17 failed / 177 passed**. Classifies each failure (a) hand-maintained literal fallen behind, (b) genuine regression, (c) obsolete assertion to retire with an argument. **Only ONE is a genuine regression**: cause 1, a detached deploy that imports then dies before serving (`lint: refusing detached corpus root … expected SKILL.md beside lint.py`, from `3cc9b0e6`) — filed **#927 P1**, because weakening that boot proof would hide a dark dashboard. The other six are stale expectations: pre-v9 schema pins (#928), one partial v5 fixture failing six event tests before they reach their assertion (#929), pre-launch-id path depths (#930), a hand-maintained write-route population (#931, partly closed here), a spent refactor-parity capture (#932), and a substring scan that false-fails on prose and would false-pass an alias (#933). Trustworthy because it **classified three failures the brief never forecast rather than silently excluding them**, and because it states its measurement boundary and recounts across the rebase. The lane fixed only its smallest coherent group, deriving the HTTP expectation from **paths actually POSTed at the request seam** vs the production dispatch — independent sources, so a route added only to the dispatch shows as missing coverage | Keep while any of #927–#933 is open; prune when the last cause closes and the reds are gone |
| `.dreamwork/docs/plans/hub-ssh-auth.md` | #360's design + docs (ssh-rooted self-hosted hub auth). SUPERSEDES `hub-public-auth.md`'s identity recommendation (Cloudflare Access/Tailscale as the boundary); EXTENDS its threat model, TLS analysis and `/data.json`-leak (C2), which are inherited not re-derived. Supersedes `#276`'s LAN bearer token if option 2 (session key over ssh) lands. Four options compared — ssh tunnel (document now), session-key-over-ssh (build first, behind a local Caddy per his Q2 ruling), user/pw (fallback, not worth building standalone), SQRL (no — dormant + stdlib-blocked) | Prune when the chosen option fully lands; the ssh-tunnel recipe (§6) is documentation of what works today and outlives the decision |
| `.dreamwork/docs/plans/migration-notices.md` | #458's design: a migration leaves a declared notice in the hot data file a stale agent still reads; IGC (where / distinguished / pointer / retirement + shrink); writer is `migration_notice.py`; contract in `file-formats.md` | Keep while the notice mechanism stands; prune only if the hot-path channel is retired |
| `.dreamwork/docs/plans/harness-containment.md` | #450's finding: where a harness **cannot** intercept a lane's file writes at all, so containment is partial by construction. Verified for `ccc` only; every other harness is marked unknown rather than guessed. Conclusion: no further mechanism exists under present authority (`#288` grants no kill or sandbox authority), so the deliverable is the stated ceiling plus the `SKILL.md` warning — read with `lane-containment.md`, whose guard this keeps from being read as a guarantee | Keep while containment is partial; prune when a harness can refuse a write |
| `.dreamwork/docs/plans/hook-plugin-coverage.md` | #470's audit of what the `ud-dreamwork-hooks` plugin guarantees and what is actually checked: a per-component claim table, the finding that the consent gate does **not** drift from `plugin_resolver` (byte-identical regexes), evidence the hook tests really are collected by `just pytest` (so this is not a `#310` case), and the one gap that cannot be closed without his harness running — no test goes through the harness, so none would notice if it stopped calling the hooks. Stated as a ceiling rather than faked, because the fake would have to synthesise a Claude Code dispatch completely enough to be testing the synthesis | Keep while the harness-firing gap stands; prune when a harness-level check exists or the plugin is retired |
| `.dreamwork/docs/plans/component-transition.md` | #630's sequenced plan for the transition to a component-based React web UI, under his 2026-07-31 ruling (receipt `dc9200a0`). Measures that *"old inline-html in watch.py"* names **two** things and only one is actionable: `watch.py` holds ~8 literal inline-HTML lines (`page_shell` :549-560 plus one `<pre><code>` at :730 — the server's document envelope, out of scope), while the real target is the string-builder idiom `#397` moved into `client/` (~6,300 lines, nine route builders, `qaCard` on four surfaces). Sequence: **P1** build step alone (committed `client/dist/`, sha256 manifest guard, page byte-identical, lands and reverts alone) → **P2** React runtime + component registry mounting nothing → **P3** first converted surface `/research` (smallest builder `views.js:1293-1307`, zero gesture families of its own — the review-row FLIP is dashboard-gated at `router.js:4305-4306`, verified — so lowest blast radius while still exercising registry + tick + primitive delegation + the oracle protocol) → **P4** session view (`#613`'s three design calls left untouched) → **P5** claude-design tokens-then-wrappers. qaCard-family endgame deferred behind a flag. Tiers divergence honestly rather than claiming it away: wrappers are divergence-proof at the **markup** level (render *calls* the builder; the build entry is *generated* from watch.py's AST-read asset order, consumed never copied), but **staleness cannot be made impossible** without a serve-time build, which the no-node requirement refuses — so it is made impossible-to-**miss** via a sha256 manifest → `lint.py` ERROR + `serving_report` WARN. For conversions the permanent mechanism is **deletion**: the builder dies in the flip commit, zero-commit overlap | Keep while the transition is unfinished; prune when the last builder is deleted or he rules the transition off |
| `.dreamwork/docs/plans/ccc-runner-routing.md` | #469's measurement: `ccc` maps alias→model in `~/.config/ccc/config.toml` (`[aliases.glm52]` is the **grok CLI harness** running `glm-5.2`), so `warning: runner "grok"` names the harness and not the model. The harness exports only `CCC_PROVIDER` to the child, so a lane cannot know its own model and its self-report is wrong — provenance comes from the alias the dispatcher passed | Keep while lanes are dispatched by alias; prune if the harness starts exporting the model |
| `.dreamwork/docs/plans/lane-containment.md` | #465's design: a lane editing the main checkout instead of its worktree, and nothing noticing until a merge fails. IGC over four candidate mechanisms (pre-commit hook / pre-merge assertion / marker file / `git -C`), with the pre-condition finding that `status.json` carries no file ownership (the brief's premise had drifted). Survivor R5: a pre-commit guard on the main checkout reading ownership from the lane's brief `Lane-owns:`; the pre-merge assertion is the recorded successor. Guard is `dev/lane_guard.py`; contract in `file-formats.md` | Keep while the guard or its successor stands; prune when containment moves to a built-in harness mechanism |
| `.dreamwork/docs/plans/composer-chat.md` | #504's design: a far-left `chat` composer command, reconciled (real IGC A/B/C × G1–G5) as the **main-dreamer first slice of the approved #229/#270 topic-chat direction**, not a separate channel — survivor A reuses the recovery spine (receipt → application → transcript) and forbids a parallel chat store or second durable inbox. Settles the message path, the thread model ("thread" = a chat in #229 vocab, created on first send), the reply-instruction attachment (a property of the chat kind's consume contract, no per-message storage), and the DB shape (the #263 receipt + `chat.json`/`transcript.md`, no separate "just-in-case" table). Escalates four forks for him with recs (POST route home, UI word "thread" vs "topic chat", #342 delivery default, whether the slice ships a visible surface). Pushes back that "tracked in the db, but only just in case" understates the receipt — under #342 it is the delivery path, not a backup. **Design only — authorises no code; the implementation anchor stays #373**, and this slice's settled shape is input to it | Keep while the design direction stands; prune when #373's topic-chat build supersedes it |
| `.dreamwork/docs/plans/orchestrator-posture.md` | #510's design: an 'Orchestrator' option in Posture, IGC'd over how to integrate it. Investigates the referent (his word for the coordinator role, `questions.md:1119`) — two live readings: orchestration **mode** (coordinator implements nothing, only dispatches+reviews; the coordinator-only-loop mode `dogfood-orchestration.md` records) vs orchestrator **identity** (which model/session runs the loop). Designed against mode; identity pushed back as a dispatch/provenance fact (alias owned at dispatch, `ccc-runner-routing.md`) mis-categorised as a posture dial. Real IGC (I1–I5 × G1–G5): survivor **I1 — a fifth posture axis**; fold-onto-delegation refuted on independence+overload, the parked `hierarchical` run-mode value (`watch.py:343`) refuted on re-bundling (`#443`)+preemption, control-only on no restart-survival, sibling file by the standing #445/#342 widen-not-sibling ruling. Escalates three forks with recs (mode vs identity; binary `hands-on`\|`orchestrator` closed set; land the axis, do not enable `hierarchical`). Pushes back that "doesn't match anything" is half-right — it matches `delegation` as a neighbour, and the fleet concept is the disabled `hierarchical` run-mode value. **Design only — authorises no code; the implementation split (WebUI subagent + coordinator docs) happens only after he rules** | Keep while the design direction stands; prune when #510's axis lands or he rules identity and the design restarts |
| `.dreamwork/docs/plans/user-settings.md` | #571's design: persistent user settings — schema + seam + frontend exposure. Investigates the amr-ui reference (`~/src/refs/amr-ui/src/lib/settings.ts`): its **registry model maps** (code-declared `SETTINGS` as single source — kinds, defaults, tags/category, descriptions, enum closed-sets, hidden subtypes; store holds only non-default overrides; generic page iterates it), but its **localStorage storage layer does not** — #228 ruled settings server-side, converging across tabs AND browsers (never localStorage). Real IGC (I1–I5 × G1–G5): **two survivors tie** — I1 (table in the ledger store, registry-in-code) and I2 (separate settings store, registry-in-code), both passing server-side/reaches-loop/lint-checkable/distinct-from-posture/schema-described; metadata-in-DB (I3) refuted on un-lintable closed-sets, fold-into-posture (I4) on overload+can't-describe-heterogeneous-kinds, localStorage (I5) on per-browser+no-loop-reach+no-lint. The I1/I2 tie escalated as Q1. Settled seam: `read_settings` in `collect()` rides the existing `/mtime` poll (`watched_mtime` walks `.dreamwork/`); write via a new `/settings` route in `WRITE_ROUTE_HANDLERS` (E2Shadow receipt + #274 idempotency + one events line, the posture ceremony). Escalates four forks with recs (table-in-ledger-store over separate-store; one TEXT+JSON value col over typed cols; a new `settings.py` registry module imported by lint+watch; settings are a SIBLING subsuming posture's pattern NOT a superset absorbing it — posture axes are not user settings). First consumers: #573 `composer.askMeDefault`, #570-future `composer.rememberManualResize`, #295 `gfx.dither` (button-group, the gfx section he named, routed to #228). **Design only — authorises no code** | Keep while the design direction stands; prune when #571's store lands or he rules the I1/I2 fork |
| `.dreamwork/docs/plans/ws-delta-transport.md` | #614's cost + plan for his websocket/RPC/state-delta transition (receipt `a71d1105`). Measured before arguing: `/data.json` is 917 KB served in ~200 ms with no gzip over HTTP/1.0, and the 2 s `/mtime` gate is **broken open** — serving `/data.json` touches `ledger.sqlite3-shm`, which advances `watched_mtime`, so every open window refetches the full document every 2 s while the real change was 21 B (quiet 10 s) to 5.4 KB (60 s incl. a commit). Real IGC (I1–I6 × G1–G8): **WS-with-RPC refuted** — a second write path beside the journal (the two-descriptions rule applied to writes) plus ~250–400 lines of hand-owned RFC 6455; survivor **I2: SSE push of deltas *derived* from `collect()` (changed keys shipped whole, born-red reconstruction test + runtime `check` hash) + the existing journaled POSTs as the RPC direction** — SSE on stdlib `ThreadingHTTPServer` verified live in a real browser incl. `Last-Event-ID` auto-resume. Phases: 0 = one-line `-shm` exclusion closing the self-perpetuating refetch loop (worth landing regardless); 1 = `/data.json?since=` delta on the existing poll; 2 = `/events` SSE carrying the same payload, poll kept as fallback; 3 = topics for the session-log streaming view; 4 = WS only behind named triggers. Escalates ONE ask (`rec` SSE vs `ws` push-only) since he named the mechanism explicitly. **Plan only — authorises no code** | Keep while the transition is live; prune when the push channel lands and `watch-design.md`'s transport section absorbs it |
| `.dreamwork/docs/plans/session-log-view.md` | #613's design for the live/streamed hierarchical session view (his receipt `a71d1105`). Measured against the real 82 MB / 31,784-line session, not assumed: compaction pages ARE derivable (`compact_boundary` + `isCompactSummary`, so his "if known" resolves to *known*); turn/step boundaries derivable; append-only VERIFIED under growth, so his line+byte bookmarks are stable; full rescan **1.0 s at 82 MB/s**, which is what makes his "just rescan" fallback correct; subagent transcripts at `<session>/subagents/agent-*.jsonl` in the same grammar (the #615 anchor). Finding: **`system_prompt` is never in the transcript**, so that hierarchy slot renders absent rather than invented. Bookmarks live in a new gitignored `session-index.sqlite3` — a derived cache, deliberately NOT the ledger store. Watcher is one `SessionWatcher` seam (ctypes-inotify primary, bounded stat-poll degrade). Everything but the mount binding is independent of the #591/#505 G2 ruling. **Design only — authorises no code**, with two exceptions that have since been ruled and built: §6's discovery gap (#665 — the loop self-reports `agent_session`) and §10, which is the repo's **per-client onboarding checklist** (what must be MEASURED before a CLI client is supported) | Keep while the view is unbuilt; prune when it lands and `watch-design.md` absorbs its surface — **except §10, which outlives this plan: it is the only per-client onboarding checklist there is (#665), so it must be REHOMED rather than deleted when the rest is pruned** |
| `.dreamwork/docs/plans/main-agent-recap.md` | #691's design (his receipt `323d2ef1`): a cheap model recaps what the main agent is doing, once per beat at a 114 s offset, stored in a new `recap` table in `ledger.sqlite3` and rendered on the dashboard. Proven end to end before designing — a real `ccc -y @glm52` run over the live session produced an accurate recap in 19 s from a 6.2 KB digest (14.8 s at 70 B, 24.6 s at 25 KB; 238-257 MB peak across **7** processes, so the timeout must `killpg`). Reads the same transcript as `session-log-view.md` and shares its measurements. **Blocking finding: `status.json` carries no `agent_session`**, so nothing names the main agent's transcript, and deriving it from the target dir hits a two-day-stale file because the live session sits under a *worktree* slug — the design refuses rather than guesses, and the missing key is a #665 regression worth its own task. Settles: compaction cannot fool it (the transcript is never truncated, the `isCompactSummary` blob is dropped by exact field test, the boundary is marked); cap 24 KiB head-1/3 + tail-2/3 with a marker naming count/span/volume; the ledger store over the journal (receipt-authority-only by `user-event-journal.md:19-20`, and gitignoring does not discriminate — both are); an IGC over six scheduling seams whose survivor is a `tee` leg on the existing heartbeat pipeline, since an independent timer holds an *offset* but not a *phase* (plain mode is free-running; aligned mode breaks once a day because 4.75 min does not divide 24 h); and `#559`'s cross-dissolve gated on the recap's id, never the tick. Escalates three forks with recs (gate home — noting `SKILL.md:913`'s "experiments are feature-gated" has **no implementation anywhere**; cadence; whether it animates at all, against five code sites saying NO MOTION for this panel shape). **Design only — authorises no code** | Keep while the recap is unbuilt; prune when it lands and `watch-design.md` absorbs its surface |
| `.dreamwork/docs/plans/session-log-questions-draft.md` | The three #613 calls drafted for `questions.md` (component-system reading, watcher mechanism, which-session-is-running). Placed 2026-07-31 17:20; kept because it records the reasoning behind each `rec`, which the questions entry compresses | Prune once #613's questions are answered and folded |
| `.dreamwork/docs/plans/g2-question-draft.md` | The #591 ask drafted for `questions.md` (per-surface G2 reading, claude-design breakpoint, framework). Placed 2026-07-31 17:00; kept for the reasoning behind each `rec` and the stated reopen condition | Prune once #591's ruling lands |
| `.dreamwork/docs/plans/suite-under-lanes.md` | #428's design + increment 1: the dogfood lint test lints the LIVE working tree, so under 8 concurrent lanes it false-redred when another lane's commit moved the tree mid-run. Fix: assert against a HEAD worktree snapshot (~94ms, `frozen_tree` fixture) so the tree is immutable. Enumerates the 7 sibling live-tree dogfood sites in `test_lint.py` (only the full-`run_checks` one false-redred tonight) and the adoption rule (snapshot / skip-with-printed-reason / move-to-quiet-gate). Skill-tool reads staying live is correct: they are committed code no lane owns | Keep while the snapshot pattern or its successor stands; prune when the suite runs under isolation that makes live-tree reads safe |
| `.dreamwork/docs/igc-bundling.md` | #447's decision to bundle IGC by vendoring (option D) — its IGC matrix (A/B/C/D × six goals), the staleness story, the SKILL.md placement at the four judgement sites, the lint-check decline reasoning, and the downstream effect on ud-dreamtask | Keep while the bundling decision stands; the matrix records why D over A/B/C |
| `.dreamwork/docs/open-task-census.md` | #420 inventory of the open ledger: derived open/landed counts, blocking posture, stale blockers, duplicates; coordinator working doc, not a proposal | Refresh when the dispatch question is inventory again; superseded for *selection* by dispatch-shortlist |
| `.dreamwork/docs/dispatch-shortlist.md` | #437 ranked 8–12 startable tasks with ownership sets, live-lane conflicts, size, and parallel-safe groupings — the dispatch view on top of the census | Yes while used for selection; overwrite on each re-run; prune when a standing tool replaces it |
| `.dreamwork/docs/mitigation-audit.md` | #416 dated re-check of the four unchecked `~/CLAUDE.md` system-mitigation records (Brave ozone, sccache-server, pi-powerline-footer patch, ntp-force-sync.timer): one-line command + verbatim output + holds/drifted/gone per claim; reword proposals only (does not edit `~/CLAUDE.md`) | Refresh when a mitigation is claimed or after package upgrades that overwrite local patches; prune when the four claims are folded into a standing check |
| `.dreamwork/docs/containment-deficiency.md` | #450 boundary statement for the containment the loop does not have: a per-harness capability table over what it actually dispatches (`ccc @grok`/`ccc @glm52` — tool calls not interceptable today, whole-harness containable but self-defeating), the trusted-nodes precondition stated where dispatch acts on it, the three seams that keep later isolation possible (the prototype, the positive PID/health invariants, the dispatch point), and the warning copy for a harness row. Implements the 2026-07-29 ruling on `#288`; the design + prototype live in `plans/subagent-containment.md` | Keep while the wall stays prototyped-not-wired; revise when a lane wires the wall, when a runner's interception seam gets investigated, or when the trusted-nodes precondition changes |
| `.dreamwork/docs/draft-durability-status.md` | #269 empirical status: what draft durability actually guarantees (process restart, write trigger/debounce, per-box coverage, clear-on-success, localStorage vs IndexedDB) with cited lines and Playwright observations — not the design (`plans/draft-durability-design`) | Keep while #269's remaining scope is open; fold or prune when the ledger entry closes |
| `dreamhub-design.md` | dreamhub standing design: the stage-1 boundary and its checks, the origin-per-project deviation, and the exact status.json / watch-port / `/mtime` / `/data.json` fields the hub depends on (guards: `dev/hub/README.md`) | Yes — shipped beside the tool it documents |
| `roll.py` / `watch.py` / `heartbeat.py` / `dreamhub.py` docstrings | Tool contracts (advisory dice; dashboard; the wake tick; the multi-project hub) | Yes — contracts live in the docstrings |
| `test_watch.py` / `test_roll.py` / `test_heartbeat.py` / `test_lint.py` / `test_relay.py` / `test_deployed.py` | The Python half of verification — asserts on generated source, cannot see what renders | Yes — a behaviour change ships with its test |
| `dev/capture/` | The structural half: browser guards (exit non-zero, gated by `just test`) plus print-only capture scripts | Yes — a guard joins `GUARDS` when its feature lands |
| `dev/capture/fixture/` | Frozen miniature target the guards run against, so a red light means the code broke | Yes — extend it when the parser learns a new input shape |
| `justfile` | Common tasks: test (both halves), pytest, guards, watch, audit-styleguide | Yes — a new routine worth repeating becomes a recipe |
| `DREAMWORK.md` (repo root) | This target's own goals, philosophy, preferences, plugin decisions | Yes — folded whenever the human reveals a durable preference |
| `../ud-dreamtask/SKILL.md` (sibling repo) | The bounded one-errand loop; inherits this skill's Guardrails, Subagents and Durable-state sections **by reference** | Yes — a change to those sections changes dreamtask's behaviour too, and a rename orphans a live pointer |

**Routing rule — a finding lands where its trigger lives.** Behaviour
that must fire unprompted goes in SKILL.md; the shape of a file goes in
`file-formats.md`; a procedure with a nameable trigger (init,
compaction, plugins) goes in that trigger's reference file; and **craft
— how to write a good ENTRY in something — goes in that file's own
header**, which is the best trigger available because the writer is
already looking at it (#145). `lessons.md` and `tasks.md` both work this
way already. SKILL.md is
re-read on every reload and fires on nothing in particular, so it is the
default destination only for things with no better trigger — it grew 246
to 433 lines in one day by being everyone's default (#120, #145).

SKILL.md is the entry point for the harness; README.md is the entry
point for a human browsing the forge. The README stays minimal on
purpose — what this is, how to install it, where to look — and every
fact in it has its home in a doc above.
