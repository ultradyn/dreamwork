# Hand-offs — work landed outside the ledger, waiting to be folded

A session that lands work it does **not** own `.dreamwork/tasks.md` for —
every session but the coordinator — appends ONE line under `## Pending`.
The coordinator reads it on every tick, folds each landing into the ledger,
and appends one `→ folded` line under `## Folded`. **Nothing ever moves**:
both sections grow only by append, so two sessions landing work at once
cannot lose each other's line — the property the dreamer inbox has and a
rewrite would not (#381).

**Section order (#406):** `## Folded` first, then `## Pending`, so an EOF
append lands under Pending and the instruction is true without a rewrite.

**Provenance notice (#469, 2026-07-29). `by grok` in a row below names the
HARNESS, not the model.** `ccc`'s `runner` field is the CLI harness: both
`@grok` (grok-4.5) and `@glm52` (glm-5.2) run the grok CLI, and the harness
exports only `CCC_PROVIDER` to the child — never the alias, never the model.
So **a lane cannot know what model it is**, and every self-reported model
name is a guess the harness's system prompt supplied. Two `@glm52` lanes
introduced themselves as *"grok-4.5 (xAI)"*.

Rows written before this notice therefore mean *the grok harness, model
unknown*, and they are deliberately **not** being back-filled: the dispatch
alias is not recoverable after the fact and a model attribution is history,
which is never guessed here. From now on **the dispatcher records the alias
it passed** — it owns that argument — and the model is derived from
`~/.config/ccc/config.toml`. Write `by ccc @glm52 (glm-5.2)`, never a name a
lane reported about itself.

One line per landing, mirroring the inbox's one-line-per-report shape.
Required: the task id (plain `#N`, sub-id `#Na`, or combined `#N/#M`), the
**sha** (what landed), who is claiming it, and a one-line `what`. The
`→ folded` line is how the writer marks one consumed, and it is itself an
append — never a deletion — so a folded hand-off is not flagged twice.

## Folded
- **#527** → folded (2026-07-30 08:24): merged `2c251c8` (lane `d48c68b1`, lane-527recon, glm-5.2) — the #519 F1 join, first half: every `/command` wake-line now carries its receipt id as a suffix (`command via watch: do-now: X [receipt <id>]`), drawn from the pre-existing `journal_result()` seam (E3 commits before dispatch, so the id is in hand at emit time; absent cleanly when the journal is off). Prefix undisturbed for existing tail-monitor parsing. 11 TestDeliveryWakeRouting tests pass, incl. the id-in-line == id-in-journal equality assertion. Coordinator independent reds ×2 on the merged tree: suffix stripped → exactly the reconciliation test FAILED; `journal_result` → `None` (a pre-existing line the lane did NOT inject) → same test FAILED. Both cp-restored byte-identical. A do-now that both wakes and drains is now reconcilable BY ID; the drain-side proof wiring is #526 (in flight).
- **#519** → folded (2026-07-30 08:03): merged `2965ade` (lane `f4c47f09`, lane-519audit, glm-5.2) — the do-now exactly-once audit, and it found the join missing: **F4 (root, HIGH)** `apply.prove_applied`/`reconcile`/the adapter registry are exercised ONLY by tests — no production module imports `apply`; `journal_consume consume` is two acts (read, advance) where delivery-modes.md:162-180 describes three (read, verify+replay, advance). **F1 (HIGH)** a batched do-now pre-empts by wake-line AND drains on the tick — `command_line` carries no receipt id, nothing reconciles the channels, the double-act rests on the coordinator noticing prose. **F2 (MEDIUM)** instant mode (the DEFAULT) journals every `/command` but SKILL.md gates the drain on batched — `(cursor, head]` grows forever; an instant→batched flip mass-replays. **F3** SKILL.md's pre-E3 "exists only as a line in that file" — the enabler. Review-gated: full doc read; three load-bearing claims spot-checked exact against master (apply imported by the two test files only; `command_line` has no receipt id; SKILL.md:118). Follow-ups filed as **#526 (wire proof or restate, P2) · #527 (reconcile wake vs cursor, P1) · #528 (drain every tick, P2) · #529 (stale SKILL.md paragraph, P3)** — the audit proposed #520–#523, those ids were taken; #529 fixed inline by the coordinator in the same tick.
- **#507** → folded (2026-07-30 07:58): merged `f663749` (lane `47346714` `77db165c`, lane-507flake, glm-5.2) — the "flake" was two different defects wearing one name, characterised before any fix (guard × load × runs table): **markrail was DETERMINISTIC** — `c882a9da`'s `.markstrip` renders every `[data-mid]` a second time, a bare selector hit the hidden 0×0 copy or strict-mode-violated, 7/7 across load 28–66; **burndown/bdhover were readiness races** — pre-#461 fixed-sleep idiom (`spawn`→`sleep(2500)`) and `networkidle`+fixed-sleep before the DOM exists, reddening as "threw before finishing" over pages never read. Fix entirely at the harness layer: `serveVerified` + a new shared `waitFor` in dom.mjs, selectors scoped to `.marktab[data-mid]`; **zero assertion values or thresholds loosened**. Lane red-proofs incl. content sabotages reaching 47-53 checks (born-hollow ruled out), N≥5 under-load PASS ×3. Coordinator gate on the merged tree: all three guards PASS solo; independent red at `bdinspLay`'s maxWidth pin (line the lane did NOT inject) → exactly `FAIL #298/#487: at 390px the inspector stays inside the panel`; cp-restored byte-identical.
- **#515** → folded (2026-07-30 07:56): merged `8908b96` (lane `5fdc1c6e`, lane-515decide, glm-5.2) — the #514 F1 VIOLATION closed: `/decide` emits its wake line only inside `emits_wake("/decide", target)`, mirroring the `/comment` idiom; the receipt commit and store write are untouched — withholding the wake IS batching, the cursor carries the decision to the tick. Under `delivery: batched` a review decision no longer interrupts the loop. 8 TestDeliveryWakeRouting tests pass (batched withholds + receipt lands; instant + absent-axis wake). Coordinator independent red at the guard itself (`→ if True:`) → exactly `test_decide_withholds_wake_in_batched` FAILED; cp-restored byte-identical.
- **#517/#518** → folded (2026-07-30 07:52): merged `0ac902bf` (lane `1f105aec`, lane-517doc, glm-5.2) — delivery-modes.md caught up to the landed #342b code. #518: the stale 4-route enumeration replaced by a 9-row `WRITE_ROUTE_HANDLERS` table, each handler verified while writing (GATED ×4 COMPLIANT; `/decide` NOT GATED with #515 named as in flight — no future asserted as present; `/tint`+`/deploy` non-waking by design with the reasons). #517: new "Wake channels outside the route table" section — control-plane wakes (`/run-mode`, `/posture` triple + delivery axis) as the explicit always-instant carve-out (a delivery toggle that waits for the tick defeats the toggle), journal failure-path wakes as always-loud error diagnostics, the migrate one-shot noted, `question-updated` deferred to #516 in flight. Review-gated by the coordinator: full diff read; six line citations spot-checked against master — all exact (`WRITE_ROUTE_HANDLERS`:14541, unguarded `/decide` log_event:14334, four handler defs). Doc lane: no test red-proof applies.
- **#512** → folded (2026-07-30 07:36): merged `a0bf2fc` (lane `d9f555cc`, lane-512show, glm-5.2) — `journal_consume show <receipt-id>`: read-only full-payload read via the single public `Journal.get_receipt` (cursor-independent, so already-consumed receipts read back — consume moves only the cursor). Header + verbatim UTF-8 payload (the deliberate #126-collapse exception: this output is for READING a multi-line instruction, not a line-log). Unknown id / absent journal → one-line stderr + EX_USAGE, no db created (#501 discipline). 10 passed; coordinator independent red at `if receipt is None:` → `if False:` → exactly `test_show_unknown_receipt_exits_usage` FAILED; cp-restored byte-identical. Live dogfood in the same tick: `show 36d7e68d-c630-…` printed the blindly-consumed ord=39 human instruction in full — the incident that filed #512 is now a one-command recovery.
- **#508** → folded (2026-07-30 07:32): merged `5ad3dc0` (lane `2d75d111` `db9ef405` `d45382e0` `b8d503ea`, lane-508deploy, glm-5.2) — `just deploy` success is now IDENTITY, not liveness: `verify_deployed` returns 0 only when `listening_pid(port) == expect_pid` AND its argv runs the snap (the pid is load-bearing because autoreload's in-place re-exec keeps the old argv identical to the new snap); `wait_port_free` requires the port to STAY free (settle defeats the close-on-exec execv flicker that `stop_deployed`'s 50ms poll could grade as free); the recipe drops `--dev` from the deployed start (autoreload was the re-exec enabler and serves nothing on a frozen snapshot the recipe restarts; GENERATION still bumps, tabs still reload). Mechanism confirmed in a fixture before any fix. 9 new tests, each red-proved by the lane. Coordinator independent reds on the MERGED tree at lines the lane did not inject: `if holder != expect_pid:` → `if False:` → exactly `test_verify_deployed_refuses_foreign_holder` FAILED; settle-check disabled → exactly `test_wait_port_free_settle_rejects_the_execv_flicker` FAILED; both cp-restored byte-identical. Gate found one real suite failure — my own diff3-conflict resolver left `|||||||` base markers + stale duplicated lines in handoffs.md (the resolver predated diff3 style; lesson material) — cleaned, grammar fixed, silence test green.
- **#514** → folded (2026-07-30 07:27): merged `df5e764` (lane `9ad47c76`, lane-514wake, glm-5.2) — wake-semantics audit under `delivery: batched`, static read-only, all 13 `log_event` sites + 1 raw append traced. Verdicts: 4 COMPLIANT (`/command` by kind, `/answer`, `/ask`, `/comment`), **2 VIOLATION** — F1 `/decide` wakes unconditionally (journals a receipt but the #342 gate missed it; silently undoes batched mode for review decisions) and F2 `question-updated` ungated + unjournaled (the batched-class question signal interrupts the loop; possibly #509-adjacent) — 7 UNCLEAR (control-plane, journal failure-path, migrate one-shot). Review-gated by the coordinator: full 222-line doc read, claims spot-checked against watch.py (`emits_wake`:13418, `PREEMPT_KINDS`:13406, unguarded `_handle_decide` log_event, `question-updated`:12917 — line drift +15, claims accurate). Follow-ups filed #515–#519 (F1→#515 P1, F2→#516 P2, doc gaps→#517/#518 P3, do-now exactly-once→#519 P2). Audit lane: no test red-proof applies.
- **#505** → folded (2026-07-30 07:06): design, merged `5eab68f4` (lane `e55791eb` `87092028`, lane-505arch, glm-5.2) — `render-architecture.md`: the wholesale-rerender smell designed against a MEASURED 16-surface reset inventory (1 is his bug — prose selection inside `#view`; 11 re-applied by hand post-swap; the chrome survives because `renderChrome` is already a keyed diff). Real IGC (I1–I5 × G1–G5): his three directions refuted as standalone fixes (ids alone don't survive the swap; content-check is an accelerator not a fix; React/preact = build step + second render authority against his own single-file architecture, pushed back honestly as invited). Survivor: keyed reconciliation of `#view` (morphdom-idiom) + content-hash skip — generalises `renderChrome`, subsumes #141/#503/#494 (`data-keep` retained as a reconciliation key), every transitions.md family's fate named. Review-gated by the coordinator (full doc read, IGC structure verified, claims spot-checked against watch.py: `setContent`'s `innerHTML` swap, `renderChrome` keyed reuse + content gate, `data-qid`/`data-keep` identity attrs, `.qaghost`, watch-design.md:2741 single-file; line drift +6/+13 from later merges, claims accurate). Q1–Q4 surfaced in questions.md with declared sub-decisions (recs: vendored morphdom; hold no-build; `#view` first; guard the corpse rule). Doc lane: no test red-proof applies. **`#505` stays open** — implementation follows his ruling.
- **#357** → folded (2026-07-30 07:02): implementation, merged `33b754ae` (lane `c952fe05` `7eac5e2d`, lane-357impl, glm-5.2) — `dev/ledger.py`'s `emit_warnings()`: the warning footer on every verb's success path, stderr-only (stdout stays pipeable), stateless, exit-transparent, seven counts each from its PRODUCTION reader (`store_ids_by_state`, `lint.check_unfolded_answers`, `watch.parse_open_answers/parse_open_questions`, `open_journal` head−cursor) — never a second copy. Q5/Q6 rulings implemented as settled (every verb, full line, no throttle). **Green red-run at the gate — a finding, acted on**: the `if rc == 0:` gate was bound by none of the lane's 7 red-proofs (sabotage to `if True:` passed all 7); coordinator added `test_footer_absent_on_a_failing_verb` (`64d8a3e7`), red-proved by the gate's own injection (FAIL line 188), cp-restore → 8 passed, full suite 1526. First live dogfood: the store `fold` for this very task printed `warnings: 127 open tasks · 4 unanswered questions · 234 untyped · 107 missing origin` — the counts are real and immediately informative. **`#357` fully landed** (design + impl); the untyped/missing-origin hygiene counts it surfaced are follow-up material, not this task.
- **#511** → folded (2026-07-30 06:53): merged `b498e690` (lane `99e2d9c0`, lane-511live, glm-5.2) — master red cleared. Option (a) re-point of the four live migrate acceptance tests at `tasks.md.deprecated` (the frozen post-cutover ledger); the history test splits text (frozen) from git walk (the `tasks.md` path's 678 commits) and its verify moved to `verify_db` with the rich snaps because the CLI couples text+walk through one `--ledger` path the cutover split. Preconditions asserted at runtime (`## Open` present; `len(snaps) > 1`). Coordinator independent red at a tool line the lane did NOT inject (`ud-dw-tasks-migrate`'s groomed-conflict `_flag`): **first sabotage GREEN — a finding, acted on**: `False and X or Y` precedence left landed-section ids flagged, the injection never reached the branch; corrected to a full disable → FAIL at line 419 (`assert groomed`); cp-restore → 64 passed. Full suite 1518 passed. Collateral: the coordinator's own #498/#499 fold lines violated the handoffs by-claimer grammar ("folded by coordinator"), lint's live-silence test caught it — fixed `99761f4a`.
- **#506** → folded (2026-07-30 06:12): merged `76fd99c4` (lane `984fa9b1` `29fc4cce` `dd208e4d`, lane-506pip, grok-4.5) — his 03:54 do-next. `linkify` emits `pipBtn` immediately after every known-internal file link it promotes — the same `linkable_paths` closed set decides link AND pip (no second list); pip outside the backticks (chrome, not copy) inside a `.mdfile` nowrap unit so the glyph never orphans; `user-select:none` so a drag across prose never swallows the button (#505); reduced-motion drops only the colour transition; external github.com links get a link and no pip (a pip floats a local view). Guard `qlinkpip.mjs` registered (68) with both link kinds DERIVED from live data.json at runtime (known `.dreamwork/lessons.md` + unknown `nosuch/vanished.md` planted in one fixture question body — a green without the pair is vacuous). Coordinator independent red at a line the lane did NOT inject (lane injected the pipBtn call): **`user-select:none` removed from `.pipbtn`** → exactly the user-select check FAILed, restored byte-identical → qlinkpip PASS. Visual verdict by the coordinator (desktop rest/hover, mobile rest): pip quiet at rest, accent on hover, unit holds at 390px, unknown stays code. 372 passed. **`#506` stays open** only in that #505 (the general render-architecture smell) is the proper-home follow-up.
- **#510** → folded (2026-07-30 06:10): design, merged `7ebf431f` (lane `9e1491e5`, lane-510orc, glm-5.2) — his 04:54 do-next. `orchestrator-posture.md`: the referent investigated (his "opus 5 claude orchestrator" usage at questions.md:1119) — designed against orchestration MODE (coordinator implements nothing, only dispatches+reviews; witnessed orthogonal to `delegation` — coordinator-only mode held constant while fleet size moved 2→5 in dogfood-orchestration.md); identity escalated as Q1 with a rec against (provenance owned at dispatch per ccc-runner-routing.md, not a posture dial). Real IGC I1–I5 × G1–G5: **a fifth posture axis is the sole survivor** — fold-onto-delegation refuted (a number can't encode a mode, the #443 defect), `hierarchical` run-mode value refuted (re-bundles #443 + preempts), control-only refuted (no restart-survival, #426), sibling file refuted (the #445/#342 widen-not-sibling standing ruling). Q1–Q3 surfaced to him in questions.md with declared sub-decisions (recs: mode; binary `hands-on`|`orchestrator` absent=hands-on; land the axis, don't enable `hierarchical`). Doc lane: review-gated (full doc read, IGC structure verified, factual claims spot-checked against lint.py/watch.py); no test red-proof applies. **Dispatch incident recorded in lessons.md**: this lane and lane-511live went out without `isolation="worktree"` (coordinator error) and ran git in the main checkout — topology repaired (lane commit preserved on lane-510orc, coordinator commit rebased home, stray branch deleted); the lane's own recovery was graceful. **`#510` stays open** — impl split (subagent webui, coordinator docs) follows his ruling.
- **#501** → folded (2026-07-30 04:58): merged `fa61998f` (lane `41eec321`, lane-501consume, glm-5.2) — `dev/journal_consume.py pending|consume`, the batched-delivery drain half of #342. Thin composition over the landed `events_since_cursor` projection and the `advance_cursor` CAS — no `user_events/` change, no new journal query; the atomicity seam (read and advance are two calls) is named in the module docstring and proved safe-by-construction (append-only chain; only an alteration refuses, which no public API can induce — the test reaches it by direct SQL corruption). Five red-first tests seeded via the production `receive()` path with derived preconditions. Coordinator independent red at a line the lane did NOT inject (lane injected: empty-path write, pending-loop skip, advance-in-pending, advance-removed, refusal-swallowed): **`expected=head.event_hash` corrupted to a wrong hash** → exactly `test_consume_advances_then_pending_empty` FAILED (exit assertion, line 277), restored byte-identical → 5 passed. Live first use the same hour: he set `delivery: batched` at 04:52 and the coordinator's first `consume` drained 37 historical events to set the baseline; `pending` now reads empty. Full suite post-merge: 2 pre-existing reds in `test_tasks_migrate_verify.py` (stale against the #294 shim — filed `#511`, verified identical at pre-merge `76367115`), nothing from this lane.
- **#504** → folded (2026-07-30 04:50): design, merged `12fd763c` (lane `08b031dc`, lane-504chat, glm-5.2) — his 03:39 add-idea. `composer-chat.md` reconciles the third chat ask against the four approved directions that bind it (#229/#270 v2, #253, #263, #342); a real IGC (A/B/C × G1–G5) decides his `chat` command **is** the #229 main-dreamer first slice — no parallel store, no second inbox, reply via the CLI-only seam. His "get unread at loop start" is, by name, the #342 cursor read (lane-501consume's `journal_consume pending`). Pushback kept: "just in case" understates the receipt, which under #342 is the delivery path. Four forks surfaced to him as a questions.md entry with declared sub-decisions Q1–Q4 (recs: `/command` chat kind; "topic chat" UI word; batched delivery; minimal dashboard chat list). Doc lane: review-gated (full diff read, IGC structure verified, claims spot-checked against #229/#342); no test red-proof applies. **`#504` stays open** — implementation is #373's, gated on his Q1–Q4.
- **#503** → folded (2026-07-30 04:28): merged `6584c7e7` (lane `29c673f7`, lane-restcollapse, grok-4.5) — his 03:30 do-next. Root cause confirmed by repro: `expand()` emitted no `data-keep`, so `snapshotFolds`/`restoreFolds` (#141) never saw the peeks and the ~2s poll re-render detached the open node (his 03:39 data.json suspicion was right). Fix reuses the #141 idiom with stable keys (`status-rest`, `file:*`, `dream:*`, `dreams-archive`); restore is silent; peeks also now travel on his gesture via `foldDetailsLocal` (the brief's own sanction; `transitions.md` + `watch-design.md` updated same commit). Guard `restcollapse.mjs` registered (67). Coordinator independent red at the `restoreFolds` CALL SITE (the lane injected at the emit site): guard FAILed all four assertions, restored byte-identical → PASS. **The merge surfaced a stale full-call literal pin** in `test_page_reflows_prose_but_not_raw_text` (broke on the new keep args while asserting the unchanged thing); coordinator repinned to the `mdB(d.files[n])` prefix and red-proved the repin (renderer sabotage → FAIL; restore → 372 passed). Full suite: 372 passed; guard suite's burndown+markrail reds triaged to chronic load (identical at pre-merge, at deployed `d62265d9`, and at merged master) and filed as `#507`.
- **#496** → folded (2026-07-30 03:55): merged `d0dddc62` (lane `c691f34`, lane-496routes, glm-5.2) — E2Shadow extended to `/decide` (#289, the genuinely unshadowed route; the brief guessed `/deploy`, already exercised — the lane halved the brief's assumption and was right to). Literal alarm 8→9 kept literal with the born-hollow warning comment; `REVIEW_DECISIONS` closed set asserted at runtime; stale "six routes" docstring fixed. Lane red-proofs both directions (literal→8: `9 != 8`; `/decide` removed from the table: 404 in the status list). Coordinator independent red: `_journal_receive` neutered to `return None` → `E2Shadow.test_every_write_route_commits_a_receipt_and_changes_nothing_else` FAILED (all 503s), restored byte-identical → 15 passed. Folded to landed in the store.
- **#357** → folded (2026-07-30 03:45): design increment, merged `b32ea47b` (lane `ecac44a0`, lane-357throttle, glm-5.2) — the Q5 ruling recorded (footer on EVERY verb, ruled 03:11) and his throttle sketch evaluated with a genuine IGC: **refuted** on statelessness (needs memory a stateless verb process cannot hold), never-suppress-unseen (drift state hides a warning forever), and surface-early (a throttle delays the thing he asked to hasten — the sketch is internally contradictory, the headline he asked to have surfaced). The surviving fork (full line vs terse hint on read verbs) escalated as **Q6, now in questions.md** with rec full-line. Doc-only lane: review-gated (full diff read, IGC structure verified, factual claims spot-checked); no test red-proof applies. **`#357` stays open** — implementation behind Q6.
- **#500** → folded (2026-07-30 03:25): first slice, merged (lane-500bridge, worktree) by glm-5.2 — the bridge plugin scaffold (`plugins/ud-dreamwork-matt-pocock-skills/`: SKILL.md, `tracker_adapter.py`, T1–T5 seam checks); the adapter maps tracker ops onto `dev/ledger.py` verbs (create→`file`, close→`fold --note`, list_open→`counts`, injectable runner) and grill questions onto the production `watch.human_block` seam (new `_insert_first_open_entry` helper — no production "new Open entry" helper existed; flagged as a seam the core may want). Authority floor only: no activation, no lifecycle hooks. **Coordinator red-proof was a GREEN RED-RUN and the repo rule fired**: injecting `fold`→`note` in `close()` passed 11/11 — T1–T5 never asserted WHICH verb close/list_open invoke. Finding acted on coordinator-side: T1b argv-mapping checks added (`e5c5cf51`), red-proved against the identical injection (exactly the new test failed, showing the real argv), restored byte-identical, 13/13 green. The lane also never appended its Pending line here — recorded by the coordinator instead. **`#500` stays open** — activation, lifecycle hooks, grill-skill wiring remain.
- **#342** → folded (2026-07-30 03:05): lane B, merged (lane `b1cb91a6` `62eb4177` `58afee93` `d6b7a2a9` `0eff7602`) by glm52 (lane-342b, worktree) — the delivery posture axis (instant|batched, absent=instant; lint closed-set + file-formats in the same commit), per-kind wake routing (`emits_wake`: receipt unconditional, wake conditional; do-now/do-next pre-empt even batched), and the picker chip on the shared 10s arm (no second gesture). Lane red-proofs per surface incl. catching its OWN hollow POST-body red-run twice; coordinator red-proof: `/comment` gate removed → exactly `test_answer_ask_comment_withhold_wake_in_batched` FAILED, restored byte-identical. Guard gate: summaryjson caught the new axis (whitelist fix `d62265d`) + a pre-existing `research` mirror drift (#484). Deployed `d62265d9` on :35110. **`#342` stays open** — the tick-consume habit is `#501`.
- **#495** → folded (2026-07-30 02:10): merged `08e8d790` (lane `6aee7d9`); the lane's repoint at the frozen `tasks.md.deprecated` plus store materialization via the REAL `perform_cutover` verified by my independent red — `_metadata_clause`'s body-break removed in `lint.py` failed exactly `test_the_four_false_positives_stay_silent`, restored byte-identical; 373/373, lint clean. Model corrected per the dispatch record (glm52, not the lane's self-report — `ccc-runner-routing.md`: a lane cannot know its own model).
- **#413** → folded (2026-07-29 03:41): merged `ac6bcf3` (`4966d9c`); 6 fakes inventoried, 0 unverifiable, and its refutation of my brief kept — the motivating fake's 409 was never stale, the blindness was its SCOPE, so a stale-literal checker would have passed over the instance that motivated the task. Two lanes widened `health.mjs` for the same fact; resolved as a union. Verified: 20 PASS, and reinstating 409-only gives exit 1 on the coverage and message checks while the two state assertions stay green — the empirical case for the coverage assertion being the load-bearing part.
- **#263f** → folded (2026-07-29 03:36): merged `8ccd2fb` (`E4` `0024ad2`, `E5` `a67f308`, `E5b` `a328507`) into `#263`'s entry. Merge deliberately held 35 minutes until the client half existed. Verified in an isolated worktree: reverting `writeVerdict`'s `landed` to `res.ok` fails 10 of 12 `rejectwrite` checks including the permanent-loss vector, success-side check still passing; 12 PASS on the merged tree. Guard count 56 after a union resolution. `E6` is the remainder.
- **#461** → folded (2026-07-29 03:21): the corrected scope is three guards — `health`, `pushhealth`, `fileimg` — all converted, nothing left to roll out. Batch 2 rejected (`1197d41`, branch discarded): its red-proof aimed at an `argv[3]` pin the conversion itself added, and that pin would have reddened eight guards under `just guards`, which hands every guard the port the shared server already holds. Measured: converted `morph` exits 1, master's exits 0.
- **#461r** → folded (2026-07-29 03:03): merged `8e7ea50`; 8 of 18 own-server guards adopted, remaining eight named as a list rather than a count. **`#461` stays open.** My independent squatter proof came back GREEN first — I squatted 39782 while `gitrow` serves on 39781, because its port arithmetic is direct rather than `++port`; on the right socket it exits 1 and names the wrong target.
- **#462a** → folded (2026-07-29 03:03): merged `b1551b1`; the copyable remedy on the staleness row, guard `staleremedy` registered (55) and 11 checks verified by me on the merged tree, my own red deleting `revealStaleAction()` failing exactly the two motion checks. **`#462` stays open** on his consent call (asked 02:56). The lane withdrew its first refutation after pushback and found the real one: a deployed server's `__file__` is the snapshot, so re-exec is byte-identical.
- **#458** → folded (2026-07-29 02:42): merged `327345b` into `## Recently landed`; verified independently by replacing `strip_notice`'s `_BLOCK_RE.sub` with `return text` (4 tests red, incl. the shrink proof) and by making `NOTICE_OPEN` a visible ledger head so both `TestIndifference` tests red on real `lint.LEDGER_ID` / `watch.parse_ledger` id sets. Restored byte-identical, 15 pass. `#294` unblocked on this channel.
- **#445** → folded (2026-07-29 02:42): merged `1462aeb`; **`#445` deliberately STAYS OPEN** — a design is not a ruling, and it is now asked with `Q1`/`Q2`/`Q3` declared. doc-map conflict resolved as a union (30 == 30 both directions). Artifact verified by me: one real `#ask`, both metas, no stray `>`, fixed table layout.
- **#263e** → folded (2026-07-29 02:27): merged `df2989e` (`E1` `69b8573`, `E2` `d460947`, `E3` `38ef409`) into `#263`'s entry; verified end-to-end by the coordinator against a server whose pid was asserted from `ss -ltnp` — `POST /command` → `202`, `Location: /user-events/<id>`, receipt id present in `receipts`/`transitions`/`events`. Two earlier probes reporting `200` were reaching a stale lane server, not this code; lesson recorded. Successor: `E4`/`E5`/`E6`, then lanes G and H.
- **#420** → folded (2026-07-28 15:55): merged `61354fb` into `## Recently landed`; its ten stale blockers re-verified with `parse_ledger` and nine cleared in `30458af` (`#172` was the tenth and closed on its own). Its `#419`-reverse-direction finding is folded into `#419`'s entry as that check's red fixtures.
- **#172** → folded (2026-07-28 15:50): merged `775022d` into `## Recently landed`; his edge-pin rule verified by three route rectangles rather than a screenshot (identical box on `/` and `/questions`, trailGap 0 on a long `/review` where the bar widens), guard `projtitle` registered and red-proved on six named assertions. Refuted my brief's claim that the tab title lacked the project name — `#153` had already put it there, which is why he believed the work was done.
- **#402a** → folded (2026-07-28 15:45): merged `f1f269b` into `#402`'s entry; `gate402a.py` PASSED against the merged tree with the pre-merge baseline, and the fix verified on REAL data — the first post-merge run reported `current_task_ids [] -> ['172', '420']`, found both live lanes and pruned the dead `402a` one. Its refutation of my gate's stale-pid premise is recorded in the entry; `#402` stays open for the `file-formats`/`lint` vocabulary (`#402b`, which collided within a minute) and for `awaiting_human`, still hand-written.
- **#367** → folded (2026-07-28 15:45): merged `98670ae`; **`#367` deliberately STAYS OPEN** — this landed the option previews he asked to see, not the feature, and he has since ruled (C with a collapsible chevron index, collapsed by default). The hand-off line says *landed* because the grammar has no word for *increment 2 of n landed*, which is `#415`; that gap is what made `test_lint.py::TestHandoffs` red on master until this line existed.
- **#411** → folded (2026-07-28 14:28): merged `1f01a95` into `## Recently landed`; gate re-run on the merged tree against an explicit pre-merge baseline — 2 recovered, 44 dated unchanged, None 5→3, bait probe still inert
- **#331** → folded (2026-07-28 13:18): merged `cb476a7` into `## Recently landed`; gate re-run on the merged tree with an explicit pre-merge baseline — 19 ids recovered, landed 152→171, span core 4 copies → 1, all three heads pinned including `status_sync`
- **#399** → folded (2026-07-28 12:24): merged `0595b13` into `## Recently landed`; burndown green on the merged tree, and the 3 remaining `just test` failures verified pre-existing on master; residual space-separated multi-id gap filed as #412
- **#398** → folded (2026-07-28 09:31): folded into `## Recently landed` citing `9f2012a`; verification owed
- **#397** → folded (2026-07-28 09:52): folded into `## Recently landed` citing `1b508b0`; recommendation (do-not-extract) accepted, no ruling requested, worktree alternative filed as #405
- **#401** → folded (2026-07-28 09:58): audit half folded into the `#401` entry citing `f2c950e`; fix half stays open, still needs `watch.py`
- **#392a** → folded (2026-07-28 10:00): verified and closed against `#392`'s entry citing `159917b` — independent red taken in a worktree, deployed page checked (38/38 day-precision, zero two-figure). Misfiled Pending-shaped line was the `#406` fixture; relocated under Pending after the check went red-loud.

- **#263g** → folded (2026-07-29 01:52): the artifact increment is consumed; **`#263` itself stays open by design** — its second gate opened at 01:37 and lanes **E**, **G**, **H** are now authorised, with lane E (increments 20-22) in flight. The gate artifact is recorded in `#263`'s entry as the decision-taken record it became, not as a closing commit.
- **#269v** → folded (2026-07-29 01:52): the verification is consumed into `#269`'s entry as the 01:43 correction — acute draft durability is **(b) partly shipped**, restart-verified, `localStorage`, no debounce. **`#269` stays open by design**: the authorised remainder is the IndexedDB upgrade, cross-tab `R1` and 30-day GC, with `#459` (`#askbox`, popout `#ptext`) sequenced ahead of it.
- **#523** → folded (2026-07-30 08:45): merged `fab454bc` + guard/lesson `1d0e7c17` — #view input focus/value/caret snapshot-restore across the tick. Gate: bdinput 31/0 on master, visual verdict PASS (desktop+390). Two single-line sabotages went GREEN and were traced, not accepted: the contract is defended twice (write clamp `applyBurnLimit` watch.py:7812 + pre-existing read clamp `displayBurnLimitValue` :7777); the honest red is the disjunction — both clamps out → `(c2)` FAILs exactly where predicted. New defense-in-depth triage lesson recorded in lessons.md. The `(d)` hold-repeat timing flake surfaced during re-gate (1 in 3 runs) — filed separately, harness-layer.
- **#524** → folded (2026-07-30 08:45): merged `fab454bc` — burndown limit `−`/`+` steppers with module-level hold-to-repeat (~400ms then ~80ms, composes across re-renders) and `⟳` reset; same gate as #523.
- **#521** → folded (2026-07-30 08:53): merged `f0d9458` + readiness fix `635a29cb` (merge `299138d6`) — blockquotes render as `.mdquote`, fences win. Re-gate after the readiness repair: 3× 29/0 back-to-back solo with target resets on one shared server, then PASS via the real justfile harness under load 34. Two gate-side findings recorded: the guard derives its plant dir as `OUT/../target`, so a solo run must replicate the justfile's `$OUT/$g` layout or readiness fails for harness reasons, not feature reasons.
- **#522** → folded (2026-07-30 08:53): same merge and gate as #521 — `linkifyMd` consumes `[text](target)` whole (known-internal/relative→closed-set → `.mdfile`+pip; http(s) → external no pip; else fully literal).
- **#525** → folded (2026-07-30 09:05): merged `423db6ff` (merge `b1a58854`) — GFM pipe tables in `/file` rendered markdown. Gate: mdtable+mdquote PASS via the justfile harness; independent red on the ragged-count hinge (`watch.py:2620`, a line the lane did NOT inject) FAILed exactly the two named checks; restored byte-identical. Visual verdict PASS desktop+390, with one evidence-hygiene finding: the two screenshots were captured against different fixture iterations (desktop shows the ragged example as prose, mobile shows the by-design body-pad table case) — the guard is the load-bearing evidence and it holds; lesson recorded.
- **#510** → folded (2026-07-30 09:12): merged `52d76875` (merge `65efd9b`) + coordinator SKILL.md docs half — fifth posture axis `orchestration: hands-on|orchestrator`, absent→hands-on, his rec×3 ruling (mode not identity; binary; land the axis, `hierarchical` stays disabled). Gate: lane's 14 reds (incl. its own honestly-reported green red-run on R11, acted on) + coordinator independent red on the `write_posture` validation guard — a line outside the lane's 14 — FAILed exactly `test_write_posture_orchestration_is_optional_and_validated`; restored byte-identical. 81 targeted tests green, lint posture OK, posture.mjs PASS, visual verdict PASS (fifth chip row, correct default selected). The conduct half is SKILL.md's restate: under `orchestrator` any coordinator-implemented increment is drift — dispatch it or file it.
- **#520** → folded (2026-07-30 09:20): merged `5854bb91`+`a7188578` (merge `2ee8b22`) — deploy stops the old server BEFORE shipping the snapshot; the recipe order is stop → wait-port-free → mv → start → verify. Gate: 39 deploy-state tests green; lane's RP1–RP3 reviewed. Coordinator independent red was a double finding: the order test pinned only `stop < ship` (mv-before-wait genuinely unbound) AND the green runs that revealed it were themselves scaffold-hollow (`-k "order"` selected the standin test, deselecting the text-anchor test). Anchor strengthened to `i_stop < i_wait < i_ship` and red-proved against the same injection (`assert 1113 < 1062`); `-k`-scaffold lesson recorded.
- **#526** → folded (2026-07-30 09:27): merged `86ac94b` (merge `a0fc42e`) — the #519-F4 exactly-once proof is wired into the cursor drain: `consume` routes every drained receipt through `apply.reconcile` against `.dreamwork/applied.md` before advancing (read → prove → advance; the cursor contract unchanged), and the UNAPPLIED list is the coordinator's act-on surface. F4 closed — a production module imports `apply` for the first time. Gate: lane's 4 reds reviewed (all discriminating, none green); coordinator probe of a line outside the lane's set — the unregistered-route `KeyError → NOT_APPLIED` branch — came back GREEN (misreporting it `APPLIED` passed all 12: the coordinator would never be told to act on a receipt the proof cannot cover). Binding test added (`test_unregistered_route_is_listed_unapplied_never_silent`: listed UNAPPLIED, no marker, cursor advances), born-red against the exact sabotage, 13 passed.
- **#509** → folded (2026-07-30 09:41): merged `2364a2a2` (merge `feceeef`) — the phantom #229 question-updated events are ROOT-CAUSED and the signature is exonerated: it was detecting REAL data loss. A coordinator-side rewrite truncated the #229 thread at 07:44 (121 lines: the 16:12 loop follow-up, the 16:35 human note with the nested table, the 16:48 follow-up, the 17:10 answer; committed at `0f97df03`, restored from `fdf30ba7` at `fd53d82a`). The truncation root cause is spun off as **#533 (P1)** with an interim coordinator rule: questions.md only via search_replace, never full-file write. The lane's defensive half landed: `_sig_text` whitespace-normalised digests (rewrap-invariant). Gate: lane's 3 reds reviewed; coordinator independent red on the follows call site went GREEN and resolved per the defense-in-depth triage — the parser's continuation join (`watch.py:12430`) already single-spaces across lines — minus one real gap it exposed: within-line whitespace (tabs, double spaces) only `_sig_text` defends, and nothing tested that. Binding test added (`test_follow_internal_whitespace_is_digest_invariant`, preconditions derived at runtime incl. that the parser does NOT collapse it), born-red against the exact gate sabotage; 8 sig tests green.
- **#531** → folded (2026-07-30 10:00): merged `a89a0bcf` (merge `e54d1142`) — `consume --through <ordinal>` closes the pending↔consume race: the advance and the #526 proof loop are bounded to the ordinal the prior `pending` reported as head; events beyond stay pending and re-list next tick (the live instance was ord=43, the #505 answer receipt, advanced past unread). Both edge refusals EX_USAGE 64 checked before any read. Gate: 16 passed; lane RED1 (bound-honouring lines) and RED2 (both refusals) discriminating. Coordinator independent red on a line the lane did NOT inject — the pre-existing `pending` `ord=` print (`journal_consume.py:184`, corrupted +1000) → exactly 2 FAILs, one of them the lane's own bound test which derives its through-ordinal from pending output (the loop closes); restored byte-identical. Coordinator landed the SKILL.md clause in the same tick: tick step 3 is now `consume --through <head-ordinal-from-step-1>` and the never-blind rule gained the bounded corollary.
- **#532** → folded (2026-07-30 10:00): merged `7d46a73` (merge `6a4c2f8`) — bdinput (d)/(e) hold-to-repeat flake (#507 class) closed at the harness layer: Playwright page.clock install+runFor fires the production setTimeout(400)/setInterval(80) deterministically; zero assertions loosened; (e) given the same treatment through the mid-hold node swap; 10 consecutive PASS incl. 3 under 1-CPU-spinner load. Gate: 3× back-to-back 31/0 on master. Lane red on the interval arm (`watch.py:7860`, 80→80000). Coordinator independent red on the DELAY arm (`watch.py:7978`, a line the lane did NOT inject, 400→40000) → exactly `FAIL (d) hold [+] fires at least 2 repeats` + `FAIL (e) hold [+] continues after a data tick`, all other checks green; cp-restored byte-identical (git porcelain clean).
- **#533** → folded (2026-07-30 10:15): merged `c945f26c` (merge `7dc1545`) — the questions.md truncation is ROOT-CAUSED: a coordinator full-file write from a PARTIAL READ at ~07:06 (tail dropped at a size boundary; 122 lines of #229 lost; the verbatim call sits in the compaction-segment gap at 524288 bytes, triangulated from four witnesses — the #509 brief's timing, 509sig's live 07:44 capture, the tail-cut shape, and the `61d4cc11` append's innocence). watch.py exonerated BY CODE (collect only reads; append_subbullet iterates every line; track_question_updates writes only the sig store). It reached commit `0f97df03` because `git diff | head -30` showed only the +8 insertions — the −130 tail sat beyond the pipe (lesson recorded: review the STAT, not the head). FIX: lint guard `check_questions_truncation` — net line-loss vs HEAD over threshold 50 ERRORs at write-time via the PostToolUse hook; `groom:` marker is the deliberate-archive escape hatch. Findings: `.dreamwork/docs/findings/533-truncation-root-cause.md`. Gate: 391 test_lint passed; lane red on the conjunction (`if False and`); coordinator independent red on the THRESHOLD literal (50→5000, a line the lane did NOT inject) → exactly `test_a_tail_truncation_is_an_error` FAILED at :428, the other 4 truncation tests correctly green; cp-restored byte-identical.
- **#505** → folded (2026-07-30 10:36): phase 1, merged `75e3a6a8` (merge `8d9ae68e`) + coordinator repin `6afa68a1` + DATA_SIBLINGS fix `8a9130a8` — the wholesale-rerender architecture is replaced: `setContent` hash-skips identical builds and otherwise morphdom-reconciles `#view` children keyed by `data-qid/aid/sha/review/keep/id/t0` (vendored morphdom 2.7.4 MIT + LICENSE, still one HTML response); human-owned `open` + mid-gesture height are stamped onto incoming `details[data-keep]` nodes; ghosts never receive keys (corpse rule, two belts). `snapshotFolds`/`restoreFolds` DELETED; the lane stopped pair deletion at `snapshotBdHover` per the brief (tip/inspector is module state, not node state) — recorded on the task. Gate: watch.py/justfile/test_watch auto-merged clean; ONE stale literal pin surfaced (the #118 fold-survival tokens pinned the deleted pair — same shape as #503) repinned to the reconciler's open-stamp and red-proved (`if (false) toEl.open` → exactly the named test FAILs). Full pytest surfaced a REAL deploy-blocking defect: the vendored data file is invisible to the sibling closure's import walk — a deployed snapshot would have crashed at import; fixed with a module-level `DATA_SIBLINGS` literal that deploy_state parses by AST (born-red at merged HEAD → green → independently red-proved by blinding `data_sibling_paths`). 8 guards green on the merged tree (selectkeep 7, corpse 9, restcollapse 12, typing 34, answers 87, dashboard 44, bdinput 31, bdhover 50). Coordinator independent red on the render seam: gen-bump removed → exactly the vacuity precondition FAILed (`the tick really ran`) while every survival assertion stayed green — the survival checks stand on their own merits, the liveness check is bound. Visual verdict PASS ×2 (dashboard fully composed post-morph; the planted bold selection visibly highlighted after a forced tick). **`#505` stays open** — phase 2: the bdHover pair, cardState/viewInputs/askState belts, review dock `replaceWith` (Q3).
- **#423** → folded (2026-07-30 11:00): merged `42456d7d` (lane `b8522776` `fff998ae`, lane-423runner, glm-5.2) — dead-runner audit, **moot-with-evidence on the runner-signal half**: the 401-exits-0 defect the task was filed against cannot arise under `spawn_subagent` (the harness owns completion reporting; a dead model is a failed spawn, not an exit-0 no-op), and the `nohup ccc &` path that made it possible is disused since the 2026-07-29 18:02 cutover (zero live ccc processes, no wrapper in repo). The dispatch-time PONG probe and a ledger FAILED state are both refuted with reasons, not just declined. One narrower gap survives — a lane exiting with a dirty worktree is invisible to sweep (commit subjects), lane_guard (`wt/*` registry; harness clones are independent `.git` dirs), and status_sync (pid liveness) — filed as **#535** and landed in the same gate. Review-gated: full 313-line findings doc read; five load-bearing citations spot-checked exact against master (`dev/ledger.py:476` git-subjects, `dev/lane_guard.py:147`+`:80` wt registry, `status_sync.py:149` pid liveness, `DREAMWORK.md:364-370` cutover) plus the empirical claim verified live (the lane's own worktree absent from `git worktree list`, full independent `.git`, no commondir). Audit lane: no test red-proof applies. Merge wrinkle: the lane's handoffs.md edit had truncated the #505 Pending line and glued the remainder onto its own line — resolved by restoring master's full #505 line and trimming the glue (the glued tail verified byte-for-byte as master #505 text before cutting).
- **#535** → folded (2026-07-30 11:00): landed by the coordinator in the same gate (SKILL.md is coordinator-owned) — the exit-dirtiness habit the #423 audit scoped: at lane completion, `git -C <lane-tree> log --oneline` since dispatch + `git status --porcelain` before treating the lane as done; the three outcomes named (delivered/clean → fold; none/clean → crashed before working; none/dirty → worked-but-undelivered, the recoverable-but-invisible `gate2`/`da197b87` case — commit or salvage on the lane's behalf). One command at one step, at lane exit, not a tick-time probe; no production code, no new tool (the three repo-side seams each refuted in the audit: wrong signal, wrong registry, wrong contract).
- **#534** → folded (2026-07-30 11:08): merged `d613b0d` (lane `0a1a3f3f` `2330f149`, lane-534sig, glm-5.2) — the sig store is versioned: `_SIG_ALGO_GENERATIONS` (append-only; `sigtext-v0` = unmarked pre-#509 raw digests, `sigtext-v1` = current) + an `algo` stamp on the store; an absent/older/unrecognised algo re-seeds SILENTLY (digests recomputed under the current algo, prior `updated_at` carried, ZERO events — an algorithm upgrade is not a content change, the #509 deploy's ~21 phantoms were exactly that). The load-bearing subtlety the brief did not foresee: `collect()` used to call `track_question_updates` TWICE on one shared store, and a re-seed in the first call left the second comparing new digests against old-algo ones — a phantom PER ANSWERED ENTRY; fixed as ONE call over `q_open + q_answered`, atomic by construction. Gate: 6 lane reds reviewed (all discriminating, none green); coordinator independent red on a line the lane did NOT inject — the re-seed's per-entry `e["updated_at"]` stamping (watch.py:13409, lane injected the store-side `prev_at` read) — sabotaged to `None` → exactly `test_reseed_carries_a_prior_real_change_stamp_silently` FAILed at :2482, the other 5 correctly green; cp-restored byte-identical. Full test_watch 398 passed, lint clean. `Migration:` trailer verified on the lane commit: the live store's v0 digests re-seed silently on the first collect after deploy — no hand-edit.
- **#428** → folded (2026-07-30 11:16): merged `c41b8c8` (lane `7289234d` `1e28930d` `ee676c87` `9168625a`, lane-428suite, glm-5.2) — the guard-suite concurrency class closed at the HARNESS layer (watch.py/justfile/pytest/assertions untouched). Census first (`.dreamwork/docs/findings/428-readiness-census.md`, 218 lines): every guard tabled by its two readiness layers. **Server layer closed in full** — 8 own-server guards (bdinput, dashboard, devoverlay, morph, morphhold, motion, posture, projtitle) moved off spawn+sleep(2500)+hand-rolled fetch onto `serveVerified` (#461); posture had NO identity check at all (graded a stranger), 6 of 8 had no try/catch. **Render layer** — subslog (the #428-named instance) + the 8 render gates onto `waitFor` (#507) on the selector each reads first. Verified zero assertion drift by diff scan (the only assertion-word change is a deleted comment in the removed hand-rolled identity check). Gate on the merged tree: **9/9 PASS via the real justfile harness** (load 16.7); lane's per-guard red-still-fires reviewed incl. its honestly-reported green red-run on motion's .gsub subject (motion binds MOVEMENT, re-traced to the .qa structural check). Coordinator independent red on a line the lane did NOT inject (lane injected `__dwSubmissions` at watch.py:3234): **`st.put(r)` dropped from `subsOutcome` (watch.py:3228)** → subslog FAILed exactly the outcome-persistence family (`with the outcome it actually had` / `says so, with the status` / `as unreachable rather than as a rejection` / `the outcome is attached`) — 4 FAILs, one line; cp-restored byte-identical. **Deferred with reason, filed #536**: the ~40 shared-server render guards' per-guard waitFor sweep (enumerable in the census).
## Pending

- **#274** · landed `a2a859e9` · 2026-07-30 11:05 · by lane-274idem — duplicate Web UI submissions idempotent end to end (server half `10f517af`, client half `a2a859e9`). SERVER: do_POST short-circuits a non-inserted journal result (replay/conflict) to _replay_verdict before handler dispatch — the receipt-to-application join had no dedup-hit signal, so a replayed UUID re-ran the handler and appended a second byte-identical Answer bullet; now the original receipt's verdict is returned (ok if received, rejected if refused) and the application never re-runs. CLIENT: DraftStore carries a per-attempt aid in the draft record — minted at submit, stable for one composition (double-click sends it twice, journal dedupes), dropped on text change (an edit is a new attempt, never deduped across content), cleared with the draft on durable landed, persisted beside the words for a lost-response/restart retry. postJSON and the popout raw fetch send it as X-Client-Action-Id. Covered: /answer /ask /comment /command (composer+popout). Out: /decide (review-artifact surface), /tint /run-mode /posture (state-setting, app-idempotent), /deploy (single-flight). _parse_entries cur-answer overwrite is moot — it anchors on the FIRST answer and never displaces it. No visible surface (transitions.md N/A). 4 TestSubmissionIdempotency + 24 test_user_events_sqlite.py green; both halves red-proved (cp revert to fail, restore); lint clean.

- **#398** · landed `9f2012a` · 2026-07-28 09:26 · by ccc @grok — lint check: post-obligation briefs must mention handoffs.md

- **#397** · landed `1b508b0` · 2026-07-28 09:42 · by ccc @glm52 — design plan: client extraction is mechanically cheap (interpolation count 1) but leans do-not-extract; does not unblock #331/#352 (Python), multiplies the registry damage class, and four named breaks (deploy/serving/autoreload/audit) must ship together if ruled in
- **#401** · landed `f2c950e` · 2026-07-28 09:47 · by ccc @grok — research: executed id-grammar matrix (14 patterns × 17 forms); #401 silent drop reproduces; 7 silent rejects ranked
- **#392a** · landed `159917b` · 2026-07-28 09:43 · by ccc @glm52 — date-only question ages show ONE figure (paintDayAge, data-day flag); today reads "today"; timed commits keep two figures
- **#401** · landed `e53d70c` · 2026-07-28 10:09 · by ccc @grok — hand-off id grammar accepts plain/sub-id/combined; malformed outside section; Folded-then-Pending so EOF append works (#401+#406)

- **#399** · landed `ddc6614` · 2026-07-28 12:08 · by dreamer-399b @grok — _landed_ids counts the historical inline form again (col0 + ref-field exclusion keeps #367 closed); burndown + forgotten_folds both green, 3 opposite-direction reds proven; just-test exit 1 is 3 pre-existing/unrelated guards (qacard=#392, docktarget/noteprop=motion flake under load) proven identical on master

- **#331** · landed `ddc4e3e` · 2026-07-28 13:13 · by dreamer-331 — one IDS_ONLY_SPAN core in watch.py, consumed by lint.LEDGER_ID and status_sync.LEDGER_HEAD; landed 152→171, open 135, all 19 joined-span ids recovered; red-proved both directions; the three guard reds (qacard/docktarget/noteprop) reproduce at parent 97becd9 and were fixed on master by 7007d5b+e15b0c0 (guard-contract fixes, not this parser change)
- **#411** · landed `25a3fe4` · 2026-07-28 14:08 · by grok (wt/411) — (also `54c68e8`) answered_at anchor \A→^+re.M+.search so the 2 second-line markers recover (5→3 None); 44 dated byte-identical; lint WARN with derived count; both red-proofs discriminating

- **#367** · landed `a36c674` · 2026-07-28 15:02 · by grok (wt/367p) — option previews A/B/C at true below-cliff geometry; chrome measured at load (A 167.9 / B 127.2 / C 31.8 px at 780); 16px margin holds at 640; row-count red-proof 3→1 rows
- **#402a** · landed `78be285` · 2026-07-28 05:32 · by grok (wt/402a) — status_sync liveness is pid-primary (kill -0) with brief-path fallback; `^ccc @` -> broad `ccc`+brief-substring so a flag between binary and alias no longer hides the lane; dreamers pruned by the same test; unknown probe raises (exit 3, fields untouched) instead of returning []; coverage line derived from file keys; mixed int/str/sub-id ids sort by str; docstring's "wrapper pid dies" claim was backwards — measured, pid is the survivor; watch.py does not render current_task_ids today (no renderer change); 3 red-proofs + just test exit 0 (1008 passed, all guards PASS)
- **#172** · landed `33284c2` · 2026-07-28 15:40 · by grok (wt/172) — project basename edge-pinned in title bar (#hproj); tab title already had dreamwork/<project>; guard projtitle.mjs with three-route identity box
- **#420** · landed `2d7e242` · 2026-07-28 15:43 · by glm52 (wt/420) — read-only census of all 139 open entries (parser-supplied counts, blocking posture, the #419 blocked-on-human question cross-check, 10 stale blockers, symbol-overlap duplicates, ranked next-five); doc only, no review artifact (coordinator working document); 1 pre-existing pytest failure on the live #367 unfolded hand-off, outside this lane's ownership
- **#430** · landed `1dd973f` · 2026-07-28 18:03 · by the coordinator — one shared above-fold check for review artifacts, replacing the inline copy each lane wrote; criterion restated from `bottom < innerHeight` (unsatisfiable for a three-decision ask block 870px tall) to "the ask starts above the fold and its first decision does too"; three red-proved preconditions — viewport actually applied (both width AND height, because the default width 1280 equals the desktop request so a width-only check passes the very bug), page actually scrolls, element actually present; declared a tool not a guard in `lint.NOT_GUARDS` with the reason it gates nothing yet
- **#429** · landed `a54d162` · 2026-07-28 18:20 · by glm52 (wt/fold) — the #263 artifact's decision moved into the hero, following 421-question-options rather than inventing an idiom: `#ask.top` 594→188 desktop and 1006→266 mobile, first decision above the fold at both, verified by the shared checker; content preserved (2109→2116 visible words, every load-bearing sha identical, both `3 of 5` hits still inside the retained 16:24 correction); Q1/Q2/Q3 detail moved below the fold at 1298/1523/1790 so the decision is above and the detail below; the criterion's remaining half — making `#ask` a documented contract so the other 19 artifacts can be measured at all — stays open on #429
- **#425** · landed `cf452d2` `51729f4` · 2026-07-28 19:07 · by glm52 (wt/425) — the safety net for #368's symlink, not the symlink: `just deploy` resolves a symlinked `watch.py` via `git ls-tree` (rev-aware, cycle-capped) and asserts the snapshot defines top-level `main` and `GENERATION` **before** `pkill`, so a broken link is refused with the dashboard still up instead of after it goes dark; the recipe and `dev/deploy_state.py` share one resolver so bash and python cannot drift; procedure and per-step checks in `.dreamwork/docs/migrate-watch-symlink.md` with a doc-map row; 13 tests, all temp-dir; verified by the coordinator in a scratch clone with a real symlink (19-byte blob accepted by bare `ast.parse`, resolver emits the real 500,807-byte module, guard refuses both the blob and a truncated module); `watch.py` still mode 100755 and `deprecated/` absent, because the flip is #368's first increment and behind his #263 Q3

- **#441** · landed `a06f6ea` · 2026-07-29 00:43 · by grok (wt/floors) — one vacuity floor **per motion** instead of one shared: `MIN_FOLD_SPAN 20` (fold travels 193px) and `MIN_GROW_SPAN 6`, because the minimum real single-line note grow measures **exactly 20px**, so the old shared 20 sat *on* the signal with zero headroom, not the 3px the filing estimated; refused a fraction-of-observed-span floor as `#444`'s trap one level down (any observed span satisfies its own fraction); `transitions.md` updated in the same commit; states.mjs 22 PASS solo and 22/22 under a concurrent second run at load 57.23

- **#360** · landed `4d4e705` · 2026-07-29 00:43 · by glm52 (wt/sshauth) — ssh-rooted self-hosted hub auth, **design and docs only**; supersedes `hub-public-auth.md`'s identity recommendation, inherits its threat model and `/data.json` leak; four options compared; public/WAN serving remains forbidden pending his review

- **#447** · landed `d387ba3` · 2026-07-29 00:54 · by grok (wt/igc) — IGC vendored as `igc-method.md` + `igc-concepts.md` (his instruction), chosen as option **D** of four by an actual IGC: **A** fails because a verbatim nested skill keeps its own `name:` frontmatter and a loader sees a second installable skill, **B** fails on a machine without `use-igcs`, **C** loses `cf-concepts`; staleness detected by upstream path + sha256 per file; `SKILL.md` instructs IGC at four judgement sites, and a lane that must choose is handed `igc-method.md` in its brief; lint check declined with four reasons (a semantic "presents options" cannot be matched reliably, and it would restate the clause it reads — `#444`'s ground)

- **#449** · landed `614a668` · 2026-07-29 01:22 · by glm52 (wt/mistperf) — the SVG mist is **shelved behind `MIST_ON=false`, not deleted**, with CSS compositor blur carrying the dissolve; same-run proof at load 49: **12.1 → 27.6 frames (+128%)**, worst frame 262 → 129ms, CSS blur measured free; **two hypotheses refuted before anything was built** — animating `feTurbulence@baseFrequency` (freezing it, and all six per-frame writes, ≈ baseline) and filtered area (a 42% ghost clamp, 553×1557 → 553×900, changed nothing); the cause is a **threshold**, two filter rasterisations per frame contending with the shader, either alone ≈ baseline; guard `dev/capture/dissolve.mjs` red-proven on two injections; successor `#453` (moved/tiled texture)

- **#455** · landed `8a83df1` · 2026-07-29 01:23 · by grok (wt/context) — the audit **refuted the brief**: 17 of 27 artifact first screens already answer ≥3 of 4 orientation questions, so a blanket context paragraph was the wrong fix; the structural hole is *"what happens if he says nothing"* at **3 of 27**, now a build-time required slot mirroring `#436`'s ask contract (production line `enforce_if_silent_contract`), refused on **absence** never on length (his 01:13/01:17 ruling); `269-draft-durability.html` — the artifact he was reading when he said he felt lost, scoring 1/4 — rewritten as the worked example; all 15 buildable artifacts current, the 12 `src`-less remain the standing reason the walking guard is unregistered

- **#456** · landed `bd0f0d1` · 2026-07-29 01:29 · by grok (wt/dayage) — ` · ` between a question's date and its age, and the pad zero to `opacity:.5`; chose **opacity over the colour token** because opacity composites against the animated shader background and a dim token does not, which is why he asked for opacity; found a second defect on the way — `.qage`'s `margin-left` was carrying the gap, so with a separator added the spacing would have doubled; `watch-design.md`'s age contract updated in the same commit

- **#457** · landed `e38e9be` · 2026-07-29 01:31 · by the coordinator — every built artifact's head carried a stray `>` (he reported it twice and suspected the template; **the template was innocent**): all four meta-anchor patterns in `review_artifact.py` matched the tag *minus its own closing `>`*, so `sub()` left the old `>` behind and an insertion at `.end()` landed before it — one stray per meta, `#436` making one and `#455` three; red-proved by reverting only `ASK_META_RE`'s `\s*>`; new check asserts no head `<meta>` is missing its close, with a derived precondition that the scanned corpus carries those metas at all; **neither originating lane could see it because both checked the meta was *present*, never that the head stayed well-formed**

- **#450** · landed `9544f9e` · 2026-07-29 01:33 · by glm52 (deficiency, master) — the containment deficiency stated per his `#288` ruling (*"just plan for it and make sure the deficiency is noted"*): per-harness capability table, the trusted-nodes-only precondition where a reader would act on it, the seams that keep later isolation possible, and the warning copy; **no mechanism built**, the namespace wall stays prototyped and unwired; carries his reframe — whoever supplies the API key can supply the harness, so it is **not our seam** rather than our unbuilt work

- **#263g** · landed `693b2e9` · 2026-07-29 01:45 · by grok (wt/gateart) — dispatched to rebuild the second-gate artifact with context, problem, IGC and a rec; **mid-flight the question was answered** (*"ack good to go"*, 01:37), so it was steered to land the page as a **record of the decision taken** rather than an ask — context, problem and IGC kept, declared `#ask`/if-silent exemptions instead of a decoy question; merge conflicted on the *built* file (the coordinator had rebuilt all 15 for `#457`) and was resolved by taking the lane's **source** and regenerating the output, never hand-merging generated HTML

- **#269v** · landed `6a6ddff` · 2026-07-29 01:43 · by grok (draftcheck, master) — his correction *"drafts are durable btw"* proved right and the ledger wrong: typed text **survives a real `watch.py` kill and restart** (pid 1614177 → 1673912 on :39897) in the review dock, the `/questions` boxes **and** the composer, verified by reading the fields rather than storage; writes on **every input event, no debounce**; a draft clears only on durable success and a **failed send keeps it**; storage is `localStorage`, so the authorised remainder is the *upgrade*; two boxes keep no draft at all — `#askbox` and popout `#ptext` — filed as `#459`
- **#436b** · landed `75a3488` · 2026-07-29 02:03 · by grok (wt/srcless) — the source-less half of the corpus is now **explicitly exempt rather than silently skipped**: a side-file with one reason per artifact, `corpus_contract_coverage` asserting the equation **as sets** (`examined ∪ side_exempt == built`, `examined ∩ side_exempt == ∅`, `{src} − {built} == ∅`), and the walking guard `dev/capture/reviewask.mjs` registered (DEFAULT_GUARDS 54); IGC over classes chose exemption over reconstruction for all 12 — no template stamp, never built through `review_artifact`, hand-editing a built file forbidden; the coordinator corrected its first expression mid-flight from `|built| − |src|` to a real set difference, because the arithmetic is only right while `{src} − {built}` is empty and cannot say so; **coordinator-verified independently** — guard PASS at load 33.54, and dropping one exemption line yields `unaccounted={tasks-page.html}` with the equation naming the missing member, restored byte-identical and green
- **#421** · landed `40ca81f` `01c9bd7` · 2026-07-29 02:12 · by glm52 (wt/subdec) + the coordinator — his A+B+D answer: A and D became conventions in `file-formats.md`'s ask contract, **B** became a `lint.py` ERROR when a folded entry drops a declared sub-decision; recognition is **declared, not guessed** (one `**Sub-decisions:** \`Q1\`, \`Q2\`` line, read only from that line, never prose) and **the marker is its own content-resolved cutoff**, so the historical corpus is silent and no sha is pinned (the objection that made `#405` refuse a hand-pinned cutoff); `#275`'s Q3/Q5/Q6 **verified** still open, so the defect B exists for is real and current; the lane initially *predicted* post-merge visibility instead of merging master and testing it, and was told to test — coordinator then added the pending-declarations clause so `0 folded, 0 checked` cannot read as "no subject" when it means "adopted, waiting for a fold", red-proved on `_answered_split` (sabotage → clause vanishes, test FAIL; restore → PASS) with the fixture asserting the declaration really sits in the open half; C withdrawn on his 01:13/01:17 brittleness ruling — no length gate, ever; 1122 tests


- **#510** · landed `PENDING` · 2026-07-30 05:30 · by lane-510orc (design; model per dispatch record — a lane cannot know its own model) — design doc `.dreamwork/docs/plans/orchestrator-posture.md` + a doc-map row; an 'Orchestrator' option in Posture, IGC'd (I1–I5 × G1–G5, survivor: a fifth posture axis). Investigated the referent — designed against orchestration MODE (coordinator implements nothing, only dispatches+reviews; the coordinator-only-loop mode) over orchestrator IDENTITY (pushed back: provenance owned at dispatch, not a posture dial). Refuted fold-onto-delegation, the disabled `hierarchical` run-mode value (re-bundles #443), control-only, and sibling-file. Escalates Q1 (mode vs identity), Q2 (`hands-on`|`orchestrator` binary), Q3 (land the axis, don't enable `hierarchical`) with recs; draft questions.md entry included, NOT edited. Design only — no code authorised
- **#513** · landed `23de13cf` · 2026-07-30 06:30 · by coordinator (inline; SKILL.md is coordinator-owned) — tick restates the resolved posture axes each beat (the structural fix for posture drift, per the human steer recovered from blindly-consumed journal event ord=39); batched-delivery tick habit `pending → process → consume` with the never-blind rule; durable-state posture entry gains the delivery axis. Store-folded 06:30.
- **#498** · landed `f9144beb` · 2026-07-30 06:38 · by coordinator (fold) — gate found the 390px bdhover overflow (real, not #507 flake: baseline PASS at f74197b0); fix lane lane-burndown2-bdhover merged `4c192843`. Red-proof on merged tree: `box-sizing:border-box` removed → bdhover FAIL ("at 390px the inspector stays inside the panel"); cp-restore → PASS. burndown guard PASS. Collateral test repair `5dfe2738` (#487 window literal), red-proved via centre-clamp reintroduction.
- **#499** · landed `f9144beb` · 2026-07-30 06:38 · by coordinator (fold) — same merge as #498; independent red at `all.slice(-lim)` (3 #499 checks FAILED; restore → burndown PASS, 372 passed).
- **#511** · landed `PENDING` · 2026-07-30 06:30 · by lane-511live — the four "live ledger" migrate acceptance tests were RED on master because they read .dreamwork/tasks.md (now the one-line migration-notice shim); chose option (a) RE-POINT at tasks.md.deprecated (the frozen ledger is the post-cutover subject, the same convention test_ledger_store.py already follows; git_snapshots — exercised ONLY by the live history test — kept its real 678-commit history via a LIVE_LEDGER_HISTORY path on tasks.md, so retire's coverage loss was avoided); the history test's verify moved to verify_db directly with rich snaps because the CLI couples ledger-text and git-walk through one --ledger path, now split by the cutover (CLI verify over the deprecated path's single commit skips the groomed-body-match as vacuous); every added/changed check red-proved: the `## Open` precondition vs the shim, `len(snaps)>1` vs the deprecated file's 1-commit history (which makes rec[tasks] genuinely empty, the vacuity the guard prevents), and verify_db body-match vs a committed groomed-body tamper (a first green red-run caught an uncommitted-tamper scaffolding bug — sqlite rolls back DML on close without commit); pytest 1518 passed 0 fail; the one lint ERROR (tasks.md no Next id header) is pre-existing on HEAD and out of lane scope
- **#357** · landed `PENDING` · 2026-07-30 · by lane-357impl — `dev/ledger.py` emits the warning footer on every verb's success path via `emit_warnings()` to **stderr** (stdout stays pipeable): his five counts (unchecked messages · open tasks · unanswered questions — the new-question count collapsed in, one parse · unfolded answers) + incomplete-data (untyped · missing origin, store source only) + journal unconsumed receipts (`head_ordinal − coordinator cursor.scanned_through`); zeros absent, clean tree silent, **stateless** (throttle refuted by the IGC), WARN never ERROR (exit codes unchanged). Reuses `store_ids_by_state` / `lint.check_unfolded_answers` / `watch.parse_open_answers`+`parse_open_questions` / `open_journal`+`head_ordinal`+`cursor` — never a second copy. Red-first `test_ledger_warnings.py` (7 checks, each red-proved by injection + cp restore); Q6 ruled (full line, I1, 03:52 `rec`) recorded in `cli-warning-layer.md` this commit
- **#505** · landed `PENDING` · 2026-07-30 · by lane-505arch (design; model per dispatch record — a lane cannot know its own model) — design doc `.dreamwork/docs/plans/render-architecture.md` + a doc-map row; the wholesale-rerender smell. Inventory: 16 reset surfaces (1 = his bug, 11 re-applied by hand post-swap, 1 partially resumed, 3 never at risk); the chrome survives because it is ALREADY a keyed diff (`renderChrome`), which is the absence-of-idiom inside `#view` (one `innerHTML` swap/tick, `watch.py:6778`, + ~11 snapshot/restore pairs). IGC (I1–I5 × G1–G5) refutes his three directions as standalone fixes — ids alone don't survive the swap, content-check only skips unchanged ticks, React/preact impose a build step + second render authority against the single-file architecture (`watch-design.md:2737`); survivor is **keyed reconciliation of `#view` (morphdom-idiom) + content-hash skip (I5)** — generalise `renderChrome`'s keyed diff, keep survivor nodes by existing keys (`data-qid`/`data-aid`/`data-sha`/`data-review`/`data-keep`) so selection/caret/scroll/focus survive as a class. Subsumes #141/#503/#494 (their snapshot/restore pairs become dead code; `data-keep` retained as a reconciliation key). Every transitions.md family named with its fate (preserved/absorbed). Pushed back on "React" honestly. Escalates Q1 (vendored morphdom vs hand-rolled), Q2 (hold no-build G4), Q3 (scope), Q4 (guard the corpse rule) with recs; draft questions.md entry included, NOT edited. Design only — no code authorised
- **#508** · landed `5ad3dc0` · 2026-07-30 07:09 · by lane-508deploy (model per dispatch record — a lane cannot know its own model) — `just deploy` reported `deployed` against the OLD process. CONFIRMED MECHANISM (fixture, scratch_508 evidence): the deployed server ran `--dev` (=> autoreload); the recipe's `mv $snap.tmp $snap` overwrote the snapshot it watches, so autoreload `os.execv`'d the OLD pid IN PLACE (same pid, new generation, port flickered free via close-on-exec then rebound) — observed pid-unchanged/generation-changed; stop_deployed's 50ms poll can grade that flicker as 'free' and succeed while the old process rebinds; the new `nohup` server then dies on bind (EADDRINUSE, invisible); the curl-liveness readiness prints 'deployed' against the old pid (reproduced deterministically). STRUCTURAL root cause: success was LIVENESS (curl 200), not IDENTITY. FIX (true-by-construction): `verify_deployed` (dev/deploy_state.py) returns 0 only when `listening_pid==expect_pid` AND its argv runs the snap; `wait_port_free` confirms the port STAYS free (settle defeats the execv flicker) before the start; the recipe captures `$!` and calls `--verify-deployed --expect-pid`, echoing success only after it. `--dev` REMOVED from the deployed start line — autoreload was the re-exec enabler and serves no purpose on a frozen snapshot the recipe itself restarts (GENERATION still bumps on the fresh process). The PID is load-bearing over argv because autoreload's in-place re-exec keeps the old argv identical to the new snap. 9 new tests, all red-proved (named lines: success-return, pid-check branch ×2 via message+rc, _pid_alive, wait_port_free refuse, settle rebind-detection, --verify-deployed CLI branch, recipe --dev re-add). Identity check stands regardless of the --dev decision.
- **#514** · landed `df5e764` · 2026-07-30 06:35 · by lane-514wake — audit of wake semantics under `delivery: batched`; report at `.dreamwork/docs/findings/514-wake-semantics-audit.md`. Traced all 13 `log_event` call sites in `watch.py` + 1 raw append in `ud-dw-tasks-migrate`; the single append fn is `log_event` (`watch.py:13451`), and the #342 gate `emits_wake(kind, target)` (`watch.py:13403`) + `delivery_mode` (fresh per-event read, `watch.py:13398`) IS landed. **4 COMPLIANT** content routes (`/command` by kind, `/answer`, `/ask`, `/comment` — each gated); **2 VIOLATION**: `/decide` (`watch.py:14321`, journals a receipt but wakes unconditionally, missed by the doc's 4-route list) and `question-updated` (`track_question_updates` `watch.py:12902`, ungated + unjournaled digest signal on the batched question class); **7 UNCLEAR** (control-plane `/run-mode`+`/posture` always-instant but doc-silent; 3 journal failure-path wakes exception-only; migrate one-shot). `dev/journal_consume.py` drain confirmed silent (no self-wake); a do-now wakes at POST naming the event, not the batch. No contradiction of the ruling itself — the doc's route enumeration is stale (4 of 9 `WRITE_ROUTE_HANDLERS`) and silent on control/system/error wakes. Proposed follow-ups #515–#519; **audit only, owns no production file**
- **#512** · landed `a0bf2fc` · 2026-07-30 07:10 · by lane-512show — `dev/journal_consume.py show <receipt-id>` (third subcommand, #512): a read-only full-payload verb so batched delivery is never lossy. Composes the single public `Journal.get_receipt(receipt_id)` (by-id, cursor-INDEPENDENT read) — no `user_events/` change, no new journal query; prints a key:value header (receipt_id/state/revision/client_action_id/request_digest) then the payload decoded UTF-8 verbatim (no 80-char cap, no #126 newline-collapse — the deliberate exception, this output is for an agent to READ a multi-line instruction). Never advances the cursor; works for ALREADY-CONSUMED receipts (consume moves only the cursor, the row persists — the recovery case for a blindly-consumed event). Unknown id / absent journal → one-line stderr + EX_USAGE(64), no db created (read-only #501 discipline). 5 red-first tests seeded via the production `receive()` path with runtime-derived preconditions (payload len > _PREVIEW_LIMIT asserted, cursor-moved asserted, multi-newline asserted). Red-proof per test via scratch-snapshot/cp restore. Pending/consume/_PREVIEW_LIMIT/_format_event untouched
- **#517/#518** · landed `1f105aec` · 2026-07-30 · by lane-517doc — `.dreamwork/docs/plans/delivery-modes.md` caught up with the landed #342b code per the #514 audit. **#518:** the stale "What changes in watch.py's command handlers" section (named 4 of 9 `WRITE_ROUTE_HANDLERS`, framed the gates as a future increment) is rewritten design-as-built with a 9-row table verifying each handler against watch.py while writing — the four gated content routes (`/command`,`/answer`,`/ask`,`/comment`), `/decide` (NOT gated — its `emits_wake` gate is in flight under #515, named as such, never asserted as present), `/tint`+`/deploy` (non-waking by design, with the reason), and the control routes; the "authorises no code / next increment" framing is reframed to landed-as-#342b. **#517:** new "Wake channels outside the route table" section states a policy for every wake the ruling was silent on — control-plane wakes (`/run-mode`,`/posture` triple + delivery axis) as an explicit always-instant carve-out (a delivery toggle that doesn't wake until the next tick defeats the toggle); journal failure-path wakes (`_journal_receive`/`_journal_record_health`/`_journal_reject`, exception-only) as always-loud error diagnostics; the one-shot `ud-dw-tasks-migrate` wake; and `question-updated` (policy pending under #516, in flight). Every code claim cites a watch.py line verified while writing (line numbers drift; none copied from the audit — e.g. the audit's `log_event:13451`/`emits_wake:13403` are now `13464`/`13418`). Doc-map row for `delivery-modes` stays accurate (directory enumeration; the plan file remains). Doc lane — no code touched; no server started, no port bound
- **#515** · landed `8908b96` · 2026-07-30 · by lane-515decide (model per dispatch record — a lane cannot know its own model) — `#514` F1 fix: `_handle_decide` journaled its receipt via the E3 cutover but emitted its `review-decision` wake line with NO `emits_wake` guard, so under `delivery: batched` a `/decide` woke every time — silently undoing batched mode for review decisions (the same content family as `/answer` and `/comment`). The wake line is now gated behind `if emits_wake("/decide", target):` mirroring the `/comment` idiom (`watch.py:~14337`); the receipt commit and store write are UNCHANGED — withholding the wake IS batching, the cursor carries the decision to the tick. Did not touch `track_question_updates` (~12917, lane-509sig) or `emits_wake`/`PREEMPT_KINDS`. Two red-first tests in `TestDeliveryWakeRouting`, each red-proved by injection + cp restore (never `git checkout`), each naming its production line: **(1)** `test_decide_withholds_wake_in_batched` — RED by removing the `if emits_wake("/decide", target):` guard (the line fires in batched → the withhold `assertFalse` fails); production line injected: that guard in `_handle_decide`; **(2)** `test_decide_wakes_in_instant_and_default` — RED by removing the `log_event(...)` `review-decision` emission (zero lines in instant → the `len==1` fails); production line injected: that emission in `_handle_decide`. Preconditions derived at runtime (the hollow-check rule): posture file carries the delivery axis (`read_posture_file[\"delivery\"]==\"batched\"`/`\"instant\"`/absent), target is store-mode (`source_of_truth==\"store\"`, else `/decide` refuses and a withheld-line check is vacuous), route returned 202, and the decision landed in the store (`_review_decisions`). Born-red run against unfixed production: batched test FAILED at the wake-line assertion after all preconditions passed (not hollow). pytest `test_watch.py` 374 passed (372 + 2). The two pre-existing lint ERRORs (`tasks.md` no `Next id` header; the `#398` brief-obligation on this brief) are dispatch-level, out of lane scope, and not caused by this change
- **#507** · landed `f663749` · 2026-07-30 · by lane-507flake — guard-load flake characterised then fixed at the HARNESS layer; watch.py/justfile/pytest untouched. MECHANISM, two readiness layers raced fixed sleeps under chronic load: (1) SERVER — burndown/bdhover spawned watch.py then `await sleep(2500)`+fetch; under load python outlasted the sleep, fetch threw ECONNREFUSED → "threw before finishing", and a port-squatter was graded (the hand-check `process.exit(1)`'d, never gated); (2) RENDER — `goto networkidle`+a fixed sleep(1000/1200) before the first panel read; networkidle fires when data.json is fetched but the client JS that BUILDS `.bd` has not run, so burndown's bare-server panel read `no ledger: {}` over a correct server at load 58. markrail was NOT a flake — a deterministic selector-scope regression: `c882a9da` (#367/2b) added `.markstrip` (the collapsible walk below the cliff) rendering every `[data-mid]` a second time, and a bare `[data-mid="N"]` resolved to the hidden copy first (0×0) / Playwright strict-mode-violation; failed 7/7 at load 28-66 with the page fully laid out. FIX: burndown/bdhover → `serveVerified` (serve.mjs, #461 — the shared primitive 16 guards already use) for server+target identity + a new shared `waitFor(page,sel,timeout)` in dom.mjs (wraps Playwright `waitForSelector`, the filehl/fileview idiom) for the rendered DOM; markrail → scope every `[data-mid]` to `.marktab[data-mid="N"]` (compound — data-mid and marktab share one element). No assertion value or threshold changed. EVIDENCE: fixed code burndown 5/5 · bdhover 5/5 · markrail 5/5 PASS under load 40-54 (was: master bdhover FAIL 1/3 at load 47.62 on `#494 precondition: tip and inspector both up`; master burndown FAIL bare-server at load 58). RED-PROOFS (each at rest+load, byte-identical `git checkout` restore): markrail RP1 inject `html{scroll-behavior:smooth}` → "LANDS SETTLED (0 part-way)" FAILS (7/34 part-way) — the named defect; markrail RP2 revert scoping → 0×0 + strict-violation returns; burndown RP1 ledger sabotage (arrived 9→10) → head literal `4 open · 9 arrived` FAILS (FAIL:2, reached 53 checks); bdhover RP1 commit-profile sabotage (bucket0 2→1 revs) → `precondition: served buckets carry the planted commit profile` FAILS (FAIL:3, reached 47 checks). Lane-owns EXTENDED to `dev/capture/dom.mjs` (the new shared `waitFor`). No REAL timing-dependent rendering finding — served content was correct; the measurement ran before readiness, which is the harness defect, now closed
- **#519** · landed `2965ade` · 2026-07-30 07:47 · by lane-519audit — audit of the do-now exactly-once path (wake AND drain); report at `.dreamwork/docs/findings/519-exactly-once-audit.md`. Static read-only across `user_events/apply.py`, `dev/journal_consume.py`, `SKILL.md`'s tick habit, and `watch.py`'s `_handle_command`/`do_POST` E3 cutover; every claim cites a verified file:line. **Double-act path: VIOLATION** (batched) — a do-now pre-empts by wake-line (`emits_wake`→True, `watch.py:13426`) AND drains on the tick; the two channels share no id (`command_line:13690`, no receipt id) and the proof meant to make the drain a no-op (`apply.prove_applied`, `delivery-modes.md:168-170`) is **not wired**. **Never-consumed/leak path: VIOLATION** (instant, the default, `watch.py:13386`) — every `/command` receipt journals (E3, `watch.py:14148`) but the drain is instructed only for `delivery: batched` (`SKILL.md:171`), so the coordinator cursor never advances and `(cursor, head]` grows without bound; contradicts delivery-modes.md:218-226's "cursor read on every tick" guarantee, and an instant→batched switch mass-replays. **Root cause (F4):** `apply`'s exactly-once proof (reconcile/prove_applied/adapters) is imported by NO production module (only `test_user_events_apply.py`/`test_user_events_domain_files.py`); `consume` is a two-act read-then-advance (`journal_consume.py:168-181`), missing delivery-modes.md's middle "replay through adapters" act. Proposed follow-ups #520–#523. **Audit only — owns no production file; review-gated, no test red-proof**
- **#521/#522** · landed `b91edc6a` · 2026-07-30 07:57 · by lane-521md — markdown rendered view: blockquotes (`blockquote.mdquote`, consecutive `>` lines, fences win, zero `>` glyphs) + general `[text](target)` via `linkifyMd` (known-internal+relative→closed-set → .mdfile+pip; http(s) plain no pip; else fully literal — never half-render); file view `ensureData` so cold /file has linkable_paths; guard `mdquote.mjs` registered (27 PASS); reds by injection+cp: A mdRender class→4 FAIL, B MD_QUOTE null→4 FAIL, B2 keep-`>`→glyph FAIL, C no internal promote→6 FAIL, D promote unknown→2 FAIL, E fence loses→3 FAIL; visual desktop+390 pass (quote quieter left-rule, links consumed+pip, unknown literal, fence keeps `>`)
- **#527** · landed `2c251c8` · 2026-07-30 08:16 · by lane-527recon (model per dispatch record — a lane cannot know its own model) — the wake-line now carries its receipt id so the coordinator can match a drained receipt (channel B — the journal cursor) to a wake it already acted on (channel A — watch-events.log): the join F1 of the #519 exactly-once audit found missing. **Seam:** `self.journal_result().receipt_id` read in `_handle_command` — the SAME `ReceiveResult` the E3 commit in `do_POST` stashed before dispatch (the line the brief names: "the cleanest existing seam between the E3 receipt commit and _handle_command"); `_send_receipt` already uses this seam to build the Location header. `command_line(kind, text, source, receipt_id)` appends ` [receipt <id>]` as a suffix (after the text, so existing prefix-based readers — `assertIn`, tail monitors, `plugcmd.mjs` — are undisturbed); absent when the journal is off (the legacy E2 baseline that commits no receipt). **Reds (each injection into the production line the check binds + cp restore):** RED1 `command_line`'s `suffix=` line → `suffix=""` → `test_command_line` FAIL at line 3180 (suffix absent); RED2 `_handle_command`'s `rid=result.receipt_id...` line → `rid=None` → `test_command_wake_line_carries_the_same_receipt_id_as_the_journal` FAIL at line 7624 (assertIsNotNone — no id in the wake-line); RED3 same line → `rid="deadbeef-mismatch-0000"` → same test FAIL at line 7640 (assertEqual wake_id≠journal_id — proves equality not just presence). All cp-restored byte-identical; `git diff --stat watch.py` clean after each. Guards `plugcmd dismiss restcollapse qsignal` PASS SOLO (port 39899, 39896/39897 stale — not touched). pytest 1542 passed; the one lint ERROR (`tasks.md` no `Next id` header) is pre-existing on HEAD and out of lane scope
- **#523/#524** · landed `cccfc7d6` · reds: restoreViewInputs no-op → (a)(b) focus/value/caret FAIL; skip value restore → typed-value FAIL; bdStepNudge no-op → (c)(d)(e) FAIL; hold no-interval → (d)(e) FAIL; always-stop pointercancel → hold-across-tick FAIL; each cp-restored byte-identical · guard `bdinput` 29 PASS 0 FAIL (solo) · visual: desktop+390 rest/hold — quiet − [N] + ⟳ on one head line, hold steps the value, mobile ellipsises counts not the control · by lane-523burndown — #523 focused #view input snapshot/restore (typed wins; #bdlimit-in id; type=text for caret APIs); #524 module-level hold-to-repeat steppers composing across ticks; not #505 reconciliation
- **#521/#522** · landed `635a29cb` · 2026-07-30 08:17 · by lane-521md — mdquote readiness fix (#507 class): bounded-poll /data.json until planted paths appear in linkable_paths + /filedata serves fixture before any precondition or /file navigate; waitForSelector #filebody .md no longer swallows 404. Evidence: 5/5 back-to-back PASS (29 each) on one shared server incl. target resets; reds still fire — E fence-loses→3 FAIL (quote count, fence holds >, fenced not blockquote); D promote-unknown→2 FAIL (unknown not promoted, stays literal); C no-internal→6 FAIL (known/relative link+pip+consumed, corpus brief bleed). Assertions not loosened; watch.py untouched.
- **#525** · landed `423db6ff` · 2026-07-30 · by lane-525tables — GFM pipe tables in `/file` rendered-markdown: `mdBlocks` table kind (header + `|---|` delimiter + body; fences win; ragged col-count mismatch degrades to prose) + `mdRender` `<table class="mdtable">` with cells through the same inline pipeline as prose (`linkifyMd`); CSS quiet dim-ruled (`--line`), header `--lit`, `tabular-nums`, static. Guard `mdtable.mjs` registered (DEFAULT_GUARDS, 28 PASS 0 FAIL). Reds by injection+cp restore (byte-identical; production lines named): RED1 `out.push({ kind: 'table', …})` → `kind:'p'` → FAIL exactly one table.mdtable (+header/body/link); RED2 `if (fence) { … continue; }` → `if (false && fence)` → FAIL fence holds pipes + FAIL fenced region not a table; RED3 `inline(c)` on th/td → `esc(c)` → FAIL cell known link + FAIL no bleed. Visual: `screenshots/lane-525tables/mdtable-desktop.png` + `mdtable-mobile-390.png`. watch-design.md Prose rendering updated same commit. Nothing else in watch.py touched.

- **#510** · landed `52d7687` · 2026-07-30 · by lane-510impl (model per dispatch record — a lane cannot know its own model) — he ruled all three recs (orchestration is a MODE; binary axis `hands-on`|`orchestrator`, absent→`hands-on`; land the axis, do NOT enable `hierarchical`). The axis is INERT until a consumer reads it (same forward-looking-dial shape delivery held pre-consumer); absent→hands-on keeps a pre-axis file byte-identical in effect. Four touches, the shape `delivery` (#342) took. **lint.py sites (3):** `POSTURE_STOPS_ORCHESTRATION = ("hands-on","orchestrator")` beside the others; `POSTURE_AXES` widened; `check_posture` optional-axis block (ERROR on out-of-set, absent silent) + clean-bill denom (joins "of N" only when present). **watch.py Python sites (8):** `POSTURE_ORCHESTRATION_DESC`; `_get_page` vocab injection (STOPS+DESC); `parse_posture_text` branch; `resolve_posture` key+`ORCHESTRATION_DEFAULT`; `write_posture` param+line+guard; `posture_line` carries `orchestration=` (orchestration rides the `posture via watch` line — NO separate events line, per the brief, because it has no consumer, unlike `delivery_line`); `_handle_posture` (parse/validate/preserve/silent/event); `_summary_posture` projection. **watch.py JS sites (12):** `committedPosture`, `readPostPending`, `writePostPending`/`writePostCancel`, `pickPostureAxis` guard, `armPostureDraft` (cancel-cmp+postDraft), `stepPostureDelegation`, `posturePicker` (cur+orchChips+.paxis row), `postDescFor`, `syncPostureFromData` (3 spots), storage handler, `commitPosture` (POST body+optimistic), `__getattr__` proxy. **Deliberate skips (delivery's wake-routing consumer — orchestration is inert, so no consumer):** `delivery_line`, `delivery_mode`, `emits_wake`, `PREEMPT_KINDS` have NO orchestration analogue (the spec's "inert until a consumer" property); the lint.py "delivery" matches at 764/846/3656/3668/3749 are the #381 hand-off single-writer rule, a different word — not posture-axis sites. **14 reds, each injection+cp-restore, naming the production line:** R1 lint closed-set ERROR→`orchestration_ok` membership (FAIL `test_unknown_orchestration_errors_loud`:5572); R2 lint clean-bill denom→orch term dropped (FAIL `test_orchestration_present_joins_clean_bill_count`+`..._count_five`:5621); R3 file-formats/code disagreement→`POSTURE_AXES` minus orchestration WARNs "unknown axis"; R4 `parse_posture_text` branch→neutered (FAIL:7334); R5 `resolve_posture` default→wrong value (FAIL:7347); R6 `write_posture` line-write→dropped (FAIL KeyError:7365); R7 `posture_line` field→dropped (FAIL 2 posture_line tests:6870); R8 `_handle_posture` event `or not orch_same`→dropped (FAIL FileNotFoundError:7410); R9 `_handle_posture` preserve→forced hands-on (FAIL:7446); R10 `_handle_posture` reject guard→neutered (FAIL 500≠202:7452); R11 `_summary_posture` projection→dropped (FAIL KeyError:7471, AFTER strengthening the test to also assert the `summary()` path — the first run was a green red-run, a finding acted on); R12 JS chip row→removed (FAIL:7501); R13 `commitPosture` POST body→dropped (FAIL:7525); R14 `committedPosture` default→removed (FAIL:7529). Both files byte-identical to backups post-restore. **Tests:** `test_lint.py test_watch.py` 773 passed + 57 subtests; `python3 lint.py` clean for posture (the lone `tasks.md` "no Next id" ERROR is pre-existing — reproduced with changes stashed at `cd843a3b`, not caused by this). **Guard:** posture.mjs SOLO 39/39 PASS (freePort, no collision with the lane on :39897). `git commit --only` one commit for all four touches (file-formats+lint same commit). No `Migration:` trailer (absent→hands-on = today; #426 covers the re-read). **`#510` stays open** — the coordinator's SKILL.md restate + conduct (what `orchestration: orchestrator` DOES to loop behaviour) lands separately; this lane landed the dial, not the consumer
- **#520** · landed `5854bb91` `a7188578` · 2026-07-30 · by lane-520deploy (model per dispatch record — glm-5.2; a lane cannot self-report) — `just deploy` shipped the snapshot (`mv $snap.tmp $snap`) BEFORE `--stop-deployed`: against an autoreloading occupant the mv overwrote the file it watched, so autoreload os.execv'd the old process IN PLACE (same pid, port flickered free via close-on-exec then rebound) — arming the race the #508 identity checks then had to catch. The first identity-verified deploy (2026-07-30 07:38) hit exactly this (stop sampled the flicker, wait-port-free refused, a human step was needed). FIX (ordering, not a new check): stop → wait-port-free → mv(ship) → start → verify; the mv now overwrites a path no live process watches. ORDER SUBTLETY (the brief's question — does the stop path read shipped content?): NO — `--stop-deployed` needs only the snap PATH (`argv_runs_snap` compares realpaths, never shipped CONTENT), so the old process still running $snap with a matching argv is correctly identified and stopped before the mv. The reorder is clean. The #480 boot-proofs (`--ship-siblings` + `--assert-importable`) stay BEFORE the stop — a snapshot that cannot boot is still refused with the dashboard up; only the race-arming `mv` moved (ship-siblings writes `$dir/watch.py` etc., NOT `$snap`, so it does not arm the race). `rm -f "$snap.tmp"` added to the stop/wait error handlers (temp not yet renamed at that point). 2 new tests: **(1)** ORDER — reads the recipe and asserts stop < ship(mv) < start(nohup) < verify at runtime (four anchors derived from recipe text, never line-pinned); **(2)** autoreloading-standin fixture — a real process that watches its own file and os.execv's on mtime change + the NEW ordering completes with identity verified; precondition asserted at runtime (standin really re-exec'd: generation changed + same pid, else vacuous). All 8 #508 identity tests pass UNCHANGED (diff shows only additions, zero modifications). Red-proofs by injection + cp restore (each byte-identical): **RP1** ORDER test — mv moved before --stop-deployed (the pre-#520 order) → FAIL at `assert i_stop < i_ship` (914 < 863); named line: the mv position in the deploy recipe. **RP2** fixture precondition — `os.execv(...)` neutered in AUTORELOAD_STANDIN._watch → FAIL at `assert reexeced` (standin never re-execs); named line: the os.execv call in the standin fixture. **RP3** fixture main — `pid = listening_pid(port)` sabotaged to `pid = None` in stop_deployed (the exact race: listening_pid grades the execv flicker as free, returns 0 without killing) → FAIL at `assert not _alive(old.pid)`; named line: `pid = listening_pid(port)` in stop_deployed. pytest test_deploy_state.py 39 passed (37 existing + 2 new)
- **#526** · landed `86ac94b` · 2026-07-30 · by lane-526proof (model per dispatch record — a lane cannot know its own model) — wired the **#519-F4** exactly-once proof into the cursor drain. `dev/journal_consume.py` now imports `user_events.apply` (a production module imports the proof for the first time — F4 closed) and routes every drained receipt through its adapter's `reconcile` BEFORE advancing: the middle act delivery-modes.md:162-180 names that was missing. `_prove_drained`: `adapter_for(route)`→`reconcile` against a new applied-ledger (`--applied`, default `.dreamwork/applied.md`); APPLIED finishes only (no write, the apply.py:318-321 branch), NOT_APPLIED writes the marker once and is reported UNAPPLIED; an unregistered route returns NOT_APPLIED (no marker — the proof covers adapter-backed routes only; the cursor advances past it once so it is not re-drained). reconcile's `finish` callback is a NO-OP: the drain's completion is the cursor advance (read-then-advance, UNCHANGED — advance only over read, EX_SOFTWARE 70 on refuse, cursor unmoved on refuse), not lane E's per-receipt claim/finish CAS; the marker IS the durable applied record. The applied-ledger is a single-generation monotonic marker log (no fork/rollback), so the proof takes the committed-lineage marker path. **By construction**: a second consume of a rewound range applies nothing twice — the adapters' writes are zero on the replay (spy at the adapter boundary over the REAL ledger; has_marker delegates to the real adapter so the proof runs, not faked). Output: `consumed N` + applied/unapplied sub-counts + one `UNAPPLIED\t<id>\t<kind>\t<route>` line per unapplied receipt (the coordinator's act-on list). **`pending` did NOT gain the proof annotation** — it would need a SECOND (read-only) proof call site alongside consume's reconcile call site; both route through the same `prove_applied` (no second semantics), but two call sites that must agree are a drift surface, and consume's UNAPPLIED list already surfaces the actionable verdict the tick needs — the brief permits skipping (shape point 2). **REDS (4, each injection + cp restore, all discriminating, NONE came back green)**: (1) apply.py `if proof is Proof.APPLIED`→`if False` → replay writes (spy 1→2), FAIL `test_command_receipt_routes_through_reconcile_replay_writes_nothing` L522; (2) `_prove_drained` bypasses reconcile (returns NOT_APPLIED) → first drain writes 0, FAIL same test L505; (3) unapplied loop iterates `applied` not `unapplied` → 0 UNAPPLIED lines, FAIL `test_unapplied_receipts_listed_in_consume_output` L558; (4) verdict collection `if verdict is apply.Proof.APPLIED`→`if True` → unapplied 0≠N, FAIL `test_consume_advances_then_pending_empty` L304. `test_journal_consume.py` 12 passed (10 existing + 2 new); `apply.py` untouched (RP1 restore byte-clean); lint clean of this change (tasks.md ERROR + questions.md WARNs pre-existing). Did not touch `watch.py`/`SKILL.md`/`delivery-modes.md` (lane-527recon + coordinator own those); the SKILL.md "consume prints receipt ids" line is now slightly stale (ids still lead each UNAPPLIED line) but SKILL.md is out of lane
- **#509** · landed `PENDING` · 2026-07-30 · by lane-509sig — ROOT CAUSE CORRECTED by live capture: the #229 phantom is NOT a signature instability over unchanged content — the entry's content is TRUNCATED on rewrite. Captured: a background monitor on the live target caught the #229 payload change at 07:44:39 — follows dropped 7→4, body unchanged — and the truncated form's old-code digest `3886be13ab6bdc63d126` is an EXACT match for the value the live `question-sigs.json` store has held since the 07:06 phantom (updated_at matches that event to the ms). The truncation deletes #229's 16:35 grok-review note (the nested-table one, ~7070 chars), the 16:48 follow-up and the 17:10 answer, and cuts the 16:12 follow-up mid-sentence at "or precise reference still"; the file ended mid-#229 (EOF) and the deleted text is gone from the file entirely (not relocated). The coordinator was actively truncating #229 DURING the investigation (follows 7→4→3 over minutes); the signature correctly fired on each real content loss. CONCLUSION: the signature is NOT buggy — it detects genuine data loss — so there is no signature/diff-logic fix for this phantom without losing teeth. watch.py does NOT truncate questions.md (collect() only reads; every writer is an append_* helper), so the truncation is the coordinator's rewrite, OUTSIDE this lane's signature-logic scope — and it is a DATA-LOSS bug (content deleted from the longest entry on rewrite) more serious than the phantom event; needs coordinator/rewrite-tool investigation. SEPARATELY SHIPPED (in-scope, red-proved, defensive — does NOT suppress the truncation phantom): `_sig_text` whitespace-normalisation in `_entry_content_digest` makes the signature invariant to body re-wrapping, a LATENT class proven at unit level (re-wrap of identical words flips the OLD digest; follow/note text is already single-spaced by the parser so the body is the sole wrap-sensitive field). The git-committed #229 body is byte-stable across every recent commit, so re-wrap is not currently the trigger — but the class is real and closed defensively. Three red-proved checks in test_watch.py (fixture derived from the real #229 trigger features — nested table, code fence, resolution head, long body — with the precondition asserted): (1) digest rewrap-invariant + (2) two same-word rewrites emit ZERO events, both RED under `_sig_text→return s` (no-op = pre-fix); (3) a real word change emits ONE event, RED under `_sig_text→return ""` (word-blind; that sabotage is a FALSE GREEN for (1)/(2), so the no-op is the discriminating red). pytest test_watch.py 375 passed 0 fail; lint clean (the one tasks.md ERROR is pre-existing on HEAD, out of lane scope per #511). lane-514wake surveys ALL wake semantics in parallel; this lane's finding is that #509's emitter is correct and the defect is upstream (rewrite truncation).
- **#532** · landed `7d46a73` · 2026-07-30 09:10 · by lane-532flake — bdinput (d)/(e) hold-to-repeat timing flake (#507 class): wall-clock sampling loop (1100ms / 700ms+550ms, delta≥3) raced the production setTimeout(400)/setInterval(80) under CPU throttling — 1/3 gate runs read delta<3 over a correct feature. Fix: Playwright page.clock — install() fakes the page's timers so runFor() fires the 400ms delay and each 80ms repeat deterministically; virtual time also advances with real time between calls so repeats fire at least as often (delta always ≥7, never <3). Zero assertion loosened. (e) audited and given the same treatment — runFor drives the hold through the mid-hold node swap. Tried pauseAt for exact-count determinism but it intermittently hung the evaluate/fetch path; the non-paused install+runFor+resume approach is reliable. 10 consecutive PASS: 7 normal (delta 7–10), 3 under 1-CPU-spinner load (delta 9–11). Red-proof: sabotaged the interval arm — setInterval period 80→80000 at watch.py:7860 (bdStepHoldStart interval arm, the production line (d) binds) → (d) FAIL delta=1 <3, (e) FAIL maxAfter==tickE.v (no repeats); cp-restored byte-identical, git status clean. (c2) typed-clamp untouched and green
- **#531** · landed `a89a0bcf` · 2026-07-30 · by lane-531through — `consume` advanced to the LIVE head, so an event landing between the coordinator's `pending` read and its `consume` in the same tick was advanced past UNREAD (the live instance: ord=43, the #505 answer receipt, committed between the pending and the consume; recovered by hand via `show`). FIX: `dev/journal_consume.py consume --through <ordinal>` bounds the advance — and the #526 proof loop — to the ordinal the prior `pending` reported as head; events beyond `--through` stay in `(cursor, head]` and are re-listed next tick. `pending` ALREADY prints the head ordinal (`ord=<n>` is one tab-field on every line; the last line's is head) so no output change was needed. **Edge semantics (both refuse EX_USAGE 64, checked BEFORE any read so a stale/bogus ordinal never silently no-ops or rewinds):** `--through` at-or-below the cursor → refuse (a rewind `through<cursor`, or a no-op `through==cursor` — neither may pass silently); `--through` above the head → refuse (cannot advance past what exists); both leave the cursor unmoved and stdout empty with a stderr explanation. Without `--through`, consume keeps today's advance-to-head semantics — the flag is the tight form the tick habit uses, not a breaking change. The #526 proof act (reconcile per drained receipt) now runs only over `(cursor, through]` — receipts beyond the bound are neither proven nor reported (they are not drained); the crash-window/exactly-once property is unchanged (a crash between proof and advance leaves the cursor unmoved; the next tick re-reads `(cursor, head]` and re-proves, and already-applied markers write nothing). **REDS (2 injections, each cp snapshot→inject→watch-fail→cp-restore, byte-identical; NEVER `git checkout`; production line named in each docstring):** RED1 reverted the bound-honouring `drained = [ev…ordinal<=through]` / `target = next(ev…ordinal==through)` to `drained=events; target=events[-1]` (ignore `--through`) → `test_consume_through_bounds_advance_leaves_late_event_pending` FAIL at the consumed-count assertion (`consumed 4 event(s)` vs `consumed 3 event(s)` — discriminating: the count rose, proving the line was reached, not a hollow green); RED2 swallowed both edge refusals (`return EX_USAGE`→`return EX_OK` in the two bounds branches) → BOTH `test_consume_through_at_or_below_cursor_refuses_usage` and `test_consume_through_above_head_refuses_usage` FAIL at `assert code == EX_USAGE` (got 0 vs 64). All cp-restored; `grep -c 'return EX_USAGE'` = 5 after restore (2 bounds + 2 cmd_show + 1 dispatch). **Tests:** `test_journal_consume.py` 16 passed (13 existing UNCHANGED — incl. the #526 unregistered-route binding — + 3 new); `test_user_events_sqlite.py` 24 + `test_user_events_apply.py` 7 green (the drain's composees). **SKILL.md clause for the coordinator (lane did NOT edit SKILL.md — coordinator-owned):** the tick habit at SKILL.md:203-219 — step 1 (line 206-207 `pending`) already prints the head `ord=<n>`; step 3 (line 211-212, bare `consume`) should become `consume --through <head-ordinal-noted-from-step-1>`, and the "Never consume without a prior `pending` read" rule (line 214-219) gains the corollary that the consume is bounded to what that prior read reported (closing the pending↔consume race this lane fixed). **Not fixed / out of lane:** the `tasks.md` "no `Next id` header" lint ERROR is pre-existing on HEAD (reproduced with my changes isolated — only `dev/journal_consume.py` + `test_journal_consume.py` touched) and out of this lane's scope; SKILL.md not edited by this lane.
- **#533** · landed `c945f26` · 2026-07-30 · by lane-533trunc — root cause NAMED with evidence: the 07:44 truncation (122 lines of #229 — the nested-table note + tail, +the #202 entry) was a **coordinator full-file `write` of questions.md from a partial read** at ~07:06 (the verbatim turn fell in the compaction-segment gap — `segment_009.md` is truncated at 524288 bytes, "29 turns omitted" after turn 271; `segment_010.md` begins turn 279 — so the act is triangulated: the #509 brief ties the loss to "immediately after the #505 append" at 07:06; 509sig's live monitor caught follows 7→4 at 07:44:39 and "the coordinator was actively truncating"; the loss is a tail-cut ending mid-sentence at `/chat route too.`→`/chat route `, not a 1000-line read (would lose ~2050) and not a serializer (none exists)). watch.py EXONERATED by code: `collect()` (`watch.py:13331`) only reads; `append_subbullet` (`:12696`) iterates every line and rejoins — cannot drop the tail; `track_question_updates` (`:13265`) writes only `question-sigs.json`. Every earlier fold preserved #229 (verified across 7 commits); `8e19b091` ran on the already-truncated file. The loss was committed UNSEEN at `0f97df03` because `git diff questions.md | head -30` (`segment_010.md:6272`) showed only the +8 answer insertions at the top, not the −130 tail at line 2925. FIX (guard, not a script — no script truncates): `check_questions_truncation` (`lint.py`) compares working-tree questions.md to HEAD; a NET line-loss over threshold 50 ERRORs (the incident is 122; the largest real fold `8e19b091` is net +3; a `groom:` marker allows a deliberate archive); fires via the PostToolUse lint hook at write-time. RED-PROOF: `test_a_tail_truncation_is_an_error` builds a git fixture (full nested-table entry committed, tail-truncated; loss asserted > threshold at runtime; guard ERRORs); sabotaging the production line `if False and lost > threshold` (`lint.py:286`) FAILS the test (hollow) — restore PASSES; `test_a_line_neutral_fold_is_not_flagged` (no false positive), `test_the_groom_marker_allows_a_deliberate_archive`, `test_no_git_baseline_is_silent`; 391 test_lint passed. Findings: `.dreamwork/docs/findings/533-truncation-root-cause.md`. Standing interim rule until dogfood-verified: coordinator writes questions.md ONLY via search_replace, never full-file write
- **#505** · landed `75e3a6a8` · 2026-07-30 · by lane-505impl — wholesale-rerender architecture fix (I5: keyed morphdom reconcile of `#view` + content-hash skip). **Increments:** I1 `10104cb8` vendor morphdom (MIT, `vendor/morphdom.min.js`) + `setContent` morphs children by `data-qid`/`aid`/`sha`/`review`/`keep`/`id`/`t0` + `lastViewHtml` hash-skip + `__dwViewRenderGen` (all snapshot pairs kept as belt); I2 `a8da8e86` `selectkeep.mjs` R1 acceptance (born-red on master: selection `""` after tick, card replaced; green under morph: `"bold"` survives, same card node, gen advances); I3 `ff8facb2` `corpse.mjs` Q4 (dreamAway strip + `viewNodeKey` refuse; inject keyed ghost detected); vacuity `216548fc` (typing/restcollapse/bdinput/dashboard/bdhover/autogrow/indicator/answers accept gen-advanced); I5 pin `1eb3eb91` (`test_live_tick` asserts `morphdom(viewEl)`/hash-skip, rejects `innerHTML=` in setContent body; watch-design `data-keep` = reconciliation key); I4a `f9d8d353` **deleted** `snapshotFolds`/`restoreFolds` (open+mid-gesture height stamped onto toEl in `onBeforeElUpdated` for `details[data-keep]`; restcollapse/typing/answers/dashboard green); `75e3a6a8` data-t0 column keys. **Pair deletion STOPPED at `snapshotBdHover`/`restoreBdHover`:** tip/insp state is module-level + hidden nodes — bdhover #494 (tip stays up / pin survives) FAILS without the pair; not pure node-open absorption. Remaining belts kept: `snapshotCardState`/`restoreCardState`, `snapshotViewInputs`/`restoreViewInputs`, `snapshotAskState`/`restoreAskState`, `snapshotReviewFrame` (review dock still `replaceWith`, phase-1 out of scope). **Binding discipline:** almost all listeners are document-level delegated (`addEventListener` on document/window); post-render binds that touch reconciled subtrees: `bindAskDraft` unbinds first then rebinds (idempotent); `restoreAnswerDrafts` declines non-empty boxes; `DraftStore.bind`/`unbind` pair. No double-bind found. **Guards:** selectkeep+corpse registered; motion morph morphhold dissolve dreamfade artifactwrap qfade wisp states restcollapse typing qsec regroup bdinput bdhover answers dashboard restcollapse PASS (focused/solo); full suite partially run under load 30–50 (bdhover focus-parity flaked once at load 50, green solo — pre-existing load shape). **Born-red R1 evidence:** `/tmp/selectkeep-bornred.log` — FAIL selection non-empty, sameCard false, selected "". **Noticed not fixed:** review-dock still `replaceWith` (Q3 phase 2); cardState/viewInputs/askState still belts (morph may reset form values from empty builder markup); ages wipe avoided only for `.age` identity match; mdBlocks/mdRender untouched (lane-525tables).
- **#423** · landed `b852277` · 2026-07-30 · by lane-423runner — dead-runner AUDIT (read-only, no production code). Verdict: **runner-signal half MOOT, lane-work exit-dirtiness gap LIVE**. The 401-exits-0 defect the task was filed against is moot under the CURRENT dispatch path (Grok `spawn_subagent`, DREAMWORK.md:364-370): the harness reports completion/failure itself, so a dead/401 model is a failed spawn, not an exit-0 background process; the `nohup ccc &` topology that made exit-0-on-401 possible is the previous harness's disused form (zero live `ccc` processes, no wrapper in repo). The dispatch-time PONG probe is moot on both paths. BUT a `spawn_subagent` lane that exits with a dirty worktree is invisible to every automated signal: `sweep` reads commit subjects only (dev/ledger.py:476), the lane-containment guard's registry is `wt/*` linked worktrees (dev/lane_guard.py:147) which harness-clone lanes are NOT (independent `.git`, not linked worktrees), and `status_sync` is pid-liveness only (status_sync.py:149). That is the #423 3rd-instance gap (gate2/da197b87). Minimal fix: coordinator-side `git status --porcelain` at lane completion — a SKILL.md/coordinator-habit change, NOT production code (no file named for a fix). Fold #423 as moot-with-evidence on the runner half; file the exit-dirtiness check as a small habit task. Findings: `.dreamwork/docs/findings/423-dead-runner-audit.md`
- **#534** · landed `0a1a3f3` · 2026-07-30 · by lane-534sig — versioned the sig store so a digest-ALGORITHM change re-seeds SILENTLY (the #509 whitespace-normalisation was the live instance: ~21 phantom question-updated events fired on the 2026-07-30 deploy because every stored digest differed under the new algo). question-sigs.json gains an `algo` field naming the digest generation (sigtext-v1 today, sigtext-v0 the unmarked pre-normalisation digests live stores hold); on load an absent/older/unrecognised algo recomputes every live entry's digest under the current algo, re-stamps the store, carries prior updated_at, emits ZERO events. Generations are an append-only list (_SIG_ALGO_GENERATIONS) so the next algo change is a one-line addition. collect() now passes both sections (open+answered) to ONE track_question_updates call — the old two-call split on a shared store let the first call stamp the store current while answered entries still held old digests, so the second fired a phantom per answered entry; one call migrates atomically. 6 new TestCollector sig tests, each red-proved by cp-restore sabotage of its named production line (re-seed skip→phantom; stamp drop→lost updated_at; un-stamped algo→real change swallowed; two-call collect→answered phantom; unknown marker trusted→phantom; unconditional re-seed→pointless rewrite); file-formats.md documents the algo field. Migration: live .dreamwork/question-sigs.json holds sigtext-v0 (unmarked) digests; the first collect after deploy re-seeds it silently to sigtext-v1 with zero events.
- **#428** · landed `7289234` `1e28930` `ee676c8` · 2026-07-30 · by lane-428suite — the guard-suite concurrency CLASS (readiness races), closed at the harness layer (watch.py/justfile/pytest/assertions untouched). Census first (.dreamwork/docs/findings/428-readiness-census.md): every dev/capture guard tabled by its two readiness layers. **Server layer — CLOSED IN FULL (8 own-sleep guards → serveVerified):** bdinput, dashboard, devoverlay, morph, morphhold, motion, posture, projtitle all spawned watch.py themselves with spawn+sleep(2500)+a hand-rolled /data.json fetch; under concurrent-lane load python outlasted the sleep → ECONNREFUSED → 'threw before finishing' (6 of 8 had no try/catch; posture had NO identity check at all and graded a stranger). Replaced with serveVerified (serve.mjs #461: spawn+poll+identity, no fixed sleep); devoverlay keeps --dev and its 2nd rsrv server got the same treatment. **Render layer — subslog (the #428-named instance) + the 8 server guards' render gate → waitFor (dom.mjs #507):** goto networkidle+sleep replaced by waitFor on the selector each guard reads first (.qa.open / .bd / .git .commit[data-sha] / #posture / #hproj). Zero assertion values/thresholds changed; lint guard-registration OK (71 registered). EVIDENCE: 5× solo PASS each incl. 2 under a 1-CPU spinner (subslog 16, posture 38, morph 13, morphhold 19, motion 19, dashboard 44, bdinput 31, projtitle 19, devoverlay 42 — all 0 FAIL at load 21-37). RED-STILL-FIRES per guard (cp snapshot→inject→FAIL→cp restore, byte-identical, porcelain clean): subslog __dwSubmissions(3234); posture #posture(6288); bdinput #bdlimit-in(3947); dashboard .gsub(3692); motion/morph/morphhold .qa card(3069); projtitle hproj fill(8850); devoverlay #devbox(10747). One green red-run acted on: motion's .gsub subject injection passed (motion binds row MOVEMENT not subject) — re-traced to the .qa structural check motion does bind. **DEFERRED with reason:** the ~40 shared-server render guards with sleep→direct reads (qacard, reflow, headertravel, states, etc.) — server layer harness-owned (the acute #428 layer is closed), generous 900-1200ms sleeps, none named in a suite failure beyond subslog; a per-guard waitFor sweep is the natural follow-up lane (enumerable in the census). motion's frame-sampling checks remain the pre-existing #414/#442 flake class (flaked once at load 37.78 on movement checks unrelated to readiness), out of this lane's scope
