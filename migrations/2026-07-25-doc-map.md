# 2026-07-25 — doc-map + repo docs as loop responsibility

## What changed

Init seeds `.dreamwork/docs/doc-map.md` when first creating the docs dir:
a cross-reference of the repo's existing docs — build on top, link
outward, never fork. The maintenance rotation's docs-freshness item now
explicitly covers the repo's own docs and the doc-map, not just
`.dreamwork/docs/`.

## How to apply

Existing targets: create `.dreamwork/docs/doc-map.md` on the next orient
(list the repo's docs, what each covers, what the loop keeps current).
Nothing else changes.
