# Coordinator handoff — 2026-07-27 16:00 AEST

To: `claude-waft-vesi-5kki`

## Authoritative state

- `master` is clean, pushed, and at `787220b` (`close(#217): deploy honest provenance coverage`).
- The reviewed dashboard is serving `watch.py` from `c1f5aaa` on `127.0.0.1:35110`, PID **62810**. `python3 deployed.py` reports `current`; master is ahead only by the ledger close.
- Run mode is **hot**.
- #217 is complete, deployed, closed, and its worktree/branch are removed. No untracked #217 scratch remains.
- No coordinator subagent remains running or owns a lane. The retained Agent records are terminal reviews/implementations only.
- Do not touch `ud-dw-generate`; it remained byte-for-byte untouched.

## What landed in this coordinator cycle

- **#213** forward-only origin contract/linter from cutoff #216 — `f9dc636`, closed `2ac24c6`; +17 red-first tests.
- **#216** immutable first-sight provenance parser/API over Git history — `e9c30ff`, closed `8e1dc25`; +23 tests.
- **#217** honest human/loop/historical-unknown coverage in the burndown — merge `c1f5aaa`, close `787220b`.
  - Visual: exact segmented hairline, textual counts/source, shallow-incomplete copy, mobile/a11y, no tick animation.
  - Cache bug found in Standards review and fixed red-first: immutable snapshots key `(revision, tree-relative ledger path)`, so root/nested targets cannot poison one another.
  - Capture bug found during review and fixed: headless GL could overwrite committed evidence with a white frame; `provenance.mjs` now buffers, checks dark pixels + live/non-lost shader before/after, retries, and writes only valid plates.
  - Evidence: `596 passed + 54 subtests`; provenance guard **22/22**; Vision + Geometry PASS; Spec + Standards PASS after fix.
- **#299** user-reported `/mtime` BrokenPipe traceback — merge `fe0351d`, closed/deployed earlier, then superseded by #217 deployment. Exact real-GET red proof; handler suppresses only pipe/reset/aborted peer departures, never unrelated errors.
- Published **#252 M1** Markdown Rendered/Source design gate (`04ef22e`); implementation remains blocked on explicit approval.
- Captured human ideas **#297** disclosure motion, **#298** burndown inspector, **#300** shared morphing run-mode descriptions.
- Folded automatic burndown/status maintenance into SQLite/state tasks **#264/#294** (`e97f3af`).

## Verification honesty

- Final merged Python/lint/diff evidence for #217 is green as above.
- The directly relevant `provenance.mjs` guard is 22/22 green repeatedly, including after the cache fix.
- I attempted a complete `just guards`; the context wrapper ran for a long time and returned **no summary**. A later focused orchestration was user-aborted. **Do not claim a final all-guards sweep.** The last known full sweep before this lane was 38/38 at `99750b2`; #217's own guard and tests are current.
- The broad guard attempt left no matching active `just guards`/capture process when checked.

## Next work

1. **#172 is the human `do-next`** (watch 14:01): put the target project name (`ud-dreamwork` here) materially more prominently in the visible title section. It is P1 and **not started**. It intentionally follows #217 because both touched shell/CSS. Preserve the invariant-edge rule, read `grok-build` and `ssh://x-game:src/codename-thin`, update `watch-design.md`, and require desktop/mobile visual convergence.
2. **#218** filed-to-landed median is now unblocked by #217, but lower priority than human `do-next` #172.
3. **#252** awaits `Accept M1`; do not implement silently. M1 is rendered-default/source-deep-link in one heading switch, safe existing `mdB`, exact source bytes, body-only atmospheric swap, RM snap.
4. New human ideas are parked as #297/#298/#300; no implementation started.
5. Claude's own #281 design-only agent/worktree is independent and was not touched by this coordinator.

## Dashboard questions

`questions.md` currently has 17 Open headings: #252, #295, #277, #284, #286, #283, #289, #290, #254, #288, #287, the #260/#262/#263/#269/#274 event-journal gate, #229/#270, ud-dreamtask stage 6, cross-machine answers, Dreamhub URL space, and #194 upgrade range.

Caveat: the **#290 question is stale** relative to already-landed #290 and should be reconciled/folded, not treated as an implementation gate. Treat the file itself as authoritative for the other current open wording; `status.json` was stale and has now been refreshed.

## Processes and authority

- Load-bearing target dashboard: PID **62810**, `~/.cache/dreamwork/deployed/ud-dreamwork-watch.py`, port 35110.
- PID 2150777 on 35111 is an unknown separate dev server; do not kill without ownership.
- Old-looking servers still observed: PID 897036 `/tmp/a250` on 43875; PID 1652343 fixture on 39951; PID 3408270 `/tmp/revieworder-green` with ephemeral port. They may be guard orphans, but inspect ownership/evidence before cleanup.
- The huge Grok c2c replay delivered during shutdown is stale historical traffic. Grok has no fresh coordinator grant from this session; old A/B/C grants were cancelled. Require a fresh acknowledgement/grant before assigning work.
- #283 installed `pi-powerline-footer` mitigation (`git --no-optional-locks status --porcelain`) still needs a **pi restart** before its quiet-window verification. After restart, inspect `~/.cache/git-lock-watch/log`; detection/recovery is not containment.

## Status / compaction-unsafe residue

- `status.json` now names #217 complete, #172 next, PID 62810, and the current open questions.
- The only non-durable fact that mattered was the all-guards wrapper's missing result; it is recorded above.
- The white-frame evidence failure and cache contamination are durable in commits/tests, not merely chat knowledge.
- The unrelated `site/` preservation instruction still applies if it reappears, though the current main checkout is clean.

## Stop condition

Max requested handoff after this item. This coordinator has stopped selecting new work after closing #217. Continue from #172 unless Max redirects.
