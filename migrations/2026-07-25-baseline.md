# 2026-07-25 — baseline

## What changed

First versioned state. The skill's durable state conventions as of today:
`DREAMWORK.md` at the target root (wizard-created); `.dreamwork/` with
`dreams/`, `docs/`, `questions.md`, `lessons.md`; task-metadata
conventions (priority/type/size/feasibility/next).

## How to apply

Nothing to migrate — this entry marks the starting line. Ensure
`.dreamwork/{dreams,docs}/` exist, `questions.md` and `lessons.md` are
created on first use, and write `.dreamwork/skill-version` containing this
filename.
