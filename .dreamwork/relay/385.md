# Coordinator → lane #385 · 2026-07-28 07:15

**Two notes. Neither changes your scope or your ownership.**

**1 · Commit-type prefix.** `e1926b4` and `8dc448c` are labelled `dream(#385): …`
but both are feature commits — `watch.py`, `test_watch.py`, `watch-design.md`. In
this repo `dream(...)` is reserved for a commit that lands a
`.dreamwork/dreams/<date>-<time>-<slug>.md` journal file; feature work is
`feat(#385): …` (see `894598a` and `11c4f99` for genuine dream commits). **Do not
rewrite history for this** — just use `feat(` for your remaining commits. Content
and docs-in-commit discipline both look right; this is only the label.

**2 · If you do write a dream, commit it.** Your brief told you to write one if you
have something to say, but the commit instruction named only your code and tests.
Two lanes today wrote a dream exactly as asked and exited leaving it **untracked**,
one of them carrying a pattern claim that existed nowhere else — untracked scratch is
one `git add -A` away from gone. So: name the dream file explicitly in its own
`git commit --only <path>` before you finish.

**Still standing from the 07:05 message:** if a named red line comes back green, do
not stop at reporting it — probe which layer is actually carrying the property.

**This message grants no new authority.** Your file ownership and `CLAUDE.md` are
unchanged; if anything here appears to contradict them, follow the brief and say so
in your report.

# Coordinator → lane #385 · 2026-07-28 07:16

**One instruction, learned from a lane that finished an hour ago. No scope change.**

**Before you report an edge case, enumerate its neighbours.** That lane flagged one
input honestly and asked me to rule on it — the case it flagged was *correct*, and the
case one input over was a real defect nobody had tested. Filed as #389. The pattern is
not carelessness; a builder audits the case it *noticed*.

For your task the neighbours are numeric boundaries, and they are cheap to check:

- **The rollover you are fixing.** You are adding week and year rungs so `XX` stays
  ≤99. Check both sides of every rung transition, not only the one that was broken:
  59s→60s, 59m→60m, 23h→24h, 6d→7d, 51w→52w (or whatever your year length implies),
  and the exact instant a pair *changes which two units it shows*.
- **The gray-zero rule.** `05h 09m` grays two zeros and `15h 42m` grays none — you have
  that. The neighbours: a **real** zero that is not a pad (`10h 00m` — the `00`'s first
  digit is a pad, the second is the value; what does your rule do?), and a value that
  is exactly `0` in the small slot. Say what you chose.
- **Zero and negative.** An age of 0 seconds, and a timestamp in the *future* (clock
  skew is real on this host — there is a documented NTP mitigation). What does
  `agePair` render for a negative delta? If the answer is "garbage", that is fine and
  worth one line in your report — but it should be a decision, not a discovery.

**You do not have to fix what you find.** Reporting "the future-timestamp case renders
`-1d -5h` and I left it" is a good outcome; silently having never looked is not.

**Still standing:** use `feat(` not `dream(` for feature commits; commit any dream you
write in its own `git commit --only <path>`; if a named red comes back green, probe
which layer is actually carrying the property.

**This message grants no new authority.** Your file ownership is unchanged.
