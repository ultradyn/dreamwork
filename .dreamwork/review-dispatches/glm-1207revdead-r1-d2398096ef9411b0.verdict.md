# Review verdict — glm-1207revdead round 1 (@cx-reviewer)
# Recovered from the launcher stdout log 2026-08-04 (see #1214: the reviewer wrote no
# inbox heading and no review decision). Verbatim tail below.


Live counterexample confirmed. `--review-status` returned:

```text
glm-1072answers-review-r2-a1efec1823b48462  runner-absent  glm-1072answers r2  — runner gone from glm-1072answers-review-r2 worktree; runner_exit never observed; the dispatch recorded no outcome — the review is no longer progressing (a dead review, or one whose verdict landed elsewhere; check inbox/ledger reviews)
```

Its launch witness has `runner_exit: null`, while the preserved [verdict](/home/xertrov/.llm-general/skills/ud-dreamwork/.dreamwork/review-dispatches/glm-1072answers-r2-a1efec1823b48462.verdict.md line 1) shows a completed 252,251-token review with three P1 findings. So this is exactly the predicted false alarm.

That makes `runner-absent` low-precision, but still a real improvement: it is best understood as “delivery outcome unobserved; reconcile now,” not “review proven dead.” Previously this incident looked benign and prompted no check; now it points directly at the delivery defect in #1214. It must not itself trigger an automated failure/merge decision.

Requested checks:

- Slow review: pass. The new test does not stub a `live` boolean; it feeds a fake `/proc` row through the real classifier and shared cwd probe, with a matching worktree cwd and recognized `ccc` argv. The existing liveness test additionally uses a real process and actual `/proc/<pid>/cwd`. Duration is not used, so a 20-minute live reviewer remains `in-progress`.
- Probe honesty: pass. [The probe](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 899) establishes containment from `/proc/<pid>/cwd` before consulting argv; argv is only a basename check for a known runner. [Examined zero](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 982) returns `unknown`, never `runner-absent`.
- PID reuse: no PID is stored in the review record. The classifier scans current processes, so literal stored-PID reuse is not the mechanism. But it is not identity-safe: any later `ccc`/`claude`/`grok`/`codex` process whose cwd is that review worktree reports `in-progress`; no start-time, lane ID, or reviewer-role check distinguishes it. That can mask a dead original review indefinitely while the unrelated runner persists.
- Documentation: [the row](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/file-formats.md line 407) correctly names `--review-status`, its read-only behavior, and the three currently reachable `runner_exit: null` outcomes. Its `JSON fields:`/`. Meanings:` anchors remain intact; [the test](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/test_dispatch_lane.py line 1313) will still extract the exact field set. `terminal` exists only for a future non-null `runner_exit`, which the row correctly says is not currently produced; update the row if that producer changes.
- Scope: this diff does not make #1215 or #1216 harder. It adds a read-only, mutually exclusive status path and no schema or launch-policy coupling.

## Standards

No documented-style breach or meaningful code smell beyond the P1 robustness failure above. Targeted classifier/status tests passed: `7 passed`.

**Verdict: MERGE WITH FIXES** — fix the malformed-record handling before merge.
hook: Stop
hook: Stop Completed
tokens used
124,093
Reviewed `064f699a1251060910ccc3af5862b2f485e4cd3d`. Both this worktree’s `HEAD` and `glm-1207revdead` resolve to that SHA; no branch-tip mismatch.

## Spec

**P1 — malformed launch records can abort the entire status report.** In [review_status](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 1021), decode failures set `category/detail` but leave `record` unset (or stale from a prior iteration), then [dereference it](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 1028). A first corrupt `.launch.json` raises `UnboundLocalError`; one after a good record can display the previous record’s identity. A syntactically valid non-object JSON value also reaches `record.get()` in [classify_review_dispatch](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 967) and crashes.

This defeats the intended per-record `unknown` result and can hide every other alarm. Initialize/reset `record` per iteration and treat non-object JSON as `unknown`; add tests for invalid JSON first/after-valid and `[]`.

Live counterexample confirmed. `--review-status` returned:

```text
glm-1072answers-review-r2-a1efec1823b48462  runner-absent  glm-1072answers r2  — runner gone from glm-1072answers-review-r2 worktree; runner_exit never observed; the dispatch recorded no outcome — the review is no longer progressing (a dead review, or one whose verdict landed elsewhere; check inbox/ledger reviews)
```

Its launch witness has `runner_exit: null`, while the preserved [verdict](/home/xertrov/.llm-general/skills/ud-dreamwork/.dreamwork/review-dispatches/glm-1072answers-r2-a1efec1823b48462.verdict.md line 1) shows a completed 252,251-token review with three P1 findings. So this is exactly the predicted false alarm.

That makes `runner-absent` low-precision, but still a real improvement: it is best understood as “delivery outcome unobserved; reconcile now,” not “review proven dead.” Previously this incident looked benign and prompted no check; now it points directly at the delivery defect in #1214. It must not itself trigger an automated failure/merge decision.

Requested checks:

- Slow review: pass. The new test does not stub a `live` boolean; it feeds a fake `/proc` row through the real classifier and shared cwd probe, with a matching worktree cwd and recognized `ccc` argv. The existing liveness test additionally uses a real process and actual `/proc/<pid>/cwd`. Duration is not used, so a 20-minute live reviewer remains `in-progress`.
- Probe honesty: pass. [The probe](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 899) establishes containment from `/proc/<pid>/cwd` before consulting argv; argv is only a basename check for a known runner. [Examined zero](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/dev/dispatch_lane.py line 982) returns `unknown`, never `runner-absent`.
- PID reuse: no PID is stored in the review record. The classifier scans current processes, so literal stored-PID reuse is not the mechanism. But it is not identity-safe: any later `ccc`/`claude`/`grok`/`codex` process whose cwd is that review worktree reports `in-progress`; no start-time, lane ID, or reviewer-role check distinguishes it. That can mask a dead original review indefinitely while the unrelated runner persists.
- Documentation: [the row](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/file-formats.md line 407) correctly names `--review-status`, its read-only behavior, and the three currently reachable `runner_exit: null` outcomes. Its `JSON fields:`/`. Meanings:` anchors remain intact; [the test](/home/xertrov/.llm-general/skills/.worktrees/glm-1207revdead-review-r1/test_dispatch_lane.py line 1313) will still extract the exact field set. `terminal` exists only for a future non-null `runner_exit`, which the row correctly says is not currently produced; update the row if that producer changes.
- Scope: this diff does not make #1215 or #1216 harder. It adds a read-only, mutually exclusive status path and no schema or launch-policy coupling.

## Standards

No documented-style breach or meaningful code smell beyond the P1 robustness failure above. Targeted classifier/status tests passed: `7 passed`.

**Verdict: MERGE WITH FIXES** — fix the malformed-record handling before merge.
>> ccc:output-log >> /home/xertrov/.local/state/cc-p/ccc/runs/codex-1785799682870-1355412-0
